The decisions taken while wireframing each page-type. They bind later chats — follow them rather than re-deciding.

> **Note:** The pre-scaffold history file was not present after the Next.js install. Decisions 1–54 should be restored from backup when available. Port decisions 55–58 are logged below.

---

## Next.js port (global)

**33. Home omits the page-header band** — Home has no crumb and no H1; it opens on HM1. ~~Implemented as `suppressPageHead` on the home route while the band lived in layout.~~ **Revised at the page-level band unit:** the band is composed in each page shell, not layout. Home simply does not mount it. `suppressPageHead` is gone. Programmes, Who We Are and Our Team no longer omit the band — C1 and AB1 are overview / intro only (decision 152). _Home is the only page whose opening is not a titled document._ → Set aside: a band on Home too — would put a crumb and H1 above the cinematic hero. **Restored here from the port (decisions 1–54 were missing after the Next.js install) so the revision has a home.**

**55. State panel mechanism** — a client context provider holds the state bag; the list of toggles per page-type comes from a static map keyed off the route registry. _Block bodies read state values to decide what they render, so a provider is required regardless; pages registering states separately would duplicate that channel._ → Set aside: pages self-registering with the panel — simpler API, but two channels for the same values.

**56. Real nested URLs** — App Router paths replace legacy flat hashes. `/` is Home; the wireframe index lives at `/wireframes`. _Real routes with shareable URLs; the iframe hash problem decision 17 solved no longer applies._ → Set aside: keeping flat hash-style paths — would fight the App Router and lose readable URLs.

**57. Legacy border weights kept** — ~~2px on header rule and primary buttons~~ **Revised at the website-shell unit:** header/footer 2px rules no longer exist — the rail edge is 1px; 2px stays on primary buttons only (decision 154); 3px on N8 (and N15 quotes) callouts; 2px dashed on stubs and the state panel. ~~2px on active tabs~~ **Revised at the tabs unit:** active tabs are 1px `border-neutral-200` on a light strip, not 2px black (decision 146). ~~3px on C6~~ **Revised at the C6 unit:** C6's set-apart treatment is a `bg-neutral-200` fill with padding and no border (decision 144), not weight. ~~3px on AB2~~ **Revised at the About polish unit:** AB2 is a highlight, not a quotation — same grey fill as C6 (decision 148). N8 and N15 stay 3px. _Weight is the legacy file's only emphasis signal for quotes and key-detail lines; C6 and AB2 are highlights, not quotations. The 2px header/footer rules belonged to stacked chrome that decision 154 replaced._ → Set aside: strict 1px everywhere — obeys the original AGENTS constraint but makes N8 indistinguishable from prose blocks.

**58. Type scale collapsed** — legacy 10/11/12px → `text-xs`; 13/14px → `text-sm`; body base `text-sm`. `--fill:#eee` → `neutral-200`; `--soft:#666` → `neutral-500`. _Maps the legacy palette onto the permitted Tailwind set without inventing arbitrary sizes._ → Set aside: preserving exact pixel sizes — outside the permitted utility set.

**59. Square timeline markers** — S1 programme timeline uses 2.5×2.5px square markers instead of the legacy round dots. _The permitted Tailwind set excludes `rounded-*`; a square marker preserves the sequence cue without breaking the lo-fi constraint._ → Set aside: round dots via inline styles — would work but adds a one-off exception outside the utility set.

**60. Single client boundary at ProgramPage** — wireframe state (`closed`, `offered`) is read once in `ProgramPage` and passed down as props; block components stay server-renderable except where a child primitive requires interactivity (`Accordion`). _One boundary is easier to trace than scattering `"use client"` across C7 and S3._ → Set aside: per-block client components — each block self-contained, but duplicates the state channel. **Note:** S5 is a client block as of decision 143 — it owns its Individual/Group tab index the same way Accordion owns its toggle (decision 64). Wireframe state still enters once at `ProgramPage`.

**61. C3 is facts; sequenced dates are S1** — ~~`C3AtAGlance` takes a `mode` prop (`"flat"` | `"rounds"`) rather than the legacy's two separate functions.~~ **Revised:** C3 is a wrapping card grid of facts (decision 140); sequenced dates — Wijhat's rounds, Production Awards' open/deadline/results — live in S1 (decision 145). Rounds mode is dropped; Wijhat was the only consumer. **Revised at the Abbara S4/C3 unit:** C3 retires. Sequenced dates stay in S1. Scope facts live in S4; compliance rules live in C8. The C3 code is not reused. _Same treatment, same block code; one component per block is the component-discipline rule._ → Set aside: two components (`C3flat`, `C3grouped`) — mirrors the legacy literally, but splits one block into two files.

## Publications

**62. Shared wireframe primitives over Publications-local ones** — `FilterBar`, `CountRow`, `Card`/`Cover`, `Embed` and `EmptyState` live in `components/wireframe/` because Mawred Network and News will reuse them. _One primitive per pattern; the unit that first needs a primitive owns it for the whole app._ → Set aside: Publications-local copies — faster to ship but duplicates MN3/MN4 and N1–N3 work later.

**63. `Chip` reused for removable filter chips** — active filter chips carry the `×` in the label rather than a separate `.chipx` size variant. _Decision 58 already collapsed the legacy size difference; one chip component is enough._ → Set aside: a dedicated removable-chip variant — mirrors legacy markup literally but adds a one-off size fork.

**64. `FilterBar` self-contained as a client component** — facet open/close state lives inside `FilterBar`, the same way `Accordion` owns its toggle. Page shells (`LibraryPage`, `SeriesPage`) remain the single wireframe-state boundary per decision 60. _Interactive primitives own their own UI state; the page shell only reads panel toggles._ → Set aside: lifting facet state into `LibraryPage` — one fewer `"use client"` file, but spreads interactivity up a tier.

**65. Fixed embed footprints over aspect ratios** — audio and video embeds use fixed height boxes rather than aspect-ratio utilities; the SoundCloud embed has no meaningful aspect to preserve. _Footprint difference is the wireframe signal; aspect-ratio would imply precision the provider chrome doesn't support._ → Set aside: provider-specific aspect ratios — closer to live embeds but outside the lo-fi constraint and hard to maintain across providers.

## Mawred Network

**66. Entity-tab state in DirectoryPage** — the Individuals & Groups / Organizations & Initiatives tab index lives in `DirectoryPage`, not inside the `Tabs` primitive, because MN4 (count row) and MN5 (listing) both need the same value. _One source of truth at the shell; the tabs are navigation, not a wireframe-state toggle._ → Set aside: self-contained tab state inside `Tabs` — simpler primitive API, but MN4 would need a callback or duplicate state.

**67. Tabs and DirRow as shared primitives** — `Tabs` and `DirRow`/`Badge` live in `components/wireframe/` ahead of the News unit, which reuses the same tab row and will need a row pattern for the post feed. _One primitive per pattern; the Network unit that first needed them owns them for the whole app (extends decision 62)._ → Set aside: Network-local copies — faster to ship but duplicates N1/N3 work later.

**68. Cover label prop over a second cover component** — `Cover` takes an optional `label` prop (default `"cover"`) so MN6 can render `photo / logo` without a separate portrait/logo component. _Same footprint, same block treatment; one component per pattern._ → Set aside: a dedicated `PortraitCover` — clearer naming, but two components for the same box.

## About

**69. Timeline extracted as a shared primitive** — the rail-and-marker treatment moves to `components/wireframe/Timeline.tsx`, taking `label` / optional `meta` / `fill` per item; S1 (programme timeline) is refactored onto it and AB5 (history) uses it. _The legacy file already shared one `.tl` rule between both blocks, and the rail is real styling — absolute marker, left border, transparent last row — not two utility classes. Decision 45 keeps About's **blocks** out of the programme kit; it doesn't require duplicating a primitive._ → Set aside: an About-local timeline — literal reading of decision 45, but two copies of the same rail to keep in step.

**70. AB3 mission cells not extracted** — ~~AB3 repeats the cell-row markup that S4 (programme summary) already carries rather than pulling a shared `Cells` primitive out. _Three utility classes and a one-line cell body; the pattern's third user (HM4) isn't built yet, so extraction is better decided when Home lands and the real variation is visible._~~ **Superseded by decision 80** — Home landed with HM4 as the third consumer; `Cells`/`Cell` extracted and S4/AB3 refactored onto it.

**71. AB7 bio expander reuses `AccordionItem`** — ~~the per-card "Read bio" expander is the shared accordion primitive, not a smaller About-local one.~~ **Revised at the About polish unit:** AB7 no longer uses AccordionItem. Records with a bio get a disclosure arrow next to the name (`▸` / `▾`); the name row toggles the bio in place. Records without a bio stay static (decision 48). Default Accordion stays boxed for S3 and C11. AB7 is a client block owning open/closed state (decision 151). _One interactive primitive per pattern (decision 64) still holds for accordions; a people-card disclosure is a different job from a labelled expander._ → Set aside: a compact card-sized expander — closer to the legacy density, but a second toggle component doing the same job.

**72. Banner extracted as a shared primitive** — the full-width 210px fill box moves to `components/wireframe/Banner.tsx`, taking a `label` prop; AB10 (initiative record) uses it first, with HM5 and N6 to follow. _The legacy file already shared one `.banner` rule across three blocks; extracting now avoids a third copy when Home and News land (extends decisions 62/67)._ → Set aside: an About-local banner — literal per-page ownership, but duplicates the same box for News and Home later.

**73. `EmptyState` action prop** — `EmptyState` takes an optional `action` string (default `"Clear all filters"`) so AB12 can pass `"Contact Mawred"` without a Careers-local empty component. _One empty-state primitive per pattern; the button label is the only variable._ → Set aside: a second empty-state variant — clearer intent in the type name, but two components for the same dashed box.

**74. `KV` fill prop** — `KV` takes an optional `fill` boolean (default `true`) so AB13's "Or write to — [contact address]" row renders label-only with no value bar. _Same row component; the value column is optional per field, matching publication metadata omission._ → Set aside: a label-only row hand-rolled in AB13 — works, but forks the row pattern for one field.

## News

**75. `Tag` as a shared primitive distinct from `Badge`** — the uppercase tracked label box lives in `components/wireframe/Tag.tsx`, separate from `DirRow`'s `Badge`. _Under the collapsed type scale (decision 58) the only surviving difference is `uppercase` + `tracking`, and that difference is the whole signal for category labels on cards and N7's meta strip._ → Set aside: reusing `Badge` with an uppercase class — one component, but loses the semantic split between directory row labels and taxonomy tags.

**76. `Caption` extracted as a shared primitive** — the dashed optional-field box moves to `components/wireframe/Caption.tsx`; PB2 is refactored onto it so the news card excerpt is not a second copy. _One primitive per pattern (extends decisions 62/67); the legacy `.cap` rule was already duplicated inline in PB2._ → Set aside: hand-rolling the dashed box in each block — mirrors the legacy literally but forks on the second consumer.

**77. `Card` footer prop** — `Card` takes an optional `footer` ReactNode rendered after the subtitle, so the news card's excerpt slot sits below the date without overloading `children`. _Category tag stays in `children` beside `Cover`; the excerpt is structurally below title and date, matching the legacy card order._ → Set aside: putting the excerpt in `children` after the title — simpler API, but breaks the date-then-excerpt sequence.

**78. Category-tab state in NewsLandingPage** — the All / Announcements / News & Events tab index lives in `NewsLandingPage`, not inside `Tabs`, because N2 (count row) scopes its count to the open tab (extends decision 66). _One source of truth at the shell; the tabs are navigation, not a wireframe-state toggle._ → Set aside: self-contained tab state inside `Tabs` — simpler primitive API, but N2 would need a callback or duplicate state.

**79. `NewsDetailPage` as single client boundary** — wireframe state (`structured` / `byline` / `edge`) is read once in `NewsDetailPage` and passed down as props; optional blocks N8/N10/N11 render by omission when `structured` is off (extends decision 60). _One boundary is easier to trace than scattering `"use client"` across nine blocks._ → Set aside: per-block client components — each block self-contained, but duplicates the state channel.

## Home

**80. `Cells`/`Cell` extracted as shared primitives** — the flex-wrap cell row moves to `components/wireframe/Cells.tsx`; S4 (programme summary), ~~AB3 (mission) and~~ HM4 (programmes overview) refactored onto it, resolving decision 70's deferral. **Revised at the About polish unit:** AB3 leaves Cells for a gapped card grid matching AB4 (decision 149); ~~S4 and HM4 stay a tight shared-border row.~~ **Revised at the S4 unit:** S4 leaves Cells for a 2-up gapped, borderless grid (decision 160); HM4 remains the Cells consumer. **Revised at the Wijhat S4/C5 unit:** S4 is stacked rows of item cards, not a 2-up grid (decision 160 revised). _Three consumers with the same wrapper and only the cell body differing; extraction on two samples was premature, but Home made the variation visible._ → Set aside: leaving three inline copies — worked at two samples, but HM4 confirmed the pattern before a fourth user appeared.

**81. HM1 hero frame stays local** — the 300px cinematic frame (note top-left, caption chip bottom-left, optional marker bar) stays inside HM1 rather than extending `Banner` with internal slots and a second height. _Banner is a single fill box (decision 72); the hero frame carries structure Banner was never meant to hold._ → Set aside: a taller `Banner` variant with slots — one primitive, but overloads `Banner` with hero-only internals.

**82. `HomePage` as single client boundary** — wireframe state (`nocalls` / `twentieth` / `staticfb`) is read once in `HomePage` and passed down as props (extends decisions 60 / 79). _One boundary is easier to trace than scattering `"use client"` across eight blocks._ → Set aside: per-block client components — each block self-contained, but duplicates the state channel.

## Next.js port (continued)

**83. Page shells co-located with routes** — the 16 `<Name>Page.tsx` composition files move out of `components/blocks/<page-type>/` and sit next to the route that renders them; `page.tsx` stays a two-line wrapper. Shared shells (`ProgramPage`, `SeriesPage`) sit at the route-group level (`app/programs/`, `app/publications/`). _A shell is route composition, not a reusable block; `components/blocks/` holds blocks only, and a shell has exactly one caller so distance from the route bought nothing._ → Set aside: inlining the shell body into `page.tsx` — one file per route, but loses the named shell and forces `"use client"` onto the route file; `components/pages/<page-type>/` — clean split, but no closer to the route than before.

## Content population

**84. Content props are optional, with the placeholder as the fallback** — every prop added to carry real content (`Prose.text`, `KV.value`, `Figs` item values, and the C1/C3/C4/C5/C8/C9/C10/C12 additions) is optional, and a block given nothing renders exactly the fill bars it rendered before. _Population runs one page-type per unit over shared blocks; a required prop would force every other page-type to be populated in the same session, or hand-fed empty strings until its own turn came._ → Set aside: separate populated and unpopulated variants of each block — explicit about which is which, but doubles the block kit and breaks the one-component-per-block rule; required props with placeholders passed from config — moves the fill-bar decision into nine config files.

**85. Eligibility detail folds into C8** — the Guidelines-tab eligibility rules (re-application, who submits per discipline, under-18 documents) render as C8 items alongside the exclusions and application terms; ~~C3 keeps only the one-line eligibility summary~~ **Revised:** Production Awards no longer has C3 (decision 158). The under-35 / Arab-origin line lives in C1; the detail stays in C8. ~~Abbara still uses C3 for structured eligibility (decision 98).~~ **Revised at the Abbara S4/C3 unit:** C3 retires. Abbara's structured eligibility splits — disciplines and where-they-work to S4 (scope); budget-band fused with years operating, registration, and previous-beneficiary to C8 (rules). The invented "Budget band" label is gone; the sourced figures survive. C8 on Abbara is nine items and is no longer only form mechanics. ~~**Revised at the Production Awards S4 unit:** who-submits and funds-in-the-region leave C8 for S4.~~ **Rolled back:** those facts stay in C8. Production Awards has no S4 (decision 158). _Eligibility has no block of its own, and the detail is a list of conditions — the same shape as the rest of C8._ → Set aside: a new eligibility block — cleaner semantics, but re-opens structure the spec settled, for all four programmes; expanding C3 with the detail — no new block, but buries the dates the table exists to surface. **Note:** a titled "Who is eligible" S4 was reserved, then tried, then rolled back — the facts already have homes in C1 and C8.

**86. Wijhat's covered-costs list goes in S4's "The grant" cell, not a C5** — ~~the €7,000 ceiling and the five cost types it covers (tickets, visa costs, accommodation, partial living expenses, internal transport) render as a lead plus a bullet list in the page-top summary cell; Wijhat gets no C5 block.~~ **Revised at the Wijhat S4/C5 unit:** S4 drops "The grant". Wijhat gets a single-record C5 — label "Travel grant", chip "Up to €7,000", the five covered costs as `items`. Heading stays the default "Forms of support". C1 already states the ceiling and the partial-living-expenses caveat as prose, so the list is not restated there. _The set-aside is now the decision. S4 is the scope summary (who / what-for / where); C5 is what you get. A one-record C5 is the honest itemisation once that split holds on every programme, and the list appears once._ → Set aside: ~~a single-record C5 — gives the list its own labelled block and keeps S4 cells short, but a one-row support table isn't a support table, and the list would then appear twice on one page.~~ keeping the list in S4 — what this decision originally required, but it mixed "is this me?" with "what do I get?" and blocked C5 from applying to all four programmes.

**87. The visa recommendation letter is a C6 note, placed after S5** — the provision that Culture Resource can write to the embassy once the contract is signed renders as the highlighted-note callout, immediately below the application paths. _No block owned it, and C6 is the kit's slot for exactly this: one short provision that matters more than its length. Sitting after S5 it answers the "grantees obtain their own visas" line the visitor has just read, and visas are named in C1's problem framing, so the answer belongs on the page rather than buried in contract terms._ → Set aside: a line in each S5 path — where the live page puts it, but it's stated twice there and reads as a step in applying, which it isn't; a C10 item — accurate, since it is post-signing, but it would only be found by someone already reading the contract terms.

**88. S5's shared core holds everything both tracks share; each path holds only its divergences** — the portal sequence, the save-and-return form and the purpose statement sit in the core; the Individual and Group sections carry only what differs, plus their own document lists. _The two live tracks share about 80% of their content and the block already has a shared-core element for precisely this. Repeating the common flow under both headings would bury the only thing a visitor is there to find — which track they're in and what it costs them._ → Set aside: reproducing each track's full flow — mirrors the live page literally, but duplicates 80% twice and makes the two paths look interchangeable. **Note:** the two paths render as tabs as of decision 143; the core still sits above them.

**89. Schedule rules live in S6, not C8 or C3** — the three deadline rules (no announcement before the stated date, screening starts immediately after the deadline, a late application rolls to the next round if eligible) render as notes under S6's when-to-apply rows, alongside the two-date rule. _They are rules about the schedule, and S6 is the schedule-reasoning block; the live page states them between the rounds and the when-to-apply logic. C8 is a list of eligibility and application conditions and they would be lost in it._ → Set aside: C8 items — one fewer prop on S6, but separates the rules from the dates they qualify; C3 rows — closest to the live page's order, but decision 85 keeps C3 as the scan-in-five-seconds table and prose rules would end that.

**90. C4 owns the required-documents list; S5 delegates to it** — the "Documents to include" label and list move into `C4ApplySteps` as an optional `documents` prop, and `S5ApplicationPaths` passes each path's list down to the nested C4 instead of rendering its own copy. _Stand for Art needs the list on a plain C4 with no application paths, and the markup S5 was hand-rolling is exactly what C4 should render — one component per pattern. The nested C4 renders a fragment, so the delegated markup lands in the same position with the same classes and Wijhat's output is unchanged._ → Set aside: a second hand-rolled list in C4 — no change to S5, but two copies of the same list to keep in step; a shared `DocumentList` primitive — cleaner in the abstract, but the list only ever appears inside a C4. **Note:** the nested C4's steps render as numbered cards as of decision 141; the fragment contract and the documents list below them are unchanged.

**91. The two privacy notes split — non-resumable form into C4 `note`, confidentiality into C4 `highlight`** — the live page's two adjacent notes end up in different fields on the same block: "the form cannot be saved and resumed" stays C4's `note` (below the steps), and "all information is confidential and used internally" becomes C4's `highlight` (label + text), rendered at the top of C4 in the same grey treatment as C6. ~~data confidentiality into C6, and C6 stays ahead of the apply flow~~ **Revised at the C6 unit:** Stand for Art no longer has a page-level C6. Wijhat (visa letter) and Abbara (core-funding exclusion) keep C6. _They still answer different questions. The non-resumable form is a caution about how to prepare and sits with the steps; the confidentiality assurance is what an at-risk applicant needs before writing, so it sits above the steps rather than below them. After C1 owns the opening (decision 137), a page-first C6 made Stand for Art open on a note rather than on what the programme is — the apply block is already the first thing an applicant reads after the overview._ → Set aside: both notes in C6 — mirrors the live page's adjacency, but buries a practical instruction inside a reassurance; both as C4 `note`s — keeps the source order, but demotes the page's defining fact (audit §3) to fine print under a step list; keeping C6 first on the page — what this decision originally required, but it duplicated the highlight treatment as its own block once C1 is the opening.

**92. C9 carries the whole five-step selection process, ending with the signed agreement** — the block gains an optional `process` list rendered as an `<ol>`, and Stand for Art's fifth step (agreement signed between Culture Resource and the beneficiary) stays in it. _The five live steps are a sequence, not criteria, and this programme's whole contractual footprint is that one step — there is no C10 to move it to, and dropping it would leave the page ending at "we will tell you our decision". `criteria` stays for the programmes that have a criteria list; Stand for Art has none._ → Set aside: rendering the steps as `criteria` — no new prop, but a numbered process rendered as an unordered list of criteria misstates what it is; a C10 holding the signing step alone — technically the right block, but a one-item contract-terms block on a page with no installments, windows or clawback reads as a stub.
    90|
**93. C5 records take an `items` list, and the Emergency fund chip reads "Set case by case"** — each support record gains an optional bullet list under its optional `body`, and the amount chip carries a phrase where the programme has no figure. _"Up to six months" attaches to the living-expenses line only; compressed into one sentence it would read as bounding all four cost types. And the live page states plainly that there are no preset figures, so the honest chip is the words — the placeholder `amount` chip would read as an unpopulated block, and any number would be invented._ → Set aside: a single prose `body` per record — no new prop, but loses which cost the six-month limit applies to; no chip at all on Emergency fund — accurate that there's no figure, but leaves the visitor unable to tell whether the amount is missing or deliberately variable.

**94. "Individuals only" is promoted out of the FAQ into C8** — the FAQ's answer that organisations can't apply becomes a C8 eligibility item, ~~and the FAQ keeps the finer reading~~ **Revised at the Stand for Art unit:** the C8 item carries the affiliated-vs-independent clause verbatim, so the FAQ was stating the rule twice. The question stays; its answer is now "No. Organisational support sits with Culture Resource's other programmes" — the redirect, which is the only thing C8 doesn't say (decision 161). _It is an eligibility rule, and C8 is where this kit puts eligibility rules (decision 85). A visitor scanning conditions shouldn't have to open an accordion to find out the programme excludes them._ → Set aside: leaving it in C11 only — matches the live page, but hides a hard eligibility bar in an FAQ; ~~removing it from C11 once promoted — no duplication, but the live question is asked because people ask it, and the FAQ answer adds the affiliated-vs-independent nuance.~~ dropping the question along with the clause — no duplication, but the live question is asked because people ask it, and the redirect is a real answer. **The nuance argument no longer holds: C8 took the nuance with the rule.**

**95. Rolling intake gets its own states key, and `rolling` overrides the closed state** — Stand for Art uses a new `programRolling` states key with no toggles and a note naming both absences, and `ProgramConfig.rolling` makes `ProgramPage` pass `closed={false}` so a `closed` value carried in from another programme page can't take effect. _The panel was offering "Applications closed" and `Show "offered" blocks` on a page with no rounds, no deadlines and no S3, and C7's closed render names a next round that doesn't exist — a state that can't occur shouldn't be togglable, and shouldn't leak in from a sibling page._ → Set aside: leaving the `program` key in place and flagging it in the log — no code change, but the panel would keep offering a state the programme can't be in; making the closed state per-block — more granular, but the whole point is that this programme has no call status at all.

**96. The beneficiary map is not wireframed, and the directory renders at page end** — Abbara's page carries no map block; C2 carries the impact figures (87 supported · 60 active · 27 ceased · 8 rounds) and S3 stays last in the block list, published unconditionally. _Already owned by the component register: the live geo-map is dropped from the proposal on design-clash and embed-styling grounds with the stat strip carrying impact instead, and the directory is pushed to page-end from its live position above everything. The register logs a rebuild as a client-revisit option, so wireframing one now would pre-empt a call the client hasn't made._ → Set aside: laying out a map placeholder — shows the client what they'd be choosing, but a dashed box labelled "map" is the design decision, not a question; keeping the directory at page top as the live site has it — faithful to today's page, but it puts 56 organisation names ahead of what the programme is.

**97. S4 gets two cells, not four, and the leading question marks are dropped** — ~~the hero carries the live page's two above-tab headings ("Who is Abbara for", 2 bullets; "What kind of support does the programme offer", 4 bullets), and the port's four bare labels go.~~ **Revised at the Abbara S4/C3 unit:** the support cell is dropped — C5 already carries all four components with the €22,000 chip, so the cell was a duplicate. S4 keeps "Who is Abbara for" and gains Disciplines covered (twelve chips) and Where they work from the retired C3. The port's four bare labels stay gone. _The four were placeholders borrowed from Wijhat's shape; the live page has exactly two summary headings and the cell row reads fine at two. The question mark rendering at the front of both headings is the RTL/punctuation CMS artifact already flagged on Wijhat, so it is dropped rather than reproduced or silently moved to the end. The support cell's four bullets were the same four C5 records — keeping them in S4 mixed "is this me?" with "what do I get?"._ → Set aside: keeping four cells and synthesising two more from elsewhere on the page — matches Wijhat visually, but invents a summary layer the programme doesn't have; reproducing "?Who is Abbara for" — literal to the source, but wireframes lay out for rewritten copy, not for the CMS's bugs; keeping the support cell — what this decision originally required, but it restated C5.

**98. Abbara's eligibility goes in C3, and the application terms stay in C8** — ~~the seven org-level eligibility rows (who can apply, disciplines, where they work, budget band, years operating, registration, previous beneficiaries) render as C3 rows; C8 keeps the six application requirements.~~ **Revised at the Abbara S4/C3 unit:** C3 retires. Who-can-apply is dropped — it restated "Who is Abbara for". Disciplines covered and Where they work move to S4 (scope). The remaining three facts move to C8, with budget band fused to years operating as the live page has it: "A small or midsize initiative or organisation — total expenditure or income between US$50,000 and US$500,000 — with at least three years' experience in arts and culture, as an entity or in its members." Plus registration and previous-beneficiary. C8 goes from six items to nine. The C3 code is not reused. _This is the split the register's graduation log records — Abbara's criteria are structured facts (a band, a count of years, two booleans), which is what earns the strip, where Production Awards' and Wijhat's narrative eligibility did not. Decision 85 folded eligibility into C8 for those two because it was a list of conditions; here it is a table of values, and C3 stays a scan-in-five-seconds block because there are no round dates competing for it — those went to S1. The scan job is now S4's (decision 160); C3's leftover facts that are rules, not scope, go to C8 so they are not restated._ → Set aside: following decision 85 and putting all seven rows in C8 — consistent across the four programmes, but it buries the budget band and the three-year rule, which are the two facts that decide whether an organisation reads on; splitting the dates into C3 as well — the shape the other programmes have, but Abbara's dates are a sequence and belong in the timeline; keeping C3 as Abbara's scan table — what this decision originally required, but every leftover card either restated S4 or belonged in C8.

**99. S1 renders in chronological order, with a lead naming the round and its span** — the eight stages run Open call → Deadline → Results → First workshop → Agreements → Second workshop → Technical assistance → End of contracting, and the block gains a `lead` prop carrying "a round runs 24–30 months" and that the dates are the current round, Abbara 08. _The live page lists "End of contracting period: February 2028" sixth, ahead of the 2026 second workshop and the 2026–2027 technical assistance. The register defines this block as preserving order and duration — the thing a flat label-value strip loses — so a timeline that isn't in order has no reason to exist. The lead exists because the dates are one round's, and without saying so the page reads as if February 2028 were a fixed property of the programme._ → Set aside: source order — faithful to the live page, but renders a rail that goes 2026 → 2028 → 2026; the span and round note as a first stage or a C6 — no new prop, but a duration isn't a stage and a callout would outrank the dates it qualifies.

**100. S3 takes records and a label; a plain string group keeps the placeholder rows** — `groups` widens to accept either a string or `{ title, records }`, each record a name plus an optional country, and the block takes a `label` defaulting to "Past beneficiaries". Abbara passes eight round groups with 56 organisations and the label "Abbara organisations by round". _Production Awards and Wijhat keep passing plain strings and are untouched (decision 84). The label is a prop rather than a fork because Abbara's directory is not a list of *past* beneficiaries — 60 of the 87 are still active, and the live heading says so. Country renders by omission (standing constraint 3), because the live directory publishes none._ → Set aside: a separate populated directory block — clearer at a glance which programme it belongs to, but two components for one register block; hardcoding "Past beneficiaries" and putting the round framing in a hint — no new prop, but the block's own label would then contradict its content.

**101. C10 stays in its near-empty mode, carrying only the two facts the English page states** — contracts signed after the first workshop on the basis of the first-draft strategic plan, and the funding component running no more than 24 months inside a contracting period ending February 2028. _The register defines this block as scaling detailed→near-empty across the four programmes and requires it to degrade gracefully; Abbara's English Guidelines tab has no contractual section at all. Two real items are the honest render, and the absence is flagged as a suspected English content gap to check against the Arabic page — not designed around._ → Set aside: borrowing the installments, signing windows, guarantors and clawback from Production Awards and Wijhat — fills the block, but invents contract terms for a programme whose published terms we haven't seen; dropping C10 entirely — no invented content, but it hides the gap instead of surfacing it, and the two facts we do have are real.

**102. C1 takes an `items` list, and Abbara's three programme goals go in it** — the block gains an optional bullet list under the prose; the live page's "Program goals" heading and its three bullets render there rather than as a block of their own. _The register puts Abbara's intro, sector context and goals in the overview block, and the goals are three long sentences that read as prose siblings of the paragraphs above them. C8 is conditions and C5 is what the programme offers — neither is what the programme is trying to do._ → Set aside: a fourth paragraph compressing all three goals — no new prop, but three distinct aims collapsed into one sentence stop being aims; the goals as a C6 callout — visible, but the callout is already spoken for by the core-funding exclusion, and two on one page flattens both.

**103. Abbara gets a `programDirectory` states key** — a new key carrying the `closed` toggle only, with a note saying the directory is published content rather than an offered block. _The closed state is real here (Abbara 08's call closed in July 2025), but `Show "offered" blocks` would be inert: Abbara's S3 carries no `offeredOnly` flag, so the toggle would change nothing on the page. Follows decision 95 — a state that can't occur on a page shouldn't be togglable there._ → Set aside: keeping the `program` key — no new key, but the panel would offer a toggle with no effect; flagging the S3 as `offered` to make the toggle work — one key for three programmes, but it would hide 56 published organisation names behind a wireframe state.

**104. One populated record rather than 33** — PC1 and the detail route carry the Lebanon Humanitarian Fund report; PA4 samples nine of the 33 library titles. _The live library has 33 records but only one was in scope for this unit. Nine sampled cards plus the overflow line show scale without implying the sample is the set, and 33 detail routes would be identical shells for 32 records with no content brief._ → Set aside: a detail route per record — faithful to the live sitemap, but 32 of them have nothing to populate; rendering all 33 cards — accurate count, but the grid would dominate the page the way Abbara's directory would have before it was sampled by the overflow pattern.

**105. PC1 keeps unpublished metadata rows as fill bars** — Author, Country, Theme and Type stay on the record as fill bars; only Language and Year carry values. _Standing constraint 3 would drop the empty rows, but that would hide the gap: the library filters on taxonomy the detail page does not publish. Fill bars with a hint naming the gap surface it rather than designing around it._ → Set aside: omitting the four rows — obeys the omission rule literally, but the wireframe would then look as if those fields were never part of the record; inventing tags from the library facets — fills the table, but the live page does not publish them.

**106. One download button per published language** — the single "Open the document" button becomes one primary button per language the record publishes (English, Arabic). _The live record has two files, not one bilingual file, and a single button would hide that._ → Set aside: one button labelled "Open the document" — matches the unpopulated block, but implies a single file; a downloads list like S2 — heavier treatment than two files need.

**107. PB1's headline is an optional slot** — the Fill that stood for a headline above the intro becomes an optional `heading` prop; none of the three series pass it, because none has a headline above the intro (the H1 is the chrome band). _The spec's "headline + text" was describing the page heading plus intro; rendering a second headline fill on every series page was a leftover from the unpopulated kit._ → Set aside: keeping the Fill as the decision-84 fallback — consistent with other blocks, but it would show a headline none of the series have; requiring a heading and feeding the series title — duplicates the page-header band.

**108. PA4 carries PB2's overflow line** — nine sampled cards are followed by "… 33 items in the grid". _Without it the sample reads as the whole library._ → Set aside: rendering all 33 cards — accurate count, but the grid would dominate the page; a count in the hint only — the hint is for reviewers, the overflow is for anyone reading the page.

**109. Facet values are an optional list on the shared `Facet` type** — `FilterBar` renders `facet.values[i]` where supplied, `"value"` otherwise. Library facets carry the real lists; Network and News are untouched. _One primitive; optional content (decision 84)._ → Set aside: a Publications-local filter bar — would leave MN3/N1 alone without touching the type, but forks the primitive the library already shares; required values on every facet — would force Network and News to invent placeholder names this unit isn't populating. ~~**Note:** Network's five facets carry their real lists as of the Mawred Network unit; News is still the only kit rendering `"value"`.~~ **Revised at the News unit:** News's one facet now carries its real values too, so no kit renders `"value"` and the fallback is dead code kept only for the decision-84 contract.

**110. Four sampled records, two per entity tab, with one populated detail page** — the directory carries Hiba Taim and Nour Shantout on Individuals & Groups, Al Kasaba Theatre and Cinematheque and Editions Motifs on Organizations & Initiatives; Al Kasaba is the record the detail route renders. _Extends decision 104. Two per tab means both tabs hold real names rather than one tab reading as populated and the other as a stub, and the pair per tab is the smallest sample that shows a record with one engagement beside a record with two. Al Kasaba takes the detail page because the record name lives in the route registry and cannot swap per state, and its two engagements — one of them without a project description — put both the repetition and the omission in the default render._ → Set aside: an individual on the detail page — matches the spec's field list most closely, but a single-engagement record would have hidden the repeating group the live data actually has; a second detail route for an individual — shows both record shapes, but doubles a route for a page-type whose two shapes differ only in which optional fields are present.

**111. MN6 renders a repeating engagement group, and Round generalises to round/track** — the record's flat metadata table splits: Country and Discipline stay record-level, and Involvement · Programme · Round · Year · project title · project description become a group repeated once per engagement, with the round slot holding either a round number or a track name. _Spec §B5's single field set came from the one-engagement Tarek Elazhary record; three of the four sampled records break it — Al Kasaba and Editions Motifs are each Participant **and** Grantee on the same All Around Culture project, distinguished only by track (Regional Workshop vs Alliance Support). A flat table would have to either drop an engagement or merge two involvements into one row that belongs to neither._ → Set aside: one row per field with comma-joined values (`Involvement: Participant, Grantee`) — no structural change, but it detaches each involvement from the track and project it belongs to; one directory row per engagement rather than per entity — mirrors how the data is stored, but the directory would list Al Kasaba twice and the record page is plainly about the entity.

**112. Unpublished totals stay unpublished — the count row shows only what it shows** — the port's invented `318` / `94` entity-tab counts and its page size of 40 are removed; the tabs carry no count, MN4 reads "Showing N records in \<tab\>" with no denominator, MN2's record rail keeps `00`, and the load-more button drops its number. _Nothing on the live site publishes a total: no count row, no pagination, and the listing's several-hundred rows are JS-rendered so even the markup can't be counted. A denominator is the one number this block exists to give, so inventing it would be the most quotable false fact on the page; naming it as a gap puts it where Mawred can supply it._ → Set aside: keeping the invented figures and flagging them in the log — the page reads complete, but a wireframe that gets signed off carries the numbers with it; a "several hundred" phrase in place of the count — honest about the order of magnitude, but it's our estimate, not the client's data.

**113. MN1's headline Fill retires and the responsible-data paragraph is the intro** — the Fill that stood for a headline becomes an unused `heading` slot, and the block's prose is what the page tail currently carries. _Follows decision 107: the H1 is the chrome band, so a headline fill above the intro was a leftover from the unpopulated kit. The retired "under development" notice takes the `resources@mawred.org` data-access invitation with it (spec decision 2), which is a client call rather than ours — so it is named in the hint instead of being either kept or silently dropped._ → Set aside: keeping the data-access ask as a second contact line — it may well be permanent, but wireframing it in would decide a question the spec explicitly retired; dropping it without comment — matches the spec literally, but loses the only surviving trace of an invitation Mawred may want back.

**114. The `record` states key is emptied and the sparse toggle retired** — the key carries no toggles and a note saying why; `NetworkRecordPage` drops its client boundary and `sparse` leaves `WireframeState` altogether. _Follows decisions 95 and 103: a state that a page can't be in, or no longer needs, shouldn't be togglable. The populated record shows field omission in its default render — an organisation with no descriptor, an engagement with a project title and no description — so the toggle would have had to strip real content to demonstrate something already visible._ → Set aside: keeping the toggle and hiding the body and descriptions behind it — preserves the panel entry, but a state whose only job is to remove populated content teaches nothing; keeping `sparse` in the state bag unused — smaller diff, but dead state read by nothing.

**115. Three sampled posts, two of them with a detail page, and the sample drives the tabs** — the feed carries the Made With Your Magic exhibition opening (27 July 2026, News & Events), the second-round Wijhat grantees (20 July 2026, Announcements) and the Production Awards 2026 results (6 May 2026, Announcements); the first and last get detail routes and the Wijhat post is a landing card whose link borrows the announcement route. _Extends decisions 104 and 110. Three posts is the smallest sample where the category tabs do real work — All 3, Announcements 2, News & Events 1 — so the tab row can be verified rather than asserted, and where a programme chip narrows to one real row. Two of the three earn detail pages because they are the two post kinds the spec's decision 3 splits the body shape on; a third detail page would have repeated one of them._ → Set aside: nine posts to fill the page-one grid — the feed would look complete, but six of them would be titles with no date, category or programme we had sourced; a detail page for the Wijhat post too — a third real route, but its content is the announcement shape again, one programme along.

**116. Invented counts go and the programme facet gets its real values** — `NEWS_CATEGORIES` drops the `231 / 128 / 103` tab counts the port carried, and `NEWS_PROGRAMME_FACET` carries Production Awards · Wijhat · Stand for Art · Abbara instead of four `"value"` placeholders. _Follows decision 112. Nothing on the live site publishes a post total, and the Announcements / News & Events taxonomy does not exist yet, so its per-category counts cannot exist either — a count that can't be sourced is the most quotable false fact on the page. The four programmes, by contrast, are the one facet list News can state with certainty, which is why decision 109's closing note is revised in place: no kit renders `"value"` any more._ → Set aside: keeping the counts and flagging them in the log — the tabs read complete, but a signed-off wireframe carries its numbers with it; dropping the programme facet until the tagging model is confirmed — honest about the client call, but the facet is the block's only proof that a programme cut is possible.

**117. N14 is one block for grantees and for jury** — a single roster block takes a label and a list of `{ name, country, role, project?, description? }`; a jury entry supplies the first three and the project rows render by omission. _The two rosters are the same treatment with a different field count, and standing constraint 3 already governs the difference — blocks are defined by treatment, not by intent (the component register's own rule). Two blocks would have to be kept in step through five disciplines._ → Set aside: separate grantee and jury blocks — clearer at a glance which is which, but two components for one grid, and the jury block would be the grantee block minus two rows; one block per discipline holding both rosters and the quotes — matches the live page's heading structure, but a block that contains three lists isn't a block.

**118. N15 is its own block, attached to the roster above it rather than to the post** — quotes render as one or more attributed blockquotes at 3px, placed immediately after the jury roster they come from. _Decision 57's callout weight, and the placement is what makes the quotes legible: each is a member of that discipline's jury speaking about that discipline's submissions, so a single quotes block at the post tail would detach every quote from its jury. Cinema publishes two, Literature three, and one discipline publishes none — a per-post block couldn't express that._ → Set aside: quotes as an optional field on N14 — one fewer block, but a jury card grid and a stack of long pull-quotes are two treatments; a single quotes block at the end of the post — matches the share-bar-adjacent position quotes often take, but it would read as the post's own voice rather than the jury's.
    10|
**119. Two detail routes, one per post kind, each with its own states key** — `/news/post` is the announcement (rosters, quotes, no key-detail line) on the `newsdetail` key; the new `/news/editorial-post` is the event post (prose plus one key detail) on a new `newseditorial` key carrying `byline` only. _Spec decision 3 makes the body shape the difference between the two kinds, and a single route can only be one of them — the port's one route was standing for both by toggling. Separate keys follow decisions 95/103/114: the event post has no roster, no schedule and no CTA, so it should not be offered a toggle for them, and it is not the oldest post in the archive._ → Set aside: one route with the two shapes behind a toggle — what the port did, and it keeps the route count down, but the page-header band carries a real post title now and a title can't toggle; a third route for an event post with a multi-date programme — would light up N10 and N11, but no such post was in scope and inventing a forum's schedule is inventing content.

**120. Cinema in full, Literature roster-only, three disciplines to an overflow line** — the announcement publishes Cinema's five grantees, three jurors and two quotes; Literature's six grantees and three jurors with no quotes; then "… Music, performing arts and visual arts follow in the same shape, each with six grantees and a three-member jury — 29 grantees across five disciplines". _Extends decisions 104/108/110. One discipline in full shows the shape; a second shows it repeats, and shows that the quotes are per-discipline rather than per-post, which one discipline alone can't. All five would be 29 grantee cards and fifteen juror cards on one page — the length is the fact, and the overflow line states it in a line._ → Set aside: all five disciplines — accurate, and the real page is that long, but the wireframe would be reviewed as a scrolling test rather than as a structure; Cinema alone — half the page length, but quotes attached to the only roster present read as the post's quotes.

**121. N6 takes a caption, and the image credit moves into it** — the featured-image block gains an optional caption slot; the announcement post's credit line renders there and the event post, which publishes no credit, renders none. _The live posts print the credit as a trailing italic line at the very end of the body, a screen and a half from the image it describes — the caption is the only place it means anything. The slot is optional because one of the two sampled posts has nothing to put in it._ → Set aside: the credit as a last body paragraph — faithful to the source, but it reads as part of the article; a required caption with a placeholder — consistent with nothing, since decision 84 makes content props optional.

**122. The `structured` toggle is retired; `edge` stays** — `newsdetail` carries `byline` and `edge` only, and `structured` leaves `WireframeState` altogether. _Follows decision 114 exactly: the rosters and quotes are this post's published content, so a toggle over them would exist only to remove populated content. `edge` survives because N5 is the one block on the page still standing for the template rather than for the post — a post's neighbours come from the whole 26-page archive, so the archive-boundary render can't be shown by populating anything._ → Set aside: keeping `structured` over the rosters — preserves the panel entry, but the default render of a results post would then be a results post with no results; retiring `edge` too, on the grounds that this post has 200 older posts — consistent, but it would drop a real template branch that nothing else can show.
    20|
**123. N5's neighbour titles stay placeholder** — the previous and next cards keep "Title of the previous post", with the hint saying why. _Prev/next are archive-adjacent, and the three-post sample can't supply them: the only sampled neighbour of the announcement is the Wijhat post, whose link is the announcement's own route, so populating it would produce a "next post" pointing at the page you are on. A placeholder is a gap; a self-link is a bug._ → Set aside: cross-linking the two populated posts as each other's neighbours — every link would resolve, but the Wijhat post sits between them by date, so the wireframe would misstate the sequence the block exists to show; populating one side and leaving the other — half-real, and it would still self-link.

**124. Zero results is computed from the list, not read from the panel** — `NewsLandingPage` derives `noResults` from the filtered post count and passes it to N2, N3 and the pager, instead of passing the `empty` toggle through. _The toggle and the truth had come apart: opening the News & Events tab with the programme chip active yields no posts, and the old wiring rendered an empty grid with an archive overflow line under it and a pager below. The empty state is a property of the result set, not a state a reviewer selects._ → Set aside: adding a second toggle for the tab-plus-chip case — the panel would enumerate combinations rather than states; leaving it and noting the combination as out of scope — it is reachable in two clicks on the page as built.

**125. Single-consumer About blocks take content directly, with no placeholder fallback** — AB1–AB5 take required content props (`text`, `points`, `values`, `lead` + `entries`); none keeps a fill-bar render for the no-content case. _Decision 84's reason doesn't reach them. It exists because population runs one page-type per unit over blocks several page-types share, so a required prop would force every other page-type to be populated in the same session. AB1–AB5 have exactly one consumer each — `WhoWeArePage` — so a fallback branch would be unreachable code kept for a caller that can never exist._ → Set aside: optional props for consistency with the other 40-odd populated blocks — one rule across the app, but it would leave five dead branches and let a future caller silently render a page of fill bars instead of failing to compile; deleting the fallback only from the three simplest blocks — smaller diff, but a rule that holds for two of five blocks isn't a rule.

**126. `Timeline` renders `text` if present; else Fill only when `fill` is passed** — ~~a `TimelineItem` with `text` renders it as a paragraph; one without keeps `<Fill>`.~~ **Revised:** date-only S1 stages omit the body rather than looking like missing copy. ~~AB5's 2018–2026 gap row still passes `fill: 72`, so that Fill stays.~~ **Revised at the About polish unit:** AB5 no longer passes `fill`; the gap row is labelled only (decision 128 revised). _The shared primitive has two consumers with genuinely different content shapes: AB5's history entries each carry a paragraph (the gap row is the exception), and S1 stages are label-and-date only because the live timelines publish no stage prose. Fill is now opt-in via `fill`, not the no-text default — a date-only rail is complete, not unpopulated._ → Set aside: a required `text` with S1 passing empty strings — one code path, but it would put a blank paragraph where a missing description used to be a fill bar; an About-local timeline — no shared prop to widen, but decision 69 extracted this rail precisely to avoid two copies.

**127. Eleven dated entries; the undated passage goes in the lead and the closing paragraph is dropped** — AB5's lead carries two paragraphs, the second naming the joint initiatives (Tunisia Balad El Fann, Tandem Shaml, Redzone Festival); the live history's closing "stories still to be told" paragraph is not rendered. _Spec §3.1 provides for exactly this: long-form nuance that doesn't fit a timeline row stays as a short intro above it. The joint initiatives are ongoing partnerships with no year to sit on, so any row they were given would state a date the source doesn't. The closing paragraph is voice rather than content, and copy is being rewritten — reproducing a rhetorical flourish would be migrating copy._ → Set aside: rows dated by best guess from the partners' own timelines — fills the rail, but invents dates on a page whose whole subject is dates; the closing paragraph as a twelfth entry or a trailing note — faithful to the source, but it would read as a milestone; dropping the joint initiatives entirely — the rail stays purely chronological, but it loses a third of what the organisation currently says it does.

**128. The 2018–2026 span is a labelled gap row, not an omission and not synthesised** — ~~the rail's last row is labelled `2018–2026` with a `milestone` and no `text`, so it renders a fill bar and terminates the rail.~~ **Revised at the About polish unit:** the last row stays labelled `2018–2026` with milestone “Not covered by the published history” and no `text`; AB5 no longer passes `fill`, so Timeline omits the body rather than painting a bar (decision 150). The nine-year gap stays named; the bar that read as leftover chrome is dropped. _Follows decision 105: a gap that the wireframe designs around stops being visible, and this one is nine years wide. Ending the rail at 2017 would read as a complete history that happens to be old; the labelled row reads as content Mawred owes the page. Synthesising the years from the news archive was available and not taken — a history is what an organisation says about itself, and the news archive is not that statement._ → Set aside: ending at 2017 with the gap in the hint only — the hint is for reviewers, and this gap is the reader's problem too; writing the nine years from the archive and the programme pages — the page would look finished, but the wireframe would be authoring the client's institutional history; a dashed row rather than a labelled one — dashed marks optional/conditional per standing constraint 1, and these nine years are neither; keeping the fill bar — named the gap, but the grey rect under the rail read as leftover chrome once the row already says the years are unwritten.

**129. `Btn` takes an optional `href`, rendering a `Link` with the same classes** — given an `href`, `Btn` renders a Next.js `Link`; without one it stays a `<button>`, so every existing call site is unchanged. Home's wayfinding, "Read more", "All news", "All publications" and "Who we are" pass the hrefs already declared in config. _Standing constraint 1 requires clickable end to end, and the destinations were already named; a button that goes nowhere was a leftover from the port. One optional prop rather than a second component, matching `Card`._ → Set aside: a separate `BtnLink` — clearer element type in the name, but two components for the same chrome; wrapping Home's buttons in `Link` at the call site — no primitive change, but the classes would have to be kept in step by hand.

**130. `nocalls` stays, as a template branch rather than a reachable state** — the toggle remains on the `home` key and HM3 still renders the spec §3C empty; the hint and the panel note say it is a template branch. _Stand for Art accepts applications all year with no rounds and no deadlines, so at least one call is always open — a state the page cannot be in. Decisions 95/103/114/122 retire those. Spec §3C nevertheless requires the graceful-empty branch to be laid out, and a template the CMS can fall into when a future programme set has no open call is a real branch, not a toggle over populated content._ → Set aside: retiring the toggle — consistent with 95, but it would leave spec §3C unwired and hide the empty from review; inventing a closed Stand for Art to make the state reachable — would contradict the programme's own page.

**131. The Mawred-wide grant total stays `00`** — HM2's first rail is `grants awarded since 2003` with no value. _Follows 112 and 128. Spec §3B's "~400 grants since 2004" is Production Awards alone, and 87 organisations is Abbara's; no Mawred-wide grant total is published anywhere. Borrowing either would put a programme-scoped number on an organisation-wide strip._ → Set aside: using ~400 and flagging it — the strip would read complete, but a signed-off wireframe carries the number; dropping the rail — honest, but it would hide the gap MN2 already shows how to keep visible.

**132. `HOME_PROGRAMMES` is the single source; `HOME_OPEN_CALLS` derives from it** — each programme is one `{ name, descriptor, href, status? }` record; HM4 renders all four; HM3 filters to those with a `status`. _A programme's one-liner would otherwise be written twice and drift. Status is optional, so Abbara (call closed July 2025) appears in the overview and not in the open-calls strip without a second list to keep in step._ → Set aside: two independent lists — simpler types, but the descriptors would fork the first time either strip is edited; HM3 reading the programme kit live — the spec keeps Home a curated block, not a query into the kit.

**133. The 20th stays a state on HM5, with the campaign content a gap** — default feature is Made With Your Magic, Beirut; the `twentieth` toggle still swaps the slot to a campaign eyebrow and title over fill bars, linking to the `/mawreds-20th` stub. _Decision 44 already made the 20th a state on this block rather than a block of its own. No campaign copy is sourced and the destination is still a stub, so populating the branch would invent a campaign. Keeping the toggle (unlike 122's retired `structured`) is because this is an alternate editorial choice, not a way to strip published content._ → Set aside: retiring `twentieth` until campaign copy arrives — would drop a specced branch from review; writing campaign copy from the news archive — fills the slot, but the wireframe would be authoring the client's anniversary.

## Client view

**134. Codes-off is review chrome, not codes only** — `Block.label` is the reviewer name and hides with the code badge; `Block.heading` is the client-visible section title and shows only when codes are off, so the two never stack. The `codes` toggle — panel copy "Show review chrome", key unchanged — ~~default unchanged~~ **Revised at the codes-default unit:** default is off (decision 156) — drives `body.codes-off`, which hides badges, the label row, block wrapper borders and padding, and Hint notes. Inner primitive borders, C6 and AB2's grey fill, N8/N15's 3px, chrome, fill placeholders and their captions stay. _Standing constraint [10] already called the off state a clean client view, but the toggle only hid the `.code` badge, leaving technical labels ("Cinematic hero — image sequence + positioning line") reading as if they were headings._ → Set aside: a second client-view toggle — two switches for one presentation; promoting every `label` to an `h2` — would print reviewer glosses on the client page; leaving labels visible with codes off — the client view would still be annotated.

**135. Headings are the default; review chrome hides them** — `.block-heading` is no longer default-hidden. `body:not(.codes-off) .block-heading { display: none }` keeps review view as code + uppercase label only; ~~client view shows the `h2` at `text-lg font-bold`~~ **Revised at the programme-sections unit:** programme section h2s (Application, Past, FAQs, Contact) use `.block-heading` at `text-2xl font-bold`; `Block` gains `headingAs?: 'h2' | 'h3'` (default `'h2'` so About / Home / News / Publications stay as they are); programme blocks that still pass `heading` pass `headingAs="h3"` and stay `text-lg`. Overview's h2 is `sr-only` and never uses `.block-heading`. C11, C12 and S3 drop `heading` so they do not duplicate the section title (decision 153). `.block-frame` gets `margin-block: 2rem` so adjacent lists don't collapse once padding is zeroed. C5 takes an optional `heading` (default "Forms of support"); Production Awards passes "Available grants". _Decision 134 already split label from heading, but the CSS hid the heading by default and the `body.codes-off` override never won, so client view dropped the title row. "Forms of support" is the wrong title on a page of five grant amounts._ → Set aside: showing headings in both views — would print "C4 HOW TO APPLY — STEPS" on an h2; renaming C5 globally to "Available grants" — wrong on Stand for Art and Abbara, which really are forms of support; adding headings to C2, C6, C7 or S4 — those blocks already title themselves (figures, callout, button, cells). ~~adding headings to C1 — it titled itself via an inner h3~~ **Revised at the programmes-opening unit:** C1 no longer titles itself via an inner h3; the page H1 is the heading (decisions 137–138).

**136. Unordered lists restore disc markers** — content `<ul>`s take `list-disc` (the same restore ordered lists already had with `list-decimal`), and a `@layer base` rule sets `list-style: disc` on `ul` so a future list isn't unstyled if the class is missed. Layout lists keep `list-none` (Timeline, AB10, AB13); utilities still win. _Tailwind Preflight zeros `list-style` on `ul`/`ol`, which is why the eight content lists were indented with `pl-5` and had nothing to indent for. The class is what actually shows — Preflight's `list-style: none` shorthand is not overridden by `list-style-type` alone._ → Set aside: the base rule alone — one place, but the two `<ol>`s already proved the utility class is the restore that renders; unlayered CSS — would override `list-none` and put discs on the timeline. **Note:** C4's steps left the ordered list for numbered cards (decision 141); C9's process list is the remaining `<ol>`.

## Grants & Opportunities — presentation

**137. Programme opening is the page-header band then C1** — ~~the four programme routes set `suppressPageHead`; C1 owns the crumb and the page H1 (`text-4xl`, same as the band); `ProgramPage` puts C7 in a `flex` row with `items-start justify-between` on the H1 line, far right; crumb sits above that row so the button lines up with the title;~~ **Revised at the page-level band unit:** the four programmes mount `PageHeaderBand` then C1 (overview prose + optional `items` only). ~~C7 is unmounted from the opening and parked; the component and `dest` stay on `ProgramConfig` for a later apply / guidelines placement (decision 152).~~ **Revised at the programme-sections unit:** C7 remounts in Application (decision 153); it is no longer parked. ~~first in Application, stacked under the h2~~ **Revised at the C7 heading-row unit:** C7 sits on the Application heading row, right-aligned, and stays sticky after that row scrolls away (decision 159). C2 is a full-width `wide` Figs row under the opening. C7 stays its own block — not nested inside C1's `Block`. _Every programme starts the same way: page title + description. The band is the H1; C1 no longer owns it. The original reason to give C1 the title — so C7 could sit on that row — left with the button._ Revises standing constraint 2 / decision 33. → Set aside: ~~keeping the band and dropping C1's H1 — would leave the apply button in a sidebar with no title to sit beside;~~ nesting C7 inside C1's box — one opening, but two codes in one frame. **The first set-aside is now the decision; C7 is off the opening.**

**138. C1's inner heading is gone, not an unused slot** — ~~the `title` prop stays optional and still renders as an h3 when passed; none of the four programmes pass it, and a missing title is omission rather than a Fill.~~ **Revised at the page-level band unit:** the `title` prop is dropped. C1 is overview prose + optional `items` only. The H1 is `PageHeaderBand` (decision 152). _The live "What is the … Program?" lines were leftover from when the band carried the H1 (decision 135's note); keeping an unused slot after the band returned would invite a second heading._ → Set aside: keeping the inner title under the H1 — two headings saying the same thing at different sizes; a Fill when the inner title is absent — would show a missing heading on a block that already has one.

**139. S4 sits after the opening, not before it** — when `config.hero` is present (Wijhat, Abbara), S4 renders after C1 + C2, still in Overview (decision 153). ~~C7 sat between them on the opening; it is parked off the opening (decision 152).~~ **Revised at the programme-sections unit:** C7 remounts in Application, not on the opening. **Revised at the Abbara S4/C3 unit:** S4 is the scope summary (who / what-for / where) wherever it appears; C5 is what you get. Abbara's support cell left S4 — C5 already had the four components. Wijhat already made this split (decision 86 overturned). ~~**Revised at the Production Awards S4 unit:** Production Awards now has S4 too.~~ **Rolled back:** Production Awards has no S4 — eligibility already lives in C1 and C8 (decision 158). ~~Stand for Art still has none.~~ **Revised at the Stand for Art unit:** Stand for Art gets none either, for the same reason (decision 161), so S4 is Wijhat and Abbara only and the who / what-for / where split applies where S4 exists rather than to all four programmes. _The summary cells qualify the programme; they are not the page title. Putting them first made Wijhat and Abbara start on a different shape from Production Awards and Stand for Art._ → Set aside: S4 first, then C1 — what the port had, but it led with cells before the visitor knows the programme's name or what it is.

**140. C3 is a wrapping card grid, not full-width KV rows** — `C3AtAGlance` renders as cards (`border border-black p-2.5`), not numbered and not a stacked table. Label in `text-xs text-neutral-500`, value as body; ~~Production Awards' remaining three facts (eligibility, disciplines, grant period) and Abbara's seven share the wrapping grid~~ **Revised:** Production Awards drops those three leftover cards (decision 158); ~~Abbara's seven stay, and long values wrap inside the card.~~ ~~Rounds: one card per round — title, deadline, results — in a 3-up grid, which is how Wijhat's three rounds render.~~ **Revised:** sequenced dates left C3 for S1 (decision 145); C3 is facts only. **Revised at the Abbara S4/C3 unit:** C3 retires. `C3AtAGlance`, the `C3flat` config arm, and the ProgramPage cases are deleted. The C3 code is not reused. The card chrome lives on as S4's claim/sentence items (decision 160 revised). _C3 is the scan-in-five-seconds block (decisions 85, 98); full-width KV rows made six or seven facts a list to scroll, and grouped KV rows made Wijhat's rounds look like a second C8. The scan job is now S4's._ → Set aside: numbered cards — the facts aren't a sequence; KV for flat and cards only for rounds — two treatments for one block, which is the split decision 61 already closed; full-width rows for rounds because three items fit — the open question this unit resolved; keeping C3 for Abbara — the last consumer, but every leftover card restated S4 or belonged in C8.

**141. C4 steps are a wrapping row of numbered cards** — `C4ApplySteps` drops the `<ol>` for the same wrapping card grid C3 uses (`border border-black p-2.5`). Each card carries a large index (`text-xl font-bold`), the step label, and the detail; a missing detail still renders a Fill (decision 84). Sequence reads left-to-right, then wraps. Documents and note stay below the card row. ~~and the repeat apply button~~ **Revised at the C7 heading-row unit:** C4 no longer repeats Apply now — C7 on the Application heading is the only apply control (decision 159). Nested use (`nested`) still returns a fragment, so S5's delegated markup does not double-wrap. _The steps are a sequence, which is why they are numbered where C3's facts are not (decision 140); an ordered list made three or four short actions a vertical scan._ → Set aside: keeping the `<ol>` and restyling its items as cards — would preserve list semantics, but the numbers would fight the card chrome and the sequence would still read top-to-bottom; a numbered `Cells` row — already extracted (decision 80), but Cells share borders and don't wrap as a gap grid.

**142. C11 FAQs use a plain accordion variant** — `Accordion` gains a `plain` prop used only by `C11Faqs`. Items have no borders; the question is `text-base font-bold`; rows space with `py-3` / `mb-3`. Default Accordion stays boxed — S3 beneficiary groups are unchanged. ~~AB7 bios are unchanged.~~ **Revised at the About polish unit:** AB7 bios left Accordion for a name-row disclosure (decision 151). _FAQs are a list of questions, not boxed records; the default chrome made four or six Q&As look like another S3._ → Set aside: changing the default Accordion — would restyle S3, which still needs the box; a C11-local expander — a second toggle component doing the same job, against decision 71.

**143. S5's two tracks are tabs, with the shared core above** — `S5ApplicationPaths` is a client block that owns its tab index (decision 64); one tab per path (Individual / Group); the nested C4 cards render in the active panel. The shared core stays above the tab row. The "stacked labelled sections, not tabs (decision 6)" hint is dropped. **Decision 6 revised in place:** S5 is two short tracks a visitor chooses between, not a long option list — stacking both made the second path look like a continuation of the first. Standing constraint 3's long-list rule (decision 19) is unchanged; this is the exception it already implied. **Note:** the active panel is now `Tabs`' optional `children` (decision 146). _Wijhat is the only consumer; the two paths exist so the visitor can pick one, which is what tabs are for._ → Set aside: keeping stacked sections — what the port had, and what decision 6 required, but two short tracks don't meet the long-list test decision 19 later set; lifting the tab index into `ProgramPage` — matches Network/News (decisions 66/78), but only S5 reads the value.

**144. C6 highlight is a grey fill, not a 3px border** — `C6HighlightedNote` drops `Block.callout`. The highlight is an inner `bg-neutral-200` padded band with no border, so the fill survives `codes-off` the same way N8 keeps its inner 3px (the Block wrapper is a `block-frame` again). `Block.callout` is unused and removed. C4's optional `highlight` reuses the same inner treatment. ~~N8, AB2 and N15 stay 3px.~~ **Revised at the About polish unit:** AB2 uses the same grey fill (decision 148); N8 and N15 stay 3px. **Decision 57 revised in place for C6 only.** _The 3px on C6 was the only thing that distinguished it from a prose block in review; in client view it became a second border weight on a page of 1px cards. Grey fill is the highlight; weight stays for N8/N15, where the band is a quotation._ → Set aside: keeping 3px on C6 — consistent with the original 57, but three programmes would then have two emphasis signals (cards at 1px, callout at 3px) for notes that aren't quotes; a Block-level fill that skips `.block-frame` — keeps padding in client view, but would leave C6 without the frame-zeroing the rest of the page gets.

**145. Sequenced programme dates live in S1, not C3** — Wijhat's three rounds become six chronological S1 stages (deadline then results, labels carrying the round) with a short lead that the intake is year-round, the calendar repeats, and dates carry no year. Production Awards' Open call / Deadline / Results pull out of C3 into an S1. ~~above the remaining three fact cards~~ **Revised:** those leftover cards left the page (decision 158). C3 drops its rounds mode; `C3AtAGlance` is flat-only. Wijhat has no C3 after this; Production Awards has none after decision 158. **Revised at the Abbara S4/C3 unit:** C3 retires; there is no remaining consumer and the component is deleted. ~~S6 stays immediately after Wijhat's S1.~~ **Revised at the programme-sections unit:** S6 still follows S1 in config order; on the page the Application h2 and C7 sit between them (decision 153). S1 is Overview; S6 is Application. **Decisions 61, 126 and 140 revised in place.** _S1 is the sequence block (label + date on a rail); C3 is facts. Round calendars and open/deadline/results are sequences that were parked in C3._ → Set aside: keeping rounds in C3 as cards — what decision 140 just did, but three round cards still aren't a sequence; a C3 date strip plus S1 — two date treatments on one page.

**146. Tabs are a 1px light strip, with an optional connected panel** — `Tabs` drops overlap (`-mr-px`) and the 2px active weight. The row sits on a 1px `border-neutral-200` bottom rule. Active: `font-bold`, 1px light border on top and sides, white bottom covering the rule so it joins what follows. Inactive: `text-neutral-500`, no box. Optional `children` render in a 1px `border-neutral-200` box with no top border, continuing from the tab row. S5 passes the nested C4 as children so Individual/Group is tabs + one panel. N1 and MN5 pass nothing — they keep a tab row over filters/listing. **Decision 57 revised in place for active tabs only** (~~header rule and primary buttons stay 2px~~ **Revised at the website-shell unit:** header/footer 2px rules are gone; 2px stays on primary buttons only — decision 154). _The previous treatment was a button group, not tabs. `neutral-200` as a 1px rule is the permitted fill colour used as chrome, so the strip doesn't read as the same black 1px as cards and primary buttons._ → Set aside: keeping 2px active tabs — consistent with original 57, but the overlap plus weight made a button row; boxing N1/MN5 the same way as S5 — one treatment, but News and Network tabs sit over a listing, not inside a panel.

## About — presentation

**147. Who We Are and Our Team openings own crumb + H1** — ~~`about-who` and `about-team` set `suppressPageHead`. AB1 takes `crumb` / `pageTitle` from the route registry (crumb `text-xs text-neutral-500`, H1 `text-4xl font-bold`, then the three intro paragraphs — same composition as C1, no inner heading). Our Team drops AB6 and renders crumb + H1 in the shell, then the AB7 groups.~~ **Revised at the page-level band unit:** both pages mount `PageHeaderBand`. AB1 is the three intro paragraphs only. Our Team is the band then the AB7 groups. AB6 stays omitted on Our Team and on Supporters / Past Initiatives / Careers behind `slots`. `suppressPageHead` is gone (decision 152). _The band is the H1; AB1 no longer owns it. AB6's fill stood for net-new micro-copy that does not exist, so omission until copy exists (standing constraint 3)._ → Set aside: ~~keeping the band — would leave two H1s once AB1 titles the page;~~ keeping AB6 as a fill on Our Team — marks missing copy, but the grey strip under the title reads as leftover chrome rather than a gap. **The first set-aside is now the decision.**

**148. AB2 vision is a grey highlight with the heading inside** — drop the 3px border. One inner `bg-neutral-200` padded wrapper holds the Vision heading (`block-heading`) and the sentence, same fill treatment as C6 so it survives `codes-off`. The h2 is inside that wrapper; `Block` does not also take `heading`. N8 and N15 stay 3px. **Decisions 57 and 144 revised in place.** _AB2 is a highlight, not a quotation; 3px is the quote signal. Putting the heading in the fill means client view still has a titled band once review chrome is off._ → Set aside: keeping 3px on AB2 — consistent with original 57, but Vision is a statement of intent, not a pull-quote; passing `heading` to Block as well as putting an h2 in the fill — two titles in client view; a Block-level fill that skips `.block-frame` — would leave AB2 without the frame-zeroing the rest of the page gets.

**149. AB3 mission is a gapped card grid, not Cells** — AB3 uses its own wrapping `border` cards with `gap-2.5`, matching AB4. The `01`–`04` eyebrow stays. Shared `Cells` is unchanged — ~~S4 and HM4 stay a tight shared-border row.~~ **Revised:** S4 later left Cells (decision 160); HM4 stays a tight shared-border row. **Decision 80 revised in place.** _Mission points and values sit on the same page; a tight cell row under a gapped values grid made four short claims look like a different kit. Cells' overlapping borders can't take a gap without becoming a different primitive._ → Set aside: adding gap to Cells — would restyle S4 and HM4, which need the shared-border row; keeping Cells on AB3 — one primitive, but the mission/values pair wouldn't match.

**150. AB5 spaces the rail below the lead; the gap row is labelled, not filled** — `mt-4` on the Timeline in AB5 only (S1 already has `mb-2` on its lead). AB5 stops passing `fill: 72`; the last row stays `2018–2026` + “Not covered by the published history” with no `text`, so Timeline omits the body. **Decisions 126 and 128 revised in place.** _A fill under a row that already names the gap read as leftover chrome; the labelled row still makes nine unwritten years visible (decision 105)._ → Set aside: keeping the fill — named the gap, but looked like a missing description on every other row; ending at 2017 — already set aside in 128.

**151. AB7 people cards have no outer frame; bios toggle from the name** — drop the card’s outer `border border-black`. The photo box keeps its own border; name / role / country sit under it with no frame. Drop the “Read bio” AccordionItem. Records with a bio: a disclosure arrow next to the name (`▸` / `▾`); the name row toggles the bio in place. Records without a bio stay static — no arrow, no click (decision 48). AB7 is a client block so it can own open/closed state (same pattern as Accordion / S5). Default Accordion stays boxed (S3, C11). **Decisions 71 and 142 revised in place.** _The boxed “Read bio” row under a framed card read as a second chrome layer; the name is the person, so that’s where the disclosure lives._ → Set aside: keeping AccordionItem — one primitive, but a labelled expander under a name is a second title; keeping the outer card border — matched C3/AB4 cards, but a people grid of framed boxes made photo + name look like a form field.

## Page-level header band

**152. The page-header band is page-level; it is the only crumb + H1** — `PageHeaderBand` leaves layout and is mounted by every page shell except Home. `suppressPageHead` is gone. C1 and AB1 drop crumb / pageTitle (C1 also drops the unused inner title); ~~C7 unmounts from the programme opening and stays parked until a later restructure places it with apply / guidelines.~~ **Revised at the programme-sections unit:** C7 remounts first in Application. `config.dest` is consumed again. Stand for Art: rolling, so the button is never closed (decision 95). The `closed` panel toggle on the other three programmes has a consumer again (decision 153). The band remains the only crumb + H1. **Decisions 33, 137, 138, 139, 147 revised in place.** _One H1 per page, owned by the band. Opening blocks that also titled the page duplicated it once the band moved into the shell._ → Set aside: keeping C1 / AB1 as the H1 so C7 can sit on the title row — that's what produced the duplicate; deleting C7 — the block is still specced, just not on the opening.

## Programme page sections

**153. Programme pages compose four always-on sections plus optional S3** — `ProgramPage` walks `config.blocks` in current order and buckets by type so relative order inside a section is preserved, except S1, which is always last in Overview when present (decision 162). Overview (always): C1, C2 or the no-figures hint, S4 when `hero` is set, then C5 from the list (Abbara's C6 after C5), then S1 last. ~~then any of S1 / C5 from the list.~~ ~~then any of C3 / S1 / C5 from the list.~~ **Revised at the Abbara S4/C3 unit:** C3 is gone. **Revised at the S1-last unit:** S1 no longer shares config order with C5 — it is pulled out of the overview bucket and mounted last (decision 162). Application: ~~**C7 first**~~ **Revised at the C7 heading-row unit:** C7 sits on the Application h2 row, right-aligned and sticky (decision 159) (`closed={config.rolling ? false : state.closed}` — decision 95 restored), then C4, C8, C9, C10, S2, S6, S5. C6 stays next to the block it already qualifies — `section?: 'overview' | 'application'`, default `'application'` (Wijhat after S5); Abbara passes `'overview'` (after C5, still ahead of S1). Past is omitted when S3 is absent or gated off (`offeredOnly && !offered`); its visible h2 uses S3's existing label (`Past beneficiaries` / `Abbara organisations by round`). FAQs then Contact. Overview's `<h2>Overview</h2>` is `sr-only` — in the document outline, hidden in both views, never `.block-heading`. The other section h2s use `.block-heading` at `text-2xl font-bold`. `Block` gains `headingAs` (default `'h2'`); programme blocks that still pass `heading` (C4, C5, C8, C9, C10, S1, S2, S5, S6) pass `headingAs="h3"` and stay `text-lg`. C11, C12 and S3 drop `heading` so they do not duplicate the section title. C1, C2, C6, C7, S4 stay untitled. No extra section boxes — Figma's grey frames are grouping notes. **Decisions 135, 137, 139, 145, 152 revised in place.** _Figma (Mawred, node 287:587) groups the kit this way; the band stays page-level (decision 152)._ → Set aside: wrapping each section in a bordered frame — would add chrome the Figma frames don't mean; putting Stand for Art / Abbara C5 in Application — forms of support are what the programme is, not how to apply.

## Website shell

**154. Global chrome is a sticky right rail** — layout is a two-column flex: scrolling main (`flex-1`, no max-width) on the left and a sticky right rail (`w-64`, `h-screen`, 1px left border) on the right. `SiteHeader` owns the rail top: Logo → UtilityBar (Search + EN|AR, no centering) → vertical nav. ~~Dropdowns stay CSS-only (`group-hover` / `group-focus-within`) and fly left over main (`absolute right-full top-0`).~~ **Revised at the in-flow nav unit:** dropdowns stay CSS-only (`<details>` / `<summary>`, shared `name` so one is open at a time) and expand under the parent in the rail on click — not hover, and not as a left flyout. No panel or row borders; `+` on the trigger; space below an open menu so the next item sits clear. Subsequent nav items shift down while a menu is open. Publications' Library / Series groups and the optional Mawred's 20th item stay. `SiteFooter` owns the rail bottom: Newsletter is a link that opens a lo-fi popup (Field + Subscribe + Close); Social is a labelled placeholder; copyright stays. Footer link columns and the inline email row are gone. `PageHeaderBand` stays page-level inside main (decision 152). ~~The state panel stays `fixed` as a review overlay.~~ **Revised at the state-panel unit:** the panel is a top-right overlay, closed by default, opened from a cog (decision 155). **Decision 57 revised in place:** header/footer 2px rules no longer exist; the rail edge is 1px; 2px stays on primary buttons only. _Figma website shell (Mawred, node 299:355) is schematic — labelled slots, not a visual proposal. Stacked utility bar / header / newsletter / footer duplicated chrome above and below every page; the rail is one viewport-tall column while main scrolls._ → Set aside: keeping stacked chrome — what the port had, but it is not the shell Figma specified; a left rail — main is the left column in the schematic; a mobile breakpoint — same as the rest of the wireframes, none.

**155. State panel opens from a top-right cog** — the panel is closed by default. A 1px cog button sits `fixed top-3 right-3`; click opens the same panel below it (still `fixed`, still 2px). Click again closes it. Open/closed is inverted fill on the cog. Hiding the panel does not flip review chrome. ~~Toggles and the `codes` default are unchanged.~~ **Revised at the codes-default unit:** `codes` now defaults off (decision 156). _The always-on bottom-right overlay sat on every page as review chrome the client view doesn't need; a closed-by-default trigger keeps the page readable and the panel one click away._ → Set aside: a Dev on/off that also flips `codes` — would conflate "show the panel" with "client view"; leaving the panel always visible — what we had; putting the cog in the page-header band — Home has no band.

**156. Review chrome is off by default** — `codes` starts `false`; `<body>` carries `codes-off` in layout so the first paint is the client view, not a flash of badges. The panel toggle still turns it on. _The cog already hid the panel; leaving chrome on meant every page still opened annotated. Client view is what the wireframes are for._ → Set aside: keeping default-on and only hiding the panel — what we had; flipping `codes` when the cog opens — would couple "show the panel" with "show chrome".

**157. Figs values are `text-4xl`** — the figure number in `Figs` (C2, HM2, MN2) is `text-4xl`. Labels stay `text-xs`. **Revised after `text-2xl` still sat too close to body type.** _The value is the cell; page H1 is already `text-5xl` (decision 152's band), so `text-4xl` stays under the title and above section headings._ → Set aside: `text-2xl` — permitted-scale top, but not enough contrast; changing only Production Awards' ~400 — would split the shared `Figs` component.

**158. Production Awards drops C3** — the leftover three-card strip (eligibility, disciplines covered, grant period) leaves the page. ~~`C3AtAGlance` stays in the kit; Abbara is the remaining consumer.~~ **Revised at the Abbara S4/C3 unit:** C3 retires entirely. `C3AtAGlance`, the `C3flat` config arm, and the ProgramPage cases are deleted. The C3 code is not reused. Abbara's leftover facts go to S4 (disciplines, where they work) and C8 (budget+years, registration, previous beneficiary); who-can-apply is dropped as an S4 restatement. Abbara's S4 is now the scope summary this decision reserved. ~~Production Awards' titled "Who is eligible" S4 is still reserved for a later unit.~~ ~~**Revised at the Production Awards S4 unit:** that reserved S4 landed as three untitled rows.~~ **Rolled back:** Production Awards has no S4. Who-submits and funds-in-the-region stay in C8; under 35 and Arab origin stay in C1; the birth-year range stays omitted (it shifts every round). The reserved shape restated facts that already had homes. **Decisions 85, 97, 98, 139, 140, 145 and 160 revised in place.** _After dates moved to S1 (decision 145), every leftover card repeated another block: eligibility in C1, disciplines in C5, grant period in C10. Wijhat already has no C3; Stand for Art never did. Abbara's seven cards hit the same failure: who-can-apply restated S4, disciplines and where belonged in S4, the rest were C8 rules._ → Set aside: keeping C3 as the scan table — what decision 85 required, but the scan was three facts the visitor had just read or was about to; a titled "Who is eligible" S4 instead — reserved, tried, and rolled back because the facts already live in C1 and C8; keeping C3 for Abbara only — the last consumer, but it reproduced the same restatement this decision killed C3 for.

**159. C7 sits on the Application heading row and stays sticky** — C7 floats right of the Application h2 (`float-right`, `sticky top-4`) so the button shares that row and remains in view after the heading scrolls away, through Past / FAQs / Contact. C4 drops `withRepeat`; Apply now exists once. Same on all four programmes. C7's Block is `flush` so codes-off margin does not drop it off the heading line. _Two Apply nows was a leftover from remounting C7 above C4's in-block CTA. Sticky is this button staying put, not a second coloured bar — decision 2's per-program-colour CTA stays set aside._ **Decisions 137, 141 and 153 revised in place.** → Set aside: stacking C7 under the h2 — what 153 had, but the heading row is the apply action's home; keeping C4's repeat — the live pages do this, but one control already names the destination; a dedicated right column through FAQs — would indent later sections around a 104px gutter.

**160. S4 is a 2-up gapped grid; cell labels are h3s** — S4 leaves shared `Cells`. ~~Four (Wijhat) or two (Abbara) cells render `grid-cols-2` with `gap-8`, no card borders.~~ **Revised at the Wijhat S4/C5 unit:** S4 is stacked full-width rows. Each row is the `h3` label (`text-lg font-bold`, not `.block-heading`), the optional lead, then items as wrapping cards reusing C3's chrome (`border border-black p-2.5` — decision 140). ~~`grid-cols-[repeat(auto-fill,minmax(210px,1fr))]`~~ **Revised:** items are `flex flex-wrap`, each card `w-fit max-w-full shrink-0`, so a card is as wide as its copy; two sit on one line when they fit, otherwise they wrap. Body type on every card — no `text-xs` fork. ~~A row whose items are single terms (no `.!?`, at most three words) renders as a `Chip` row, not one-word cards.~~ **Revised:** the chip exception is gone. Short terms (Abbara's disciplines, a one-word Wijhat item) are the same cards; hug-content makes "Cinema" a small card rather than a stretched cell, so the chip size split is unused. The string `HeroCell` variant and its Fill fallback are dropped: every consumer now passes populated cells (decision 125's reasoning, not 84's). The Block stays untitled; the row labels are the titles (decision 153). Wijhat has three rows (Who is it for · For what purpose · From where to where). **Revised at the Abbara S4/C3 unit:** Abbara has three rows (Who is Abbara for · Disciplines covered · Where they work); the support cell is gone (decision 97 revised). ~~**Revised at the Production Awards S4 unit:** Production Awards has three rows.~~ **Rolled back:** Production Awards has no S4 (decision 158). **Revised at the Stand for Art unit:** Stand for Art has no S4 either (decision 161), so S4 is Wijhat and Abbara only. HM4 stays on Cells. **Decisions 80 and 86 revised in place; 140 noted.** _The 2-up grid made four summary headings look like a scan strip; stacking them with item cards is the at-a-glance treatment C3 established. Equal-width `minmax(210px)` columns stretched short copy and made taxonomy items look like a smaller kit; hugging content keeps one chrome for every item._ → Set aside: keeping Cells — would restyle HM4; putting `.block-heading` on the cell h3s — would hide the only titles the cells have in review chrome; ~~four-up without borders — the lists would be too narrow;~~ keeping the 2-up borderless lists — what this decision originally required, but it left S4 looking like titled prose rather than the scan block; keeping chips for term rows — what the Wijhat S4/C5 unit required, but once cards hug content a one-word card is already a chip.

**161. Stand for Art gets no S4; the page's restated facts are removed instead** — the last programme without a scope summary keeps its shape: C1 and C5 in Overview, C7–C4–C8–C9 in Application, then FAQs and Contact. S4 is Wijhat and Abbara only. What this unit did instead was take out four statements the page made twice. C1's second paragraph drops its closing sentence — the two ways an applicant can be at risk restated the first paragraph's own "because of the work they make … or because of an unsafe environment"; the five prioritised risk types stay. C4 drops "Complete it in Arabic" as a step, leaving the encrypted form and the documents, with the rule kept as C8's condition. C9's label drops ", and it sets the form of support", which the lead's second sentence states in full, so it now matches the other three programmes' short jury labels. C11's organisations question drops the individuals-only clause C8 carries verbatim and keeps only the redirect to Culture Resource's other programmes. **Decisions 94, 139 and 160 revised in place.** _With no S4 there is nothing to move anything into, so the plan's exchange — S4 gains a fact, C1 or C8 gives it up — has no second half, and what was left is one page saying the same thing twice. That is the failure mode decision 158 retired C3 for. Stand for Art's scope facts already have homes: C1 is the prose framing, C8 is the rules._ → Set aside: the planned S4 (Who it's for · What counts as a risk · Where) — it would lift the risk list out of C1 and two items out of C8 to restate them one screen higher, the trade that was rolled back on Production Awards; keeping the Arabic rule as C4's step and dropping C8's item — matches where the live page states it, but "complete it in Arabic" is a property of the form rather than a step, and C8 is where this kit puts application terms (decision 85); trimming the how-much FAQ against C5's "Set case by case" chip — an answer that opens by answering is not the same as an eligibility rule printed twice.

**162. S1 sits last in Overview** — when a programme has a timeline, it renders after C5 (and after Abbara's C6, which still qualifies C5). Production Awards: C1–C2–C5–S1. Wijhat: C1–C2–S4–C5–S1. Abbara: C1–C2–S4–C5–C6–S1. Stand for Art has no S1 — rolling intake, no dates — so Overview stays C1–C5. `ProgramPage` pulls S1 out of the overview bucket and mounts it last, so the rule does not depend on config array order. **Decision 153 revised in place.** _Overview is what the programme is, then what it offers, then when. Dates first made three of the four pages open on a calendar._ → Set aside: keeping S1 after S4 / before C5 — what 153 had, but the visitor then reads dates before they know the grant; putting S1 in Application — the calendar is still this round's facts, not how to apply.

