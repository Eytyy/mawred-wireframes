The decisions taken while wireframing each page-type. They bind later chats — follow them rather than re-deciding.

> **Note:** The pre-scaffold history file was not present after the Next.js install. Decisions 1–54 should be restored from backup when available. Port decisions 55–58 are logged below.

---

## Next.js port (global)

**12. Long facets truncate to search + first six + show all** — ~~a facet over ~12 values renders as a search field, the first six values and a "show all N"; shorter facets list every value.~~ **Revised at the facet-combobox unit:** the truncated panel is gone. Facets open as anchored combobox dropdowns (decision 168); scroll replaces truncation. The people-group clause is decision 50 and stands. **Restored here from the port (decisions 1–54 were missing after the Next.js install) so the revision has a home.** _A full-width panel of six wrapped labels plus a show-all was three affordances for one facet, detached from the trigger that opened them._ → Set aside: listing every value in a panel under the bar — blows the page on Programme's 38 values.

**33. Home omits the page-header band** — Home has no crumb and no H1; it opens on HM1. ~~Implemented as `suppressPageHead` on the home route while the band lived in layout.~~ **Revised at the page-level band unit:** the band is composed in each page shell, not layout. Home simply does not mount it. `suppressPageHead` is gone. Programmes, Who We Are and Our Team no longer omit the band — C1 and AB1 are overview / intro only (decision 152). _Home is the only page whose opening is not a titled document._ → Set aside: a band on Home too — would put a crumb and H1 above the cinematic hero. **Restored here from the port (decisions 1–54 were missing after the Next.js install) so the revision has a home.**

**55. State panel mechanism** — a client context provider holds the state bag; the list of toggles per page-type comes from a static map keyed off the route registry. _Block bodies read state values to decide what they render, so a provider is required regardless; pages registering states separately would duplicate that channel._ → Set aside: pages self-registering with the panel — simpler API, but two channels for the same values.

**56. Real nested URLs** — App Router paths replace legacy flat hashes. `/` is Home; the wireframe index lives at `/wireframes`. _Real routes with shareable URLs; the iframe hash problem decision 17 solved no longer applies._ → Set aside: keeping flat hash-style paths — would fight the App Router and lose readable URLs.

**57. Legacy border weights kept** — ~~2px on header rule and primary buttons~~ **Revised at the website-shell unit:** header/footer 2px rules no longer exist — the rail edge is 1px; 2px stays on primary buttons only (decision 154); ~~3px on N8 (and N15 quotes) callouts;~~ **Revised at the N5/N8 retirement:** 3px on N15 quotes only (decision 200); 2px dashed on stubs and the state panel. ~~2px on active tabs~~ **Revised at the tabs unit:** active tabs are 1px `border-neutral-200` on a light strip, not 2px black (decision 146). ~~3px on C6~~ **Revised at the C6 unit:** C6's set-apart treatment is a `bg-neutral-200` fill with padding and no border (decision 144), not weight. ~~3px on AB2~~ ~~**Revised at the About polish unit:** AB2 is a highlight, not a quotation — same grey fill as C6 (decision 148). N8 and N15 stay 3px.~~ **Revised at the Vision block unit:** AB2 is no longer a highlight (decision 148); C6 keeps the grey fill; ~~N8 and N15 stay 3px.~~ **Revised at the N5/N8 retirement:** N15 stays 3px; N8 is gone (decision 200). _Weight is the legacy file's only emphasis signal for quotes; C6 is a highlight, not a quotation. The 2px header/footer rules belonged to stacked chrome that decision 154 replaced._ → Set aside: strict 1px everywhere — obeys the original AGENTS constraint but makes N15 indistinguishable from prose blocks.

**58. Type scale collapsed** — legacy 10/11/12px → `text-xs`; 13/14px → `text-sm`; ~~body base `text-sm`~~ **Revised at the 16px body unit:** body base is `text-base` (16px); `text-sm` is remapped to 16px so the collapsed 13/14px layer is no longer smaller than body (decision 163). `--fill:#eee` → `neutral-200`; `--soft:#666` → `neutral-500`. _Maps the legacy palette onto the permitted Tailwind set without inventing arbitrary sizes._ → Set aside: preserving exact pixel sizes — outside the permitted utility set.

**59. Square timeline markers** — S1 programme timeline uses 2.5×2.5px square markers instead of the legacy round dots. _The permitted Tailwind set excludes `rounded-*`; a square marker preserves the sequence cue without breaking the lo-fi constraint._ → Set aside: round dots via inline styles — would work but adds a one-off exception outside the utility set.

**60. Single client boundary at ProgramPage** — wireframe state (`closed`, `offered`) is read once in `ProgramPage` and passed down as props; block components stay server-renderable except where a child primitive requires interactivity (`Accordion`). _One boundary is easier to trace than scattering `"use client"` across C7 and S3._ → Set aside: per-block client components — each block self-contained, but duplicates the state channel. **Note:** S5 is a client block as of decision 143 — it owns its Individual/Group tab index the same way Accordion owns its toggle (decision 64). Wireframe state still enters once at `ProgramPage`.

**61. C3 is facts; sequenced dates are S1** — ~~`C3AtAGlance` takes a `mode` prop (`"flat"` | `"rounds"`) rather than the legacy's two separate functions.~~ **Revised:** C3 is a wrapping card grid of facts (decision 140); sequenced dates — Wijhat's rounds, Production Awards' open/deadline/results — live in S1 (decision 145). Rounds mode is dropped; Wijhat was the only consumer. **Revised at the Abbara S4/C3 unit:** C3 retires. Sequenced dates stay in S1. Scope facts live in S4; compliance rules live in C8. The C3 code is not reused. _Same treatment, same block code; one component per block is the component-discipline rule._ → Set aside: two components (`C3flat`, `C3grouped`) — mirrors the legacy literally, but splits one block into two files.

## Publications

**62. Shared wireframe primitives over Publications-local ones** — `FilterBar`, `CountRow`, `Card`/`Cover`, `Embed` and `EmptyState` live in `components/wireframe/` because Mawred Network and News will reuse them. _One primitive per pattern; the unit that first needs a primitive owns it for the whole app._ → Set aside: Publications-local copies — faster to ship but duplicates MN3/MN4 and N1–N3 work later.

**63. `Chip` reused for removable filter chips** — active filter chips carry the `×` in the label rather than a separate `.chipx` size variant. _Decision 58 already collapsed the legacy size difference; one chip component is enough._ → Set aside: a dedicated removable-chip variant — mirrors legacy markup literally but adds a one-off size fork.

**64. `FilterBar` self-contained as a client component** — facet open/close state lives inside `FilterBar`, the same way `Accordion` owns its toggle. Page shells (`LibraryPage`, `SeriesPage`) remain the single wireframe-state boundary per decision 60. _Interactive primitives own their own UI state; the page shell only reads panel toggles._ → Set aside: lifting facet state into `LibraryPage` — one fewer `"use client"` file, but spreads interactivity up a tier. **Note:** Selection state joins open/close inside the primitive as of decision 168.

**65. Audio embeds are a fixed height; video is 16:9** — ~~audio and video embeds use fixed height boxes rather than aspect-ratio utilities; the SoundCloud embed has no meaningful aspect to preserve.~~ **Revised at the PB2 video-grid unit:** audio stays `h-[58px]` — SoundCloud has no meaningful aspect. Video uses `aspect-video` (`16/9`), the same allowed `aspect` utility Cover already uses (decision 166). _A fixed 190px box reads as a squat bar once video sits in a two-column grid; 16:9 is the YouTube footprint, not a design proposal._ → Set aside: provider-specific aspect ratios for both kinds — what this first set aside, but video has an aspect and audio does not; keeping 190px on video — the original wireframe signal, but it squashes the tile at two columns.

## Mawred Network

**66. Entity-tab state in DirectoryPage** — the Individuals & Groups / Organizations & Initiatives tab index lives in `DirectoryPage`, not inside the `Tabs` primitive, because MN4 (count row) and MN5 (listing) both need the same value. _One source of truth at the shell; the tabs are navigation, not a wireframe-state toggle._ → Set aside: self-contained tab state inside `Tabs` — simpler primitive API, but MN4 would need a callback or duplicate state.

**67. Tabs and DirRow as shared primitives** — `Tabs` and `DirRow`/`Badge` live in `components/wireframe/` ahead of the News unit, which reuses the same tab row and will need a row pattern for the post feed. **Revised at the listing-row primitives unit:** `DirRow` takes an optional `leading` slot before the title so a listing row can carry a compact Cover. Network (MN5), About (AB12) and HM6 omit it. _One primitive per pattern; the Network unit that first needed them owns them for the whole app (extends decision 62)._ → Set aside: Network-local copies — faster to ship but duplicates N1/N3 work later; a second row component with a thumb — would fork the directory row the listings are joining.

**68. Cover label prop over a second cover component** — `Cover` takes an optional `label` prop (default `"cover"`) so MN6 can render `photo / logo` without a separate portrait/logo component. ~~_Same footprint, same block treatment; one component per pattern._~~ **Revised at the portrait-cover unit:** the shared footprint is now `aspect-3/4` (decision 166). `tall` only drops the card margin — it is no longer a second height. _Same footprint, same block treatment; one component per pattern._ → Set aside: a dedicated `PortraitCover` — clearer naming, but two components for the same box.

## About

**69. Timeline extracted as a shared primitive** — the rail-and-marker treatment moves to `components/wireframe/Timeline.tsx`, taking `label` / optional `meta` / `fill` per item; S1 (programme timeline) is refactored onto it and AB5 (history) uses it. _The legacy file already shared one `.tl` rule between both blocks, and the rail is real styling — absolute marker, left border, transparent last row — not two utility classes. Decision 45 keeps About's **blocks** out of the programme kit; it doesn't require duplicating a primitive._ → Set aside: an About-local timeline — literal reading of decision 45, but two copies of the same rail to keep in step.

**70. AB3 mission cells not extracted** — ~~AB3 repeats the cell-row markup that S4 (programme summary) already carries rather than pulling a shared `Cells` primitive out. _Three utility classes and a one-line cell body; the pattern's third user (HM4) isn't built yet, so extraction is better decided when Home lands and the real variation is visible._~~ **Superseded by decision 80** — Home landed with HM4 as the third consumer; `Cells`/`Cell` extracted and S4/AB3 refactored onto it.

**71. AB7 bio expander reuses `AccordionItem`** — ~~the per-card "Read bio" expander is the shared accordion primitive, not a smaller About-local one.~~ **Revised at the About polish unit:** AB7 no longer uses AccordionItem. Records with a bio get a disclosure arrow next to the name (`▸` / `▾`); the name row toggles the bio in place. Records without a bio stay static (decision 48). Default Accordion stays boxed for S3 and C11. AB7 is a client block owning open/closed state (decision 151). _One interactive primitive per pattern (decision 64) still holds for accordions; a people-card disclosure is a different job from a labelled expander._ → Set aside: a compact card-sized expander — closer to the legacy density, but a second toggle component doing the same job.

**72. Banner extracted as a shared primitive** — the full-width 210px fill box moves to `components/wireframe/Banner.tsx`, taking a `label` prop; AB10 (initiative record) uses it first, with HM5 and N6 to follow. _The legacy file already shared one `.banner` rule across three blocks; extracting now avoids a third copy when Home and News land (extends decisions 62/67)._ → Set aside: an About-local banner — literal per-page ownership, but duplicates the same box for News and Home later.

**73. `EmptyState` action prop** — `EmptyState` takes an optional `action` string (default `"Clear all filters"`) so AB12 can pass `"Contact Mawred"` without a Careers-local empty component. _One empty-state primitive per pattern; the button label is the only variable._ → Set aside: a second empty-state variant — clearer intent in the type name, but two components for the same dashed box.

**74. `KV` fill prop** — `KV` takes an optional `fill` boolean (default `true`) so AB13's "Or write to — [contact address]" row renders label-only with no value bar. _Same row component; the value column is optional per field, matching publication metadata omission._ → Set aside: a label-only row hand-rolled in AB13 — works, but forks the row pattern for one field.

## News

**27. Category is a tab row; Programme is the optional facet** — ~~one primary dimension — category — as a tab row, with the optional “by programme” cut as a single facet trigger beside it. Deliberately lighter than the six-facet bar on Publications and the Network.~~ **Revised at the news-filters unit:** both cuts are FilterBar facets on N1. Category is still the first trigger; it is not a different control. “All” is the unfiltered default, not a value. **Restored here from the port (decisions 1–54 were missing after the Next.js install) so the revision has a home.** _Two short category values do not warrant tabs (decision 19); a tab row made one reverse-chronological feed look like two archives._ → Set aside: keeping the tabs because category is the primary dimension — primary is order, not a second primitive.

**28. Count row chips filters, not the visible tab; News has no sort** — ~~the count is scoped to the open tab. Only the programme filter chips, since the active tab is already visible above. No sort control — reverse-chronological is the only meaningful order for a dated feed.~~ **Revised at the news-filters unit:** there is no tab. Category and Programme both chip when applied, same as PA3. No sort stands. The “don’t chip a control already visible above” clause still holds for Network’s entity tabs (decision 181). **Restored here from the port (decisions 1–54 were missing after the Next.js install) so the revision has a home.** _A facet that is not already named by a tab has to chip, or the count row cannot say what it is showing._ → Set aside: chipping the open tab as well — restates a control the visitor can already see; adding a sort to News — a dated archive has one meaningful order.

**75. `Tag` as a shared primitive distinct from `Badge`** — the uppercase tracked label box lives in `components/wireframe/Tag.tsx`, separate from `DirRow`'s `Badge`. _Under the collapsed type scale (decision 58) the only surviving difference is `uppercase` + `tracking`, and that difference is the whole signal for category labels on cards and N7's meta strip._ → Set aside: reusing `Badge` with an uppercase class — one component, but loses the semantic split between directory row labels and taxonomy tags.

**76. `Caption` extracted as a shared primitive** — the dashed optional-field box moves to `components/wireframe/Caption.tsx`; PB2 is refactored onto it so the news card excerpt is not a second copy. _One primitive per pattern (extends decisions 62/67); the legacy `.cap` rule was already duplicated inline in PB2._ → Set aside: hand-rolling the dashed box in each block — mirrors the legacy literally but forks on the second consumer.

**77. `Card` footer prop** — `Card` takes an optional `footer` ReactNode rendered after the subtitle, so the news card's excerpt slot sits below the date without overloading `children`. _Category tag stays in `children` beside `Cover`; the excerpt is structurally below title and date, matching the legacy card order._ → Set aside: putting the excerpt in `children` after the title — simpler API, but breaks the date-then-excerpt sequence.

**78. Category-tab state in NewsLandingPage** — ~~the All / Announcements / News & Events tab index lives in `NewsLandingPage`, not inside `Tabs`, because N2 (count row) scopes its count to the open tab (extends decision 66).~~ **Revised at the news-filters unit:** retired. Category is a FilterBar facet (decision 190); selection lives inside the primitive (decisions 64 and 168). `NewsLandingPage` still reads `filtered` / `empty` / `slots` and derives `noResults`. N2 no longer takes a tab index. _The shell owned the index because two blocks needed it; once the tabs left, nothing did._ → Set aside: self-contained tab state inside `Tabs` — the original alternative, now moot.

**79. `NewsDetailPage` as single client boundary** — wireframe state (~~`structured` / `byline` / `edge`~~ **Revised:** `byline` — `structured` retired at decision 122, `edge` at 200) is read once in `NewsDetailPage` and passed down as props; ~~optional blocks N8/N10/N11 render by omission when `structured` is off~~ N10/N11 are unused sampling gaps (extends decision 60). _One boundary is easier to trace than scattering `"use client"` across nine blocks._ → Set aside: per-block client components — each block self-contained, but duplicates the state channel.

## Home

**35. HM1 advances on its own, with no controls** — ~~the hero sequence auto-advances and carries a marker bar; a marker can be clicked to jump, and interaction pauses the advance. Not scroll-driven.~~ **Revised at the HM1 hero unit:** the marker bar is gone. The sequence is a slow cross-fade with nothing to click — no markers, no arrows, no pause affordance. The frame-count note top-left stays as the label naming what the box stands for (five frames, a funded artist / work / organization), not as a control. ~~**Revised at the HM1 collage unit:** there is no sequence. HM1 is five overlapping frames in a composition (decision 165); nothing advances, so `staticfb` retires with the motion it was the fallback for. The note top-left names the composition, not a count in a sequence.~~ **Rolled back with decision 165:** the collage is gone; the sequence and `staticfb` return. _The controls were the only thing making the block read as a slider, and a slider is not what a hero is for: they sat on a row below the frame, took the eye off the imagery, and promised an interaction the wireframe has no way to show. Advance on its own needs no chrome._ → Set aside: scroll-driven frames — ties identity to scroll position, so a visitor who does not scroll never sees the sequence; ~~keeping the markers as a count indicator only — they still read as buttons;~~ ~~a collage with no sequence — the version this rollback replaces.~~ **Restored here from the port (decisions 1–54 were missing after the Next.js install) so the revision has a home.**

**36. HM1 frame captions** — ~~each frame carries a persistent caption chip bottom-left naming the artist, work and programme, rather than revealing it on hover.~~ **Revised at the HM1 hero unit:** no caption renders. `HOME_HERO_FRAMES` keeps its five sourced captions — they are what makes the frame count count something real — but HM1 does not draw them. The frame holds the note top-left and the positioning line bottom-left; ~~nothing else~~ ~~**Revised at the HM1 plate unit:** the plate also holds the wayfinding buttons (decision 164 revised). Still no caption.~~ **Revised at the HM1 inline-wayfinding unit:** the plate is the rewritten line only; the three destinations sit in the prose (decision 186). Still no caption. Whether a funded work is credited inside the frame, under it, or not at all is left open. ~~**Revised at the HM1 collage unit:** still no caption. Five overlapping frames cannot each carry a chip, and a single credit for a composition of five works would pick a winner. Captions stay in config as sourcing, unrendered.~~ **Rolled back with decision 165:** still no caption, now because it competed with the positioning line on a single frame, not because five frames couldn't share a chip. _Persistent-not-hover was the right call against hover, and it still is; what changed is that the caption was competing with the positioning line for the same corner. Two stacked plates made the frame look like a card with metadata, and the identity sentence is the thing the hero is for._ → Set aside: hover reveal — hides content behind an interaction the wireframe can't show, and the original reason this decision exists; keeping the caption above the line — the version this revision replaces; moving the caption under the frame — a live option, but there is no row under the frame anymore (decision 164 revised) and the credit is not a wayfinding element. **Restored here from the port (decisions 1–54 were missing after the Next.js install) so the revision has a home.**

**37. Figures sit as an adjacent band, not overlaid on the wall** — ~~Figures sit as an adjacent band above the mosaic wall, not overlaid on it. Numbers over arbitrary photography can't be guaranteed legible. Figures count up on scroll; degrade to plain numbers. Tiles are uncaptioned: captioning eight tiles is noise at tile size, and the hero already names its material.~~ **Revised at the HM2 stat-strip unit:** the wall retires. HM2 is `<Figs wide />` only — mosaic grid, `Banner` fallback, `HOME_MOSAIC` and `MosaicTile` are deleted. Overlay vs adjacent is no longer a question, and uncaptioned tiles with it. ~~The `00` grants rail moves last so the strip does not open on a blank; the gap stays visible (decision 131).~~ **Revised at the HM2 figures unit:** the grants rail carries a placeholder number and leads the strip; years-since-2003 is gone (decision 131 revised). _Adjacent-not-overlaid was the right call against numbers on photography, and it still is; what changed is that the photography left the block. A second grid of faces under a hero that already carries them was the same restatement the identity paragraph was._ → Set aside: overlaying figures on the wall — the original reason this decision exists; keeping the wall as a distinct mosaic — the version this revision replaces. **Restored here from the port (decisions 1–54 were missing after the Next.js install) so the revision has a home.**

**38. Hero and wall are two distinct blocks sharing material** — ~~Hero and mosaic wall are two distinct blocks sharing the faces-of-impact material (same Network record source), not a continuous dissolve.~~ **Revised at the HM2 stat-strip unit:** the wall retires. The hero is the only place the faces-of-impact material appears; HM2 no longer shares it. `staticfb` is now hero-only — it still switches HM1's frame note to the no-motion label, and no longer has an HM2 consumer. ~~**Revised at the HM1 collage unit:** `staticfb` retires. The collage does not move (decision 165), so the fallback has no sequence to fall back from. Removed from `lib/wireframe-state.tsx`, `components/StatePanel.tsx` and `lib/pages/states.ts`.~~ **Rolled back with decision 165:** `staticfb` is hero-only again. _Two distinct blocks was the right call against a dissolve the wireframe couldn't show; what changed is that sharing the material across two blocks put the same faces on the page twice. One surface is enough._ → Set aside: a continuous dissolve from hero into wall — spec §3A's chosen direction, but a handoff the lo-fi artifact can't demonstrate; keeping both blocks as two surfaces of the same material — the version this revision replaces. **Restored here from the port (decisions 1–54 were missing after the Next.js install) so the revision has a home.**

**39. Impact before opportunities — the story earns the ask** — ~~HM2 (impact) sits above HM3 (open opportunities). Spec §3C's wireframe flag leaned this way: impact first, the ask immediately after.~~ ~~**Revised at the HM3-into-HM4 unit:** HM3 retires. Impact (HM2) still sits above the programmes grid (HM4), which now carries open-call status as a badge on the cards that have one. The story still earns the ask; the ask is the four-programme offer. The HM3 code is not reused (same precedent as C3 in decision 61). _Impact-first was the right call against leading with the deadline, and it still is; what changed is that a separate open-calls strip and a lighter all-four strip were two statements of the same four programmes._~~ **Revised at the HM4 open-opportunities unit:** Impact (HM2) still sits above HM4. The ask is the open grants — Production Awards, Wijhat, Stand for Art — not the four-programme offer. The HM3 code is not reused (same precedent as C3 in decision 61). _Impact-first was the right call against leading with the deadline, and it still is; what changed is the ask. Folding HM3 into HM4 made it the whole offer; this unit puts spec §3C back on HM4 and leaves spec §3D dropped._ → Set aside: opportunities first — the original flag's alternative, which would lead with the deadline before the visitor knows who Mawred is or what it has done. **Restored here from the port (decisions 1–54 were missing after the Next.js install) so the revision has a home.**

**40. HM4 is a compact text row, lighter than HM3's cards** — ~~HM4 renders as a compact cell row, deliberately lighter than HM3's cards, so the two don't read as duplicate strips. An open programme appears in both — different jobs: HM3 says act now, HM4 says here is the whole offer.~~ ~~**Revised at the HM3-into-HM4 unit:** HM3 retires. HM4 takes HM3's card — shared `Card` + `CardGrid`, `Badge` holding `programme.status` when present, descriptor in the footer. The `Programme` eyebrow is gone. An open programme appears once. Abbara (no status) is a card without a chip, which is also how "nothing open" reads: four cards, no badges, rather than an empty block (decision 130 revised). _Lighter-than-HM3 was the right call against two identical strips, and it still is as a reason; what changed is that keeping both strips was the restatement. One surface does both jobs if the open ones carry a status chip._~~ **Revised at the HM4 open-opportunities unit:** HM4 is spec §3C's open-calls strip, not the all-four overview. Shared `Card` + `CardGrid`, `Badge` holding required `status`, descriptor in the footer. Three cards now — Production Awards, Wijhat, Stand for Art. Abbara is absent because Abbara 08 closed in July 2025. Spec §3D stays dropped — Home does not grow a fifth block. The HM3 code is not reused. ~~the fourth CardGrid column stays empty until another call opens (decision 167).~~ **Revised at the HM4 card-layout unit:** status `Badge` sits under the descriptor, not above the title; titles are `text-2xl`; three cards fill a three-column row (decision 184). _One surface for both jobs was the right call against two identical strips; what changed is that listing a closed programme next to the open ones made HM4 an overview again, which is spec §3D, not the ask. The empty fourth column was the leftover of keeping CardGrid at four; three open calls should fill the row._ → Set aside: cards on both — the version the first revision replaced, which made Home look like it had two programme listings; keeping Abbara as a chipless card — the version this revision replaces, which restated the whole offer; leaving the fourth column empty — the version this revision replaces. **Restored here from the port (decisions 1–54 were missing after the Next.js install) so the revision has a home.**

**41. One combined Publications strip, not two** — ~~HM7 is one combined strip of latest covers and one media episode, not a documents strip plus a media strip. Two three-item strips for a taste of one section makes Home sprawl.~~ **Revised at the HM7-out unit:** HM7 retires. `HM7LatestPublications`, `PubStripItem`, `HOME_PUB_STRIP` and `HOME_ALL_PUBLICATIONS_HREF` are deleted, and the HM7 code is not reused (same precedent as C3 in decision 61). Publications has no Home surface; the library and the three series stay reachable from the rail. Spec §5's "every inner section is represented" no longer holds — a deliberate departure, logged as client-visible. _One combined strip was the right call against two mini-strips, and it still is as a reason; what changed is that a taste of Publications on Home was the same latest-of aggregator the spec was trying to leave behind, and the page already had enough strips. The library is a destination, not a teaser._ → Set aside: two mini-strips — the original flag's alternative, which sprawls; keeping the combined strip — the version this revision replaces; folding one publication into HM5's feature slot — would make the feature slot a Publications feed, which it isn't. **Restored here from the port (decisions 1–54 were missing after the Next.js install) so the revision has a home.**

**42. Identity lead-in kept light** — ~~HM8 is one paragraph from Who We Are's second intro paragraph (the services-and-public-good cut, so it does not repeat HM1's sentence) plus a route into the merged Who We Are page. The hero carries identity as a positioning line, so this is the editorial hand-off, not a second identity statement.~~ **Revised at the HM1 hero unit:** HM8 retires. The positioning line now sits inside the hero frame at `text-2xl`, so identity is stated at the top of the page rather than restated at the bottom; ~~the route into About moves onto HM1's wayfinding as a third link ("Who we are").~~ ~~**Revised at the HM1 plate unit:** those links sit in the plate under the sentence, not on a row under the frame (decision 164 revised).~~ ~~**Revised at the HM1 inline-wayfinding unit:** "Who we are" sits in the positioning sentence as an inline link, with Grants & Opportunities and Mawred Network (decision 186).~~ **Revised at the HM1 positioning-copy unit:** the About route is "More about us →" in the positioning line, with "support artistic creativity" and "encourage cultural exchange" (decision 186 revised). `HM8IdentityLeadIn`, `HOME_IDENTITY_TEXT` and `HOME_WHO_WE_ARE_HREF` are deleted, and the HM8 code is not reused (same precedent as C3 in decision 61). _Kept light was already the answer to a block with little to do. Once the hero states the identity properly, a second paragraph nine blocks down is the same restatement failure decisions 158 and 161 removed on the programme pages — and Home is a summary index, so the paragraph's only real job was the link._ → Set aside: folding it into the footer — leaves Home with no editorial route into About, which is why the original decision kept the block; keeping HM8 and leaving the hero's line small — the version this revision replaces. **Restored here from the port (decisions 1–54 were missing after the Next.js install) so the revision has a home.**

**43. When one page-type surfaces another's content, it reuses that page-type's card verbatim** — ~~HM6 reuses the News feed card (image · category tag · title · date); a type tag is the only permitted addition, and only where a strip mixes kinds.~~ **Revised at the HM6 rows unit:** HM6 leaves `NewsCard` for the shared `DirRow` — title `flex-1`, date and category as `Badge`s, `border-b border-neutral-200` per row. No image. ~~N13 still reuses the feed card verbatim, so the rule holds on News; Home is the exception.~~ **Revised at the news listing-rows unit:** the News feed card is a thumb `DirRow` (compact Cover · title · date · category `Badge`s). ~~N13 reuses that row, not the landing lead.~~ ~~**Revised:** N13 reuses that row. There is no landing lead.~~ **Revised:** the feed row is the PA4 stack — compact Cover · date · title · category — not a `DirRow`. N13 reuses that row. HM6 stays the image-less `DirRow`. Home remains the exception. Standing constraint 4 revised in place. _The verbatim-card rule was the right call against inventing a second card for the same post, and it still is on News. What changed is that Home is a summary index sitting under a hero that already carries the imagery — five blocks down, the feed card's cover is the wrong thing to repeat, and a row is how this kit already lists named records (MN5, AB12)._ → Set aside: keeping the feed card on Home — obeys 43 literally, but restates the News landing as a three-card grid; a Home-local row rather than `DirRow` — one fewer import, but a third copy of the same name-plus-meta line. **Restored here from the port (decisions 1–54 were missing after the Next.js install) so the revision has a home.**

**44. The 20th is a state on HM5, not a block of its own** — ~~the campaign occupies the featured/spotlight slot when active, then reverts to other highlights, so Home can carry it without baking a temporary thing into permanent structure. The rail still carries an optional dashed item pending the client's permanent-vs-temporary call.~~ **Revised at the HM5-owns-the-20th unit:** the rail item is gone. `NavGroup.optional` and the dashed-border branch retire with it — it was the only optional item. ~~`/mawreds-20th` stays as the campaign destination reached from HM5's `twentieth` branch; its stub message names the real gap (no campaign copy) rather than the nav question. The permanent-vs-temporary question stays the client's; our structure no longer depends on the answer either way. **Decisions 133 and 154 revised in place.**~~ **Revised at the Home spotlight unit:** the 20th is the spotlight content, not a state on HM5. The slot is still one block; the occupant lives in `HOME_SPOTLIGHT`. The `twentieth` review toggle is gone. `/mawreds-20th` stays as the stub destination from HM5. The permanent-vs-temporary question stays the client's. **Decisions 82 and 133 revised in place.** ~~_A state on the feature slot was already the answer to not baking a campaign into the kit. Leaving it in the rail as well asked the client a question the structure had already answered, and a dashed nav item reads as unfinished chrome rather than a campaign._~~ _A slot rather than a block of its own was already the answer to not baking a campaign into the kit. Leaving it in the rail asked a question the structure had already answered; keeping a review toggle once the occupant is chosen restated a config value as a state._ → Set aside: keeping the dashed rail item until the client answers — the version the first revision replaced; deleting the `/mawreds-20th` stub with the nav item — would leave HM5 with nowhere to go; keeping `twentieth` as a review state — the version this revision replaces. **Restored here from the port (decisions 1–54 were missing after the Next.js install) so the revision has a home.**

**80. `Cells`/`Cell` extracted as shared primitives** — the flex-wrap cell row moves to `components/wireframe/Cells.tsx`; S4 (programme summary), ~~AB3 (mission) and~~ ~~HM4 (programmes overview)~~ refactored onto it, resolving decision 70's deferral. **Revised at the About polish unit:** AB3 leaves Cells for a gapped card grid matching AB4 (decision 149); ~~S4 and HM4 stay a tight shared-border row.~~ **Revised at the S4 unit:** S4 leaves Cells for a 2-up gapped, borderless grid (decision 160); ~~HM4 remains the Cells consumer.~~ **Revised at the Wijhat S4/C5 unit:** S4 is stacked rows of item cards, not a 2-up grid (decision 160 revised). **Revised at the HM3-into-HM4 unit:** HM4 leaves Cells for the shared `Card`/`CardGrid` (decision 40 revised). `Cells`/`Cell` and `components/wireframe/Cells.tsx` are deleted — no consumers remain. _Three consumers with the same wrapper and only the cell body differing; extraction on two samples was premature, and the third consumer left too. The primitive outlived every user._ → Set aside: leaving three inline copies — worked at two samples, but HM4 confirmed the pattern before a fourth user appeared; keeping Cells for a future consumer — a shared primitive with no caller.

**81. HM1 hero frame stays local** — ~~the 300px cinematic frame (note top-left, caption chip bottom-left, optional marker bar)~~ **Revised at the HM1 hero unit:** the frame is `aspect-[16/9]` with a `min-h-[360px]` floor rather than a fixed 300px, and holds the note top-left and the positioning line bottom-left; the marker bar is gone (decision 35 revised) and no caption is drawn (decision 36 revised). It stays inside HM1 rather than extending `Banner` with internal slots and a second height. ~~**Revised at the HM1 collage unit:** the single fill is gone. HM1 holds a `relative` composition area (`aspect-[16/9]`, `min-h-[420px]`) with one `absolute` fill box per frame; placement and `z` come from `HOME_HERO_FRAMES` (decision 165).~~ **Rolled back with decision 165:** the single `aspect-[16/9]` fill returns. _Banner is a single fill box (decision 72); the hero frame carries structure Banner was never meant to hold, and a hero that scales with the column is the one place a fixed height was doing harm._ → Set aside: a taller `Banner` variant with slots — one primitive, but overloads `Banner` with hero-only internals; a taller fixed height — simpler, but the frame stops reading as full-bleed on a wide column; ~~keeping the single `aspect-[16/9]` fill — the version this revision replaces.~~

**82. `HomePage` as single client boundary** — wireframe state (~~`nocalls` /~~ ~~`twentieth` /~~ `staticfb`) is read once in `HomePage` and passed down as props (extends decisions 60 / 79). **Revised at the HM3-into-HM4 unit:** `nocalls` retires with HM3 (decision 130 revised). ~~**Revised at the HM1 collage unit:** `staticfb` retires with the sequence (decision 165).~~ **Rolled back with decision 165:** `staticfb` stays. **Revised at the Home spotlight unit:** `twentieth` retires; Home still reads `staticfb` only. _One boundary is easier to trace than scattering `"use client"` across the remaining blocks._ → Set aside: per-block client components — each block self-contained, but duplicates the state channel.

## Next.js port (continued)

**83. Page shells co-located with routes** — the 16 `<Name>Page.tsx` composition files move out of `components/blocks/<page-type>/` and sit next to the route that renders them; `page.tsx` stays a two-line wrapper. Shared shells (`ProgramPage`, `SeriesPage`) sit at the route-group level (`app/programs/`, `app/publications/`). _A shell is route composition, not a reusable block; `components/blocks/` holds blocks only, and a shell has exactly one caller so distance from the route bought nothing._ → Set aside: inlining the shell body into `page.tsx` — one file per route, but loses the named shell and forces `"use client"` onto the route file; `components/pages/<page-type>/` — clean split, but no closer to the route than before.

## Content population

**84. Content props are optional, with the placeholder as the fallback** — every prop added to carry real content (`Prose.text`, `KV.value`, `Figs` item values, and the C1/C3/C4/C5/C8/C9/C10/C12 additions) is optional, and a block given nothing renders exactly the fill bars it rendered before. _Population runs one page-type per unit over shared blocks; a required prop would force every other page-type to be populated in the same session, or hand-fed empty strings until its own turn came._ → Set aside: separate populated and unpopulated variants of each block — explicit about which is which, but doubles the block kit and breaks the one-component-per-block rule; required props with placeholders passed from config — moves the fill-bar decision into nine config files.

**85. Eligibility detail folds into C8** — the Guidelines-tab eligibility rules (re-application, who submits per discipline, under-18 documents) render as C8 items alongside the exclusions and application terms; ~~C3 keeps only the one-line eligibility summary~~ **Revised:** Production Awards no longer has C3 (decision 158). The under-35 / Arab-origin line lives in C1; the detail stays in C8. ~~Abbara still uses C3 for structured eligibility (decision 98).~~ **Revised at the Abbara S4/C3 unit:** C3 retires. Abbara's structured eligibility splits — disciplines and where-they-work to S4 (scope); budget-band fused with years operating, registration, and previous-beneficiary to C8 (rules). The invented "Budget band" label is gone; the sourced figures survive. C8 on Abbara is nine items and is no longer only form mechanics. ~~**Revised at the Production Awards S4 unit:** who-submits and funds-in-the-region leave C8 for S4.~~ **Rolled back:** those facts stay in C8. Production Awards has no S4 (decision 158). _Eligibility has no block of its own, and the detail is a list of conditions — the same shape as the rest of C8._ → Set aside: a new eligibility block — cleaner semantics, but re-opens structure the spec settled, for all four programmes; expanding C3 with the detail — no new block, but buries the dates the table exists to surface. **Note:** a titled "Who is eligible" S4 was reserved, then tried, then rolled back — the facts already have homes in C1 and C8.

**86. Wijhat's covered-costs list goes in S4's "The grant" cell, not a C5** — ~~the €7,000 ceiling and the five cost types it covers (tickets, visa costs, accommodation, partial living expenses, internal transport) render as a lead plus a bullet list in the page-top summary cell; Wijhat gets no C5 block.~~ **Revised at the Wijhat S4/C5 unit:** S4 drops "The grant". Wijhat gets a single-record C5 — label "Travel grant", chip "Up to €7,000", ~~the five covered costs as `items`~~ **Revised:** the five covered costs as comma-separated `body` text, not a bullet list. Heading stays the default "Forms of support". C1 already states the ceiling and the partial-living-expenses caveat as prose, so the list is not restated there. _The set-aside is now the decision. S4 is the scope summary (who / what-for / where); C5 is what you get. A one-record C5 is the honest itemisation once that split holds on every programme, and the list appears once. Five short cost types are a sentence, not a list; Abbara and Stand for Art keep `items` because those records are longer provisions._ → Set aside: ~~a single-record C5 — gives the list its own labelled block and keeps S4 cells short, but a one-row support table isn't a support table, and the list would then appear twice on one page.~~ keeping the list in S4 — what this decision originally required, but it mixed "is this me?" with "what do I get?" and blocked C5 from applying to all four programmes.

**87. The visa recommendation letter is a C6 note, placed after S5** — the provision that Culture Resource can write to the embassy once the contract is signed renders as the highlighted-note callout, immediately below the application paths. _No block owned it, and C6 is the kit's slot for exactly this: one short provision that matters more than its length. Sitting after S5 it answers the "grantees obtain their own visas" line the visitor has just read, and visas are named in C1's problem framing, so the answer belongs on the page rather than buried in contract terms._ → Set aside: a line in each S5 path — where the live page puts it, but it's stated twice there and reads as a step in applying, which it isn't; a C10 item — accurate, since it is post-signing, but it would only be found by someone already reading the contract terms.

**88. S5's shared core holds everything both tracks share; each path holds only its divergences** — ~~the portal sequence, the save-and-return form and the purpose statement sit in the core~~ **Revised at the Wijhat application-highlight unit:** the portal sequence stays in `core`; the save-and-return form and the purpose statement sit in S5's optional `highlight`, the same grey fill as C6. ~~still above the tabs (decision 177)~~ **Revised:** the highlight sits last in S5, after the tabs, immediately before C6 (decision 177). The Individual and Group sections carry only what differs, plus their own document lists. _The two live tracks share about 80% of their content and the block already has a shared-core element for precisely this. Repeating the common flow under both headings would bury the only thing a visitor is there to find — which track they're in and what it costs them._ → Set aside: reproducing each track's full flow — mirrors the live page literally, but duplicates 80% twice and makes the two paths look interchangeable. **Note:** the two paths render as tabs as of decision 143; the core still sits above them.

**89. Schedule rules live in S6, not C8 or C3** — the three deadline rules (no announcement before the stated date, screening starts immediately after the deadline, a late application rolls to the next round if eligible) render as ~~notes under S6's when-to-apply rows~~ **Revised at the Wijhat application-highlight unit:** a grey-fill highlight under the when-to-apply rows (decision 177), alongside the two-date rule. _They are rules about the schedule, and S6 is the schedule-reasoning block; the live page states them between the rounds and the when-to-apply logic. C8 is a list of eligibility and application conditions and they would be lost in it._ → Set aside: C8 items — one fewer prop on S6, but separates the rules from the dates they qualify; C3 rows — closest to the live page's order, but decision 85 keeps C3 as the scan-in-five-seconds table and prose rules would end that.

**90. C4 owns the required-documents list; S5 delegates to it** — the "Documents to include" label and list move into `C4ApplySteps` as an optional `documents` prop, and `S5ApplicationPaths` passes each path's list down to the nested C4 instead of rendering its own copy. _Stand for Art needs the list on a plain C4 with no application paths, and the markup S5 was hand-rolling is exactly what C4 should render — one component per pattern. The nested C4 renders a fragment, so the delegated markup lands in the same position with the same classes and Wijhat's output is unchanged._ → Set aside: a second hand-rolled list in C4 — no change to S5, but two copies of the same list to keep in step; a shared `DocumentList` primitive — cleaner in the abstract, but the list only ever appears inside a C4. **Note:** the nested C4's steps render as numbered cards as of decision 141; the fragment contract and the documents list below them are unchanged.

**91. The two privacy notes split — non-resumable form into C4 `note`, confidentiality into C4 `highlight`** — the live page's two adjacent notes end up in different fields on the same block: "the form cannot be saved and resumed" stays C4's `note` (below the steps), ~~rendered as a bold paragraph~~ **Revised at the C4 note-highlight unit:** the same grey fill as `highlight` (decision 213), and "all information is confidential and used internally" becomes C4's `highlight` (label + text), rendered ~~at the top of C4~~ **Revised at the application-highlight-placement unit:** last in C4, after the steps, documents and `note` (decision 177), in the same grey treatment as C6. ~~data confidentiality into C6, and C6 stays ahead of the apply flow~~ **Revised at the C6 unit:** Stand for Art no longer has a page-level C6. Wijhat (visa letter) and Abbara (core-funding exclusion) keep C6. _They still answer different questions. The non-resumable form is a caution about how to prepare and sits with the steps; the confidentiality assurance is a highlight, and application highlights close their parent (decision 177) rather than opening the steps. After C1 owns the opening (decision 137), a page-first C6 made Stand for Art open on a note rather than on what the programme is — the apply block is already the first thing an applicant reads after the overview._ → Set aside: both notes in C6 — mirrors the live page's adjacency, but buries a practical instruction inside a reassurance; both as C4 `note`s — keeps the source order, but demotes the page's defining fact (audit §3) to fine print under a step list; keeping C6 first on the page — what this decision originally required, but it duplicated the highlight treatment as its own block once C1 is the opening; keeping the highlight above the steps — what this decision originally required, but two grey bands in the middle of a block interrupt the sequence.

**92. C9 carries the whole five-step selection process, ending with the signed agreement** — the block gains an optional `process` list rendered as an `<ol>`, and Stand for Art's fifth step (agreement signed between Culture Resource and the beneficiary) stays in it. _The five live steps are a sequence, not criteria, and this programme's whole contractual footprint is that one step — there is no C10 to move it to, and dropping it would leave the page ending at "we will tell you our decision". `criteria` stays for the programmes that have a criteria list; Stand for Art has none._ → Set aside: rendering the steps as `criteria` — no new prop, but a numbered process rendered as an unordered list of criteria misstates what it is; a C10 holding the signing step alone — technically the right block, but a one-item contract-terms block on a page with no installments, windows or clawback reads as a stub.
    90|
**93. C5 records take an `items` list, and the Emergency fund chip reads "Set case by case"** — each support record gains an optional bullet list under its optional `body`, and the amount chip carries a phrase where the programme has no figure. _"Up to six months" attaches to the living-expenses line only; compressed into one sentence it would read as bounding all four cost types. And the live page states plainly that there are no preset figures, so the honest chip is the words — the placeholder `amount` chip would read as an unpopulated block, and any number would be invented._ → Set aside: a single prose `body` per record — no new prop, but loses which cost the six-month limit applies to; no chip at all on Emergency fund — accurate that there's no figure, but leaves the visitor unable to tell whether the amount is missing or deliberately variable.

**94. "Individuals only" is promoted out of the FAQ into C8** — the FAQ's answer that organisations can't apply becomes a C8 eligibility item, ~~and the FAQ keeps the finer reading~~ **Revised at the Stand for Art unit:** the C8 item carries the affiliated-vs-independent clause verbatim, so the FAQ was stating the rule twice. The question stays; its answer is now "No. Organisational support sits with Culture Resource's other programmes" — the redirect, which is the only thing C8 doesn't say (decision 161). _It is an eligibility rule, and C8 is where this kit puts eligibility rules (decision 85). A visitor scanning conditions shouldn't have to open an accordion to find out the programme excludes them._ → Set aside: leaving it in C11 only — matches the live page, but hides a hard eligibility bar in an FAQ; ~~removing it from C11 once promoted — no duplication, but the live question is asked because people ask it, and the FAQ answer adds the affiliated-vs-independent nuance.~~ dropping the question along with the clause — no duplication, but the live question is asked because people ask it, and the redirect is a real answer. **The nuance argument no longer holds: C8 took the nuance with the rule.**

**95. Rolling intake gets its own states key, and `rolling` overrides the closed state** — Stand for Art uses a new `programRolling` states key with no toggles and a note naming both absences, and `ProgramConfig.rolling` makes `ProgramPage` pass `closed={false}` so a `closed` value carried in from another programme page can't take effect. _The panel was offering "Applications closed" and `Show "offered" blocks` on a page with no rounds, no deadlines and no S3, and C7's closed render names a next round that doesn't exist — a state that can't occur shouldn't be togglable, and shouldn't leak in from a sibling page._ → Set aside: leaving the `program` key in place and flagging it in the log — no code change, but the panel would keep offering a state the programme can't be in; making the closed state per-block — more granular, but the whole point is that this programme has no call status at all.

**96. The beneficiary map is not wireframed, and the directory renders at page end** — Abbara's page carries no map block; C2 carries the impact figures (87 supported · 60 active · 27 ceased · 8 rounds) and S3 stays last in the block list, published unconditionally. _Already owned by the component register: the live geo-map is dropped from the proposal on design-clash and embed-styling grounds with the stat strip carrying impact instead, and the directory is pushed to page-end from its live position above everything. The register logs a rebuild as a client-revisit option, so wireframing one now would pre-empt a call the client hasn't made._ → Set aside: laying out a map placeholder — shows the client what they'd be choosing, but a dashed box labelled "map" is the design decision, not a question; keeping the directory at page top as the live site has it — faithful to today's page, but it puts 56 organisation names ahead of what the programme is.

**97. S4 gets two cells, not four, and the leading question marks are dropped** — ~~the hero carries the live page's two above-tab headings ("Who is Abbara for", 2 bullets; "What kind of support does the programme offer", 4 bullets), and the port's four bare labels go.~~ **Revised at the Abbara S4/C3 unit:** the support cell is dropped — C5 already carries all four components with the €22,000 chip, so the cell was a duplicate. S4 keeps "Who is Abbara for" and gains Disciplines covered (twelve chips) and Where they work from the retired C3. The port's four bare labels stay gone. _The four were placeholders borrowed from Wijhat's shape; the live page has exactly two summary headings and the cell row reads fine at two. The question mark rendering at the front of both headings is the RTL/punctuation CMS artifact already flagged on Wijhat, so it is dropped rather than reproduced or silently moved to the end. The support cell's four bullets were the same four C5 records — keeping them in S4 mixed "is this me?" with "what do I get?"._ → Set aside: keeping four cells and synthesising two more from elsewhere on the page — matches Wijhat visually, but invents a summary layer the programme doesn't have; reproducing "?Who is Abbara for" — literal to the source, but wireframes lay out for rewritten copy, not for the CMS's bugs; keeping the support cell — what this decision originally required, but it restated C5.

**98. Abbara's eligibility goes in C3, and the application terms stay in C8** — ~~the seven org-level eligibility rows (who can apply, disciplines, where they work, budget band, years operating, registration, previous beneficiaries) render as C3 rows; C8 keeps the six application requirements.~~ **Revised at the Abbara S4/C3 unit:** C3 retires. Who-can-apply is dropped — it restated "Who is Abbara for". Disciplines covered and Where they work move to S4 (scope). The remaining three facts move to C8, with budget band fused to years operating as the live page has it: "A small or midsize initiative or organisation — total expenditure or income between US$50,000 and US$500,000 — with at least three years' experience in arts and culture, as an entity or in its members." Plus registration and previous-beneficiary. C8 goes from six items to nine. The C3 code is not reused. _This is the split the register's graduation log records — Abbara's criteria are structured facts (a band, a count of years, two booleans), which is what earns the strip, where Production Awards' and Wijhat's narrative eligibility did not. Decision 85 folded eligibility into C8 for those two because it was a list of conditions; here it is a table of values, and C3 stays a scan-in-five-seconds block because there are no round dates competing for it — those went to S1. The scan job is now S4's (decision 160); C3's leftover facts that are rules, not scope, go to C8 so they are not restated._ → Set aside: following decision 85 and putting all seven rows in C8 — consistent across the four programmes, but it buries the budget band and the three-year rule, which are the two facts that decide whether an organisation reads on; splitting the dates into C3 as well — the shape the other programmes have, but Abbara's dates are a sequence and belong in the timeline; keeping C3 as Abbara's scan table — what this decision originally required, but every leftover card either restated S4 or belonged in C8.

**99. S1 renders in chronological order, with a lead naming the round and its span** — the eight stages run Open call → Deadline → Results → First workshop → Agreements → Second workshop → Technical assistance → End of contracting, and the block gains a `lead` prop carrying "a round runs 24–30 months" and that the dates are the current round, Abbara 08. _The live page lists "End of contracting period: February 2028" sixth, ahead of the 2026 second workshop and the 2026–2027 technical assistance. The register defines this block as preserving order and duration — the thing a flat label-value strip loses — so a timeline that isn't in order has no reason to exist. The lead exists because the dates are one round's, and without saying so the page reads as if February 2028 were a fixed property of the programme._ → Set aside: source order — faithful to the live page, but renders a rail that goes 2026 → 2028 → 2026; the span and round note as a first stage or a C6 — no new prop, but a duration isn't a stage and a callout would outrank the dates it qualifies.

**100. S3 takes records and a label; a plain string group keeps the placeholder rows** — `groups` widens to accept either a string or `{ title, records }`, each record a name plus an optional country, and the block takes a `label` defaulting to "Past beneficiaries". Abbara passes eight round groups with 56 organisations and the label "Abbara organisations by round". _Production Awards and Wijhat keep passing plain strings and are untouched (decision 84). The label is a prop rather than a fork because Abbara's directory is not a list of *past* beneficiaries — 60 of the 87 are still active, and the live heading says so. Country renders by omission (standing constraint 3), because the live directory publishes none._ → Set aside: a separate populated directory block — clearer at a glance which programme it belongs to, but two components for one register block; hardcoding "Past beneficiaries" and putting the round framing in a hint — no new prop, but the block's own label would then contradict its content.

**101. C10 stays in its near-empty mode, carrying only the two facts the English page states** — contracts signed after the first workshop on the basis of the first-draft strategic plan, and the funding component running no more than 24 months inside a contracting period ending February 2028. _The register defines this block as scaling detailed→near-empty across the four programmes and requires it to degrade gracefully; Abbara's English Guidelines tab has no contractual section at all. Two real items are the honest render, and the absence is flagged as a suspected English content gap to check against the Arabic page — not designed around._ → Set aside: borrowing the installments, signing windows, guarantors and clawback from Production Awards and Wijhat — fills the block, but invents contract terms for a programme whose published terms we haven't seen; dropping C10 entirely — no invented content, but it hides the gap instead of surfacing it, and the two facts we do have are real.

**102. C1 takes an `items` list, and Abbara's three programme goals go in it** — the block gains an optional bullet list under the prose; the live page's "Program goals" heading and its three bullets render there rather than as a block of their own. _The register puts Abbara's intro, sector context and goals in the overview block, and the goals are three long sentences that read as prose siblings of the paragraphs above them. C8 is conditions and C5 is what the programme offers — neither is what the programme is trying to do._ → Set aside: a fourth paragraph compressing all three goals — no new prop, but three distinct aims collapsed into one sentence stop being aims; the goals as a C6 callout — visible, but the callout is already spoken for by the core-funding exclusion, and two on one page flattens both.

**103. Abbara gets a `programDirectory` states key** — a new key carrying the `closed` toggle only, with a note saying the directory is published content rather than an offered block. _The closed state is real here (Abbara 08's call closed in July 2025), but `Show "offered" blocks` would be inert: Abbara's S3 carries no `offeredOnly` flag, so the toggle would change nothing on the page. Follows decision 95 — a state that can't occur on a page shouldn't be togglable there._ → Set aside: keeping the `program` key — no new key, but the panel would offer a toggle with no effect; flagging the S3 as `offered` to make the toggle work — one key for three programmes, but it would hide 56 published organisation names behind a wireframe state.

**104. One populated record rather than 33** — PC1 and the detail route carry the Lebanon Humanitarian Fund report; PA4 samples nine of the 33 library titles. _The live library has 33 records but only one was in scope for this unit. Nine sampled rows plus the overflow line show scale without implying the sample is the set, and 33 detail routes would be identical shells for 32 records with no content brief._ → Set aside: a detail route per record — faithful to the live sitemap, but 32 of them have nothing to populate; rendering all 33 cards — accurate count, but the grid would dominate the page the way Abbara's directory would have before it was sampled by the overflow pattern.

**105. PC1 keeps unpublished metadata rows as fill bars** — ~~Author, Country, Theme and Type stay on the record as fill bars; only Language and Year carry values.~~ **Revised:** Author and Type stay as fill bars; only Language and Year carry values. Country and Theme leave (decision 207). _Standing constraint 3 would drop the empty rows, but that would hide the gap: the library filters on taxonomy the detail page does not publish. Fill bars with a hint naming the gap surface it rather than designing around it._ → Set aside: omitting the four rows — obeys the omission rule literally, but the wireframe would then look as if those fields were never part of the record; inventing tags from the library facets — fills the table, but the live page does not publish them. **Revised:** omitting Country and Theme is now the call (decision 207).

**106. One download button per published language** — the single "Open the document" button becomes one primary button per language the record publishes (English, Arabic). _The live record has two files, not one bilingual file, and a single button would hide that._ → Set aside: one button labelled "Open the document" — matches the unpopulated block, but implies a single file; a downloads list like S2 — heavier treatment than two files need.

**107. PB1's headline is an optional slot** — the Fill that stood for a headline above the intro becomes an optional `heading` prop; none of the three series pass it, because none has a headline above the intro (the H1 is the chrome band). _The spec's "headline + text" was describing the page heading plus intro; rendering a second headline fill on every series page was a leftover from the unpopulated kit._ → Set aside: keeping the Fill as the decision-84 fallback — consistent with other blocks, but it would show a headline none of the series have; requiring a heading and feeding the series title — duplicates the page-header band.

**108. PA4 carries PB2's overflow line** — nine sampled rows are followed by "… 33 items in the grid". _Without it the sample reads as the whole library._ → Set aside: rendering all 33 cards — accurate count, but the grid would dominate the page; a count in the hint only — the hint is for reviewers, the overflow is for anyone reading the page.

**109. Facet values are an optional list on the shared `Facet` type** — `FilterBar` renders `facet.values[i]` where supplied, `"value"` otherwise. Library facets carry the real lists; Network and News are untouched. _One primitive; optional content (decision 84)._ → Set aside: a Publications-local filter bar — would leave MN3/N1 alone without touching the type, but forks the primitive the library already shares; required values on every facet — would force Network and News to invent placeholder names this unit isn't populating. ~~**Note:** Network's five facets carry their real lists as of the Mawred Network unit; News is still the only kit rendering `"value"`.~~ **Revised at the News unit:** News's one facet now carries its real values too, so no kit renders `"value"` and the fallback is dead code kept only for the decision-84 contract.

**110. Four sampled records, two per entity tab, with one populated detail page** — ~~the directory carries Hiba Taim and Nour Shantout on Individuals & Groups, Al Kasaba Theatre and Cinematheque and Editions Motifs on Organizations & Initiatives; Al Kasaba is the record the detail route renders.~~ **Revised at the directory-sample unit:** twelve rows per entity tab; the original four stay at the top of each tab so the filtered-state chips still land on a real row; Al Kasaba is still the one populated detail. _Extends decision 104. Two per tab was the smallest sample that showed both tabs populated and one-vs-two engagements; it read as an empty listing once the page was in review. Al Kasaba takes the detail page because the record name lives in the route registry and cannot swap per state, and its two engagements — one of them without a project description — put both the repetition and the omission in the default render._ → Set aside: an individual on the detail page — matches the spec's field list most closely, but a single-engagement record would have hidden the repeating group the live data actually has; a second detail route for an individual — shows both record shapes, but doubles a route for a page-type whose two shapes differ only in which optional fields are present.

**111. MN6 renders a repeating engagement group, and Round generalises to round/track** — the record's flat metadata table splits: Country and Discipline stay record-level, and Involvement · Programme · Round · Year · project title · project description become a group repeated once per engagement, with the round slot holding either a round number or a track name. _Spec §B5's single field set came from the one-engagement Tarek Elazhary record; three of the four sampled records break it — Al Kasaba and Editions Motifs are each Participant **and** Grantee on the same All Around Culture project, distinguished only by track (Regional Workshop vs Alliance Support). A flat table would have to either drop an engagement or merge two involvements into one row that belongs to neither._ → Set aside: one row per field with comma-joined values (`Involvement: Participant, Grantee`) — no structural change, but it detaches each involvement from the track and project it belongs to; one directory row per engagement rather than per entity — mirrors how the data is stored, but the directory would list Al Kasaba twice and the record page is plainly about the entity.

**112. Unpublished totals stay unpublished — the count row shows only what it shows** — the port's invented `318` / `94` entity-tab counts and its page size of 40 are removed; the tabs carry no count, MN4 reads "Showing N records in \<tab\>" with no denominator, MN2's record rail keeps `00`, and the load-more button drops its number. _Nothing on the live site publishes a total: no count row, no pagination, and the listing's several-hundred rows are JS-rendered so even the markup can't be counted. A denominator is the one number this block exists to give, so inventing it would be the most quotable false fact on the page; naming it as a gap puts it where Mawred can supply it._ → Set aside: keeping the invented figures and flagging them in the log — the page reads complete, but a wireframe that gets signed off carries the numbers with it; a "several hundred" phrase in place of the count — honest about the order of magnitude, but it's our estimate, not the client's data.

**113. MN1's headline Fill retires and the responsible-data paragraph is the intro** — the Fill that stood for a headline becomes an unused `heading` slot, and the block's prose is what the page tail currently carries. _Follows decision 107: the H1 is the chrome band, so a headline fill above the intro was a leftover from the unpopulated kit. The retired "under development" notice takes the `resources@mawred.org` data-access invitation with it (spec decision 2), which is a client call rather than ours — so it is named in the hint instead of being either kept or silently dropped._ → Set aside: keeping the data-access ask as a second contact line — it may well be permanent, but wireframing it in would decide a question the spec explicitly retired; dropping it without comment — matches the spec literally, but loses the only surviving trace of an invitation Mawred may want back.

**114. The `record` states key is emptied and the sparse toggle retired** — the key carries no toggles and a note saying why; `NetworkRecordPage` drops its client boundary and `sparse` leaves `WireframeState` altogether. _Follows decisions 95 and 103: a state that a page can't be in, or no longer needs, shouldn't be togglable. The populated record shows field omission in its default render — an organisation with no descriptor, an engagement with a project title and no description — so the toggle would have had to strip real content to demonstrate something already visible._ → Set aside: keeping the toggle and hiding the body and descriptions behind it — preserves the panel entry, but a state whose only job is to remove populated content teaches nothing; keeping `sparse` in the state bag unused — smaller diff, but dead state read by nothing.

**115. Three sampled posts, two of them with a detail page, and the sample drives the tabs** — the feed carries the Made With Your Magic exhibition opening (27 July 2026, News & Events), the second-round Wijhat grantees (20 July 2026, Announcements) and the Production Awards 2026 results (6 May 2026, Announcements); the first and last get detail routes and the Wijhat post is a landing card whose link borrows the announcement route. ~~_Extends decisions 104 and 110. Three posts is the smallest sample where the category tabs do real work — All 3, Announcements 2, News & Events 1 — so the tab row can be verified rather than asserted, and where a programme chip narrows to one real row._~~ **Revised at the news-filters unit:** three posts is the smallest sample that shows both category values on the cards and that a programme chip narrows to one real row. The tab row is gone (decision 190). Two of the three earn detail pages because they are the two post kinds the spec's decision 3 splits the body shape on; a third detail page would have repeated one of them. _Extends decisions 104 and 110._ → Set aside: nine posts to fill the page-one grid — the feed would look complete, but six of them would be titles with no date, category or programme we had sourced; a detail page for the Wijhat post too — a third real route, but its content is the announcement shape again, one programme along.

**116. Invented counts go and the programme facet gets its real values** — ~~`NEWS_CATEGORIES` drops the `231 / 128 / 103` tab counts the port carried, and~~ `NEWS_PROGRAMME_FACET` carries Production Awards · Wijhat · Stand for Art · Abbara instead of four `"value"` placeholders. **Revised at the news-filters unit:** `NEWS_CATEGORIES` is gone; `NEWS_CATEGORY_FACET` carries Announcements · News & Events, with no All and no counts. _Follows decision 112. Nothing on the live site publishes a post total, and the Announcements / News & Events taxonomy does not exist yet, so its per-category counts cannot exist either — a count that can't be sourced is the most quotable false fact on the page. The four programmes, by contrast, are the one facet list News can state with certainty, which is why decision 109's closing note is revised in place: no kit renders `"value"` any more._ → Set aside: keeping the counts and flagging them in the log — the tabs read complete, but a signed-off wireframe carries its numbers with it; dropping the programme facet until the tagging model is confirmed — honest about the client call, but the facet is the block's only proof that a programme cut is possible.

**117. N14 is one block for grantees and for jury** — a single roster block takes a label and a list of `{ name, country, role, project?, description? }`; a jury entry supplies the first three and the project rows render by omission. _The two rosters are the same treatment with a different field count, and standing constraint 3 already governs the difference — blocks are defined by treatment, not by intent (the component register's own rule). Two blocks would have to be kept in step through five disciplines._ → Set aside: separate grantee and jury blocks — clearer at a glance which is which, but two components for one grid, and the jury block would be the grantee block minus two rows; one block per discipline holding both rosters and the quotes — matches the live page's heading structure, but a block that contains three lists isn't a block.

**118. N15 is its own block, attached to the roster above it rather than to the post** — quotes render as one or more attributed blockquotes at 3px, placed immediately after the jury roster they come from. _Decision 57's callout weight, and the placement is what makes the quotes legible: each is a member of that discipline's jury speaking about that discipline's submissions, so a single quotes block at the post tail would detach every quote from its jury. Cinema publishes two, Literature three, and one discipline publishes none — a per-post block couldn't express that._ → Set aside: quotes as an optional field on N14 — one fewer block, but a jury card grid and a stack of long pull-quotes are two treatments; a single quotes block at the end of the post — matches the share-bar-adjacent position quotes often take, but it would read as the post's own voice rather than the jury's.
    10|
**119. Two detail routes, one per post kind, each with its own states key** — `/news/post` is the announcement (rosters, quotes, no key-detail line) on the `newsdetail` key; the new `/news/editorial-post` is the event post (~~prose plus one key detail~~ **Revised at the editorial-column unit:** two-column prose, no key-detail line — decisions 199, 200) on a new `newseditorial` key carrying `byline` only. _Spec decision 3 makes the body shape the difference between the two kinds, and a single route can only be one of them — the port's one route was standing for both by toggling. Separate keys follow decisions 95/103/114: the event post has no roster, no schedule and no CTA, so it should not be offered a toggle for them, and it is not the oldest post in the archive._ → Set aside: one route with the two shapes behind a toggle — what the port did, and it keeps the route count down, but the page-header band carries a real post title now and a title can't toggle; a third route for an event post with a multi-date programme — would light up N10 and N11, but no such post was in scope and inventing a forum's schedule is inventing content.

**120. Cinema in full, Literature roster-only, three disciplines to an overflow line** — the announcement publishes Cinema's five grantees, three jurors and two quotes; Literature's six grantees and three jurors with no quotes; then "… Music, performing arts and visual arts follow in the same shape, each with six grantees and a three-member jury — 29 grantees across five disciplines". _Extends decisions 104/108/110. One discipline in full shows the shape; a second shows it repeats, and shows that the quotes are per-discipline rather than per-post, which one discipline alone can't. All five would be 29 grantee cards and fifteen juror cards on one page — the length is the fact, and the overflow line states it in a line._ → Set aside: all five disciplines — accurate, and the real page is that long, but the wireframe would be reviewed as a scrolling test rather than as a structure; Cinema alone — half the page length, but quotes attached to the only roster present read as the post's quotes.

**121. N6 takes a caption, and the image credit moves into it** — the featured-image block gains an optional caption slot; the announcement post's credit line renders there and the event post, which publishes no credit, renders none. _The live posts print the credit as a trailing italic line at the very end of the body, a screen and a half from the image it describes — the caption is the only place it means anything. The slot is optional because one of the two sampled posts has nothing to put in it._ → Set aside: the credit as a last body paragraph — faithful to the source, but it reads as part of the article; a required caption with a placeholder — consistent with nothing, since decision 84 makes content props optional.

**122. The `structured` toggle is retired; ~~`edge` stays~~ `edge` retires with N5** — `newsdetail` carries ~~`byline` and `edge` only~~ **Revised at the N5/N8 retirement:** `byline` only (decision 200), and `structured` leaves `WireframeState` altogether. ~~_Follows decision 114 exactly: the rosters and quotes are this post's published content, so a toggle over them would exist only to remove populated content. `edge` survives because N5 is the one block on the page still standing for the template rather than for the post — a post's neighbours come from the whole 26-page archive, so the archive-boundary render can't be shown by populating anything._~~ **Revised at the N5/N8 retirement:** `edge` leaves with N5 — a toggle whose only consumer is gone. _Follows decision 114 exactly: the rosters and quotes are this post's published content, so a toggle over them would exist only to remove populated content._ → Set aside: keeping `structured` over the rosters — preserves the panel entry, but the default render of a results post would then be a results post with no results; ~~retiring `edge` too, on the grounds that this post has 200 older posts — consistent, but it would drop a real template branch that nothing else can show.~~ retiring `edge` is now the decision.
    20|
**123. N5's neighbour titles stay placeholder** — ~~the previous and next cards keep "Title of the previous post", with the hint saying why.~~ **Revised at the N5/N8 retirement:** N5 is gone (decision 200); the placeholder question is moot. _Prev/next were archive-adjacent, and the three-post sample couldn't supply them._ → Set aside: cross-linking the two populated posts as each other's neighbours — every link would resolve, but the Wijhat post sits between them by date; keeping the block unused like N10/N11 — those wait for a post shape that is not sampled; prev/next has no remaining job.

**124. Zero results is computed from the list, not read from the panel** — `NewsLandingPage` derives `noResults` from the filtered post count and passes it to N2, N3 and the pager, instead of passing the `empty` toggle through. ~~_The toggle and the truth had come apart: opening the News & Events tab with the programme chip active yields no posts, and the old wiring rendered an empty grid with an archive overflow line under it and a pager below._~~ **Revised at the news-filters unit:** that tab-plus-chip combination died with the tabs (decision 190). Empty is the panel toggle, still computed from the list. _The empty state is a property of the result set, not a state a reviewer selects._ → Set aside: adding a second toggle for the tab-plus-chip case — the original alternative, now moot; leaving it and noting the combination as out of scope — it was reachable in two clicks on the page as built.

**125. Single-consumer About blocks take content directly, with no placeholder fallback** — AB1–AB5 take required content props (`text`, `points`, `values`, `lead` + `entries`); none keeps a fill-bar render for the no-content case. _Decision 84's reason doesn't reach them. It exists because population runs one page-type per unit over blocks several page-types share, so a required prop would force every other page-type to be populated in the same session. AB1–AB5 have exactly one consumer each — `WhoWeArePage` — so a fallback branch would be unreachable code kept for a caller that can never exist._ → Set aside: optional props for consistency with the other 40-odd populated blocks — one rule across the app, but it would leave five dead branches and let a future caller silently render a page of fill bars instead of failing to compile; deleting the fallback only from the three simplest blocks — smaller diff, but a rule that holds for two of five blocks isn't a rule.

**126. `Timeline` renders `text` if present; else Fill only when `fill` is passed** — ~~a `TimelineItem` with `text` renders it as a paragraph; one without keeps `<Fill>`.~~ **Revised:** date-only S1 stages omit the body rather than looking like missing copy. ~~AB5's 2018–2026 gap row still passes `fill: 72`, so that Fill stays.~~ **Revised at the About polish unit:** AB5 no longer passes `fill`; the gap row is labelled only (decision 128 revised). _The shared primitive has two consumers with genuinely different content shapes: AB5's history entries each carry a paragraph (the gap row is the exception), and S1 stages are label-and-date only because the live timelines publish no stage prose. Fill is now opt-in via `fill`, not the no-text default — a date-only rail is complete, not unpopulated._ → Set aside: a required `text` with S1 passing empty strings — one code path, but it would put a blank paragraph where a missing description used to be a fill bar; an About-local timeline — no shared prop to widen, but decision 69 extracted this rail precisely to avoid two copies.

**127. Eleven dated entries; the undated passage goes in the lead and the closing paragraph is dropped** — AB5's lead carries two paragraphs, the second naming the joint initiatives (Tunisia Balad El Fann, Tandem Shaml, Redzone Festival); the live history's closing "stories still to be told" paragraph is not rendered. _Spec §3.1 provides for exactly this: long-form nuance that doesn't fit a timeline row stays as a short intro above it. The joint initiatives are ongoing partnerships with no year to sit on, so any row they were given would state a date the source doesn't. The closing paragraph is voice rather than content, and copy is being rewritten — reproducing a rhetorical flourish would be migrating copy._ → Set aside: rows dated by best guess from the partners' own timelines — fills the rail, but invents dates on a page whose whole subject is dates; the closing paragraph as a twelfth entry or a trailing note — faithful to the source, but it would read as a milestone; dropping the joint initiatives entirely — the rail stays purely chronological, but it loses a third of what the organisation currently says it does.

**128. The 2018–2026 span is a labelled gap row, not an omission and not synthesised** — ~~the rail's last row is labelled `2018–2026` with a `milestone` and no `text`, so it renders a fill bar and terminates the rail.~~ **Revised at the About polish unit:** the last row stays labelled `2018–2026` with milestone “Not covered by the published history” and no `text`; AB5 no longer passes `fill`, so Timeline omits the body rather than painting a bar (decision 150). The nine-year gap stays named; the bar that read as leftover chrome is dropped. _Follows decision 105: a gap that the wireframe designs around stops being visible, and this one is nine years wide. Ending the rail at 2017 would read as a complete history that happens to be old; the labelled row reads as content Mawred owes the page. Synthesising the years from the news archive was available and not taken — a history is what an organisation says about itself, and the news archive is not that statement._ → Set aside: ending at 2017 with the gap in the hint only — the hint is for reviewers, and this gap is the reader's problem too; writing the nine years from the archive and the programme pages — the page would look finished, but the wireframe would be authoring the client's institutional history; a dashed row rather than a labelled one — dashed marks optional/conditional per standing constraint 1, and these nine years are neither; keeping the fill bar — named the gap, but the grey rect under the rail read as leftover chrome once the row already says the years are unwritten.

**129. `Btn` takes an optional `href`, rendering a `Link` with the same classes** — given an `href`, `Btn` renders a Next.js `Link`; without one it stays a `<button>`, so every existing call site is unchanged. ~~Home's wayfinding, "Read more", "All news" and "Who we are" pass the hrefs already declared in config.~~ **Revised at the HM7-out unit:** "All publications" retires with HM7. **Revised at the HM1 inline-wayfinding unit:** Home's wayfinding no longer uses `Btn` (decision 186). "Read more" and "All news" still pass href. _Standing constraint 1 requires clickable end to end, and the destinations were already named; a button that goes nowhere was a leftover from the port. One optional prop rather than a second component, matching `Card`._ → Set aside: a separate `BtnLink` — clearer element type in the name, but two components for the same chrome; wrapping Home's buttons in `Link` at the call site — no primitive change, but the classes would have to be kept in step by hand.

**130. `nocalls` stays, as a template branch rather than a reachable state** — ~~the toggle remains on the `home` key and HM3 still renders the spec §3C empty; the hint and the panel note say it is a template branch.~~ **Revised at the HM3-into-HM4 unit:** `nocalls` retires. Removed from `lib/wireframe-state.tsx`, `components/StatePanel.tsx` and `lib/pages/states.ts`. ~~With status on the HM4 card, "nothing open" is four cards with no chips — spec §3C's graceful-empty is dissolved rather than unwired.~~ **Caught up at the HM4 open-opportunities unit:** closed programmes are not listed (decision 40 revised), so the four-cards-no-chips empty is gone. Empty stays unreachable — Stand for Art is open all year — and the toggle stays retired. The set-aside is now the decision. _Stand for Art accepts applications all year with no rounds and no deadlines, so at least one call is always open — a state the page cannot be in. Keeping a toggle for an unreachable empty was already the weaker answer._ → Set aside: ~~retiring the toggle — consistent with 95, but it would leave spec §3C unwired and hide the empty from review;~~ inventing a closed Stand for Art to make the state reachable — would contradict the programme's own page. **The first set-aside is now the decision.**

**131. The Mawred-wide grant total stays `00`** — ~~HM2's first rail is `grants awarded since 2003` with no value.~~ **Revised at the HM2 figures unit:** the rail is `~1,200` grants awarded since 2003, leading the strip. It is a placeholder, not a published total. The years-since-2003 rail is gone — "23 years since 2003" restates the founding year the hero already names. Rails are `~1,200` grants · `55` countries · `38` programmes & initiatives. _Follows 112 and 128. Spec §3B's "~400 grants since 2004" is Production Awards alone, and 87 organisations is Abbara's; no Mawred-wide grant total is published anywhere. A blank `00` on the impact strip read as unfinished, not as a documented gap; a round placeholder with a tilde still refuses to borrow a programme-scoped number, and still gives the strip a figure to count. Years-since-founding is arithmetic, not impact._ → Set aside: using ~400 and flagging it — still programme-scoped; dropping the grants rail — would hide that Home wants an org-wide total; keeping `00` last — the version this revision replaces; keeping 23 years — the other rail this unit drops.

**132. `HOME_PROGRAMMES` is the single source; `HOME_OPEN_CALLS` derives from it** — ~~each programme is one `{ name, descriptor, href, status? }` record; HM4 renders all four; HM3 filters to those with a `status`.~~ ~~**Revised at the HM3-into-HM4 unit:** `HOME_OPEN_CALLS` and the `OpenCall` type are deleted. `HOME_PROGRAMMES` is the only surface — HM4 renders all four, with `status` as a `Badge` when present. No second list, no filter. _A programme's one-liner would otherwise be written twice and drift. Status is still optional, so Abbara (call closed July 2025) appears in the overview without a chip, on the same grid as the three that are open._~~ **Revised at the HM4 open-opportunities unit:** `HOME_OPPORTUNITIES` is the only surface — three `{ name, descriptor, href, status }` records, `status` required. Closed programmes are not listed. `ProgrammeOverview` and `HOME_PROGRAMMES` are gone; Abbara is not in the list. No second list, no filter, still not a live query into the kit. _A closed programme on the same grid as the open ones made the list an overview again. Curating to what a visitor can apply to now keeps one surface and drops the restatement._ → Set aside: two independent lists — simpler types, but the descriptors would fork the first time either strip is edited; ~~HM3 reading the programme kit live — the spec keeps Home a curated block, not a query into the kit;~~ keeping `HOME_PROGRAMMES` with Abbara unused — a closed record with no consumer.

**133. The 20th stays a state on HM5, with the campaign content a gap** — ~~default feature is Made With Your Magic, Beirut; the `twentieth` toggle still swaps the slot to a campaign eyebrow and title over fill bars, linking to the `/mawreds-20th` stub.~~ **Revised at the HM5-owns-the-20th unit:** the rail item is gone (decision 44 revised), so this slot is the 20th's only home. The stub message names the content gap, not the nav question. **Revised at the Home spotlight unit:** the occupant is Mawred's 20th via `HOME_SPOTLIGHT` (title, href `/mawreds-20th`). Campaign body stays fill bars — still a **gap**, not invented copy. The `twentieth` toggle is gone; swapping later is a config change, not a state. _Decision 44 already made this one slot rather than a block of its own. No campaign copy is sourced and the destination is still a stub, so filling the body would invent a campaign. A review toggle once the occupant is chosen just restated a config value._ → Set aside: keeping `twentieth` as a review toggle — the version this revision replaces; writing campaign copy from the news archive — fills the slot, but the wireframe would be authoring the client's anniversary.

## Client view

**134. Codes-off is review chrome, not codes only** — `Block.label` is the reviewer name and hides with the code badge; `Block.heading` is the client-visible section title and shows only when codes are off, so the two never stack. The `codes` toggle — panel copy "Show review chrome", key unchanged — ~~default unchanged~~ **Revised at the codes-default unit:** default is off (decision 156) — drives `body.codes-off`, which hides badges, the label row, block wrapper borders and padding, and Hint notes. Inner primitive borders, C6's grey fill, ~~N8/N15's 3px~~ **Revised at the N5/N8 retirement:** N15's 3px (decision 200), chrome, fill placeholders and their captions stay. _Standing constraint [10] already called the off state a clean client view, but the toggle only hid the `.code` badge, leaving technical labels ("Cinematic hero — image sequence + positioning line") reading as if they were headings._ → Set aside: a second client-view toggle — two switches for one presentation; promoting every `label` to an `h2` — would print reviewer glosses on the client page; leaving labels visible with codes off — the client view would still be annotated.

**135. Headings are the default; review chrome hides them** — `.block-heading` is no longer default-hidden. `body:not(.codes-off) .block-heading { display: none }` keeps review view as code + uppercase label only; ~~client view shows the `h2` at `text-lg font-bold`~~ ~~**Revised at the programme-sections unit:** programme section h2s (Application, Past, FAQs, Contact) use `.block-heading` at `text-2xl font-bold`; `Block` gains `headingAs?: 'h2' | 'h3'` (default `'h2'` so About / Home / News / Publications stay as they are); programme blocks that still pass `heading` pass `headingAs="h3"` and stay `text-lg`.~~ **Revised at the site-wide h2 unit:** ~~every client-visible h2 uses `.block-heading` at `text-2xl font-bold` — `Block.heading` (Home, About, News, Publications, Network) and~~ **Revised at the Home heading unit:** programme section titles and Home HM4 / HM5 use `SectionHeading` at `text-4xl`; other `Block.heading` h2s stay on Block's default (~~HM6, ~~About, News, Publications, Network). **Revised at the HM6 heading unit:** HM6 joins them — Latest news is `SectionHeading` too. The programme section titles (~~Application, Past, FAQs, Contact~~ ~~**Revised at the section-rules unit:** About, Application, Past, FAQs — Contact is no longer a section title, decision 173~~ **Revised at the heading/spacing unit:** Application, Past, FAQs — About's h2 is `sr-only` again, decision 172). `Block` still takes `headingAs?: 'h2' | 'h3'`; programme blocks that pass `heading` keep `headingAs="h3"` at `text-lg`. ~~Overview's h2 is `sr-only` and never uses `.block-heading`.~~ ~~**Revised at the section-rules unit:** About's h2 is a visible `.block-heading` like the others (decision 172).~~ **Revised at the heading/spacing unit:** About's h2 is `sr-only` again and never uses `.block-heading` (decision 172). C11, C12 and S3 drop `heading` so they do not duplicate the section title (decision 153). ~~`.block-frame` gets `margin-block: 2rem` so adjacent lists don't collapse once padding is zeroed.~~ **Revised at the block-gap unit:** `margin-block: 3rem` (decision 175). C5 takes an optional `heading` (default "Forms of support"); Production Awards passes "Available grants". `SectionHeading` is the shared helper so ProgramPage, HM4, HM5 and HM6 cannot drift; HM5's title sits in the copy column, not on `Block.heading`. _Decision 134 already split label from heading, but the CSS hid the heading by default and the `body.codes-off` override never won, so client view dropped the title row. "Forms of support" is the wrong title on a page of five grant amounts._ → Set aside: showing headings in both views — would print "C4 HOW TO APPLY — STEPS" on an h2; renaming C5 globally to "Available grants" — wrong on Stand for Art and Abbara, which really are forms of support; adding headings to C2, C6, C7 or S4 — those blocks already title themselves (figures, callout, button, cells). ~~adding headings to C1 — it titled itself via an inner h3~~ **Revised at the programmes-opening unit:** C1 no longer titles itself via an inner h3; the page H1 is the heading (decisions 137–138).

**136. Unordered lists restore disc markers** — content `<ul>`s take `list-disc` (the same restore ordered lists already had with `list-decimal`), and a `@layer base` rule sets `list-style: disc` on `ul` so a future list isn't unstyled if the class is missed. Layout lists keep `list-none` (Timeline, AB10, AB13); utilities still win. _Tailwind Preflight zeros `list-style` on `ul`/`ol`, which is why the eight content lists were indented with `pl-5` and had nothing to indent for. The class is what actually shows — Preflight's `list-style: none` shorthand is not overridden by `list-style-type` alone._ → Set aside: the base rule alone — one place, but the two `<ol>`s already proved the utility class is the restore that renders; unlayered CSS — would override `list-none` and put discs on the timeline. **Note:** C4's steps left the ordered list for numbered cards (decision 141); C9's process list is the remaining `<ol>`.

## Grants & Opportunities — presentation

**137. Programme opening is the page-header band then C1** — ~~the four programme routes set `suppressPageHead`; C1 owns the crumb and the page H1 (`text-4xl`, same as the band); `ProgramPage` puts C7 in a `flex` row with `items-start justify-between` on the H1 line, far right; crumb sits above that row so the button lines up with the title;~~ **Revised at the page-level band unit:** the four programmes mount `PageHeaderBand` then C1 (overview prose + optional `items` only). ~~C7 is unmounted from the opening and parked; the component and `dest` stay on `ProgramConfig` for a later apply / guidelines placement (decision 152).~~ **Revised at the programme-sections unit:** C7 remounts in Application (decision 153); it is no longer parked. ~~first in Application, stacked under the h2~~ ~~**Revised at the C7 heading-row unit:** C7 sits on the Application heading row, right-aligned, and stays sticky after that row scrolls away (decision 159).~~ **Revised at the band-CTA unit:** C7 sits in the sticky band, right of the H1 and section nav (decision 159) — beside the title without living inside C1. C2 is a full-width `wide` Figs row under the opening. C7 stays its own block — not nested inside C1's `Block`. _Every programme starts the same way: page title + description. The band is the H1; C1 no longer owns it. The original reason to give C1 the title — so C7 could sit on that row — is now done by the band._ Revises standing constraint 2 / decision 33. → Set aside: ~~keeping the band and dropping C1's H1 — would leave the apply button in a sidebar with no title to sit beside;~~ nesting C7 inside C1's box — one opening, but two codes in one frame. **The first set-aside is now the decision for the H1; C7 sits beside that H1, not inside C1.**

**138. C1's inner heading is gone, not an unused slot** — ~~the `title` prop stays optional and still renders as an h3 when passed; none of the four programmes pass it, and a missing title is omission rather than a Fill.~~ **Revised at the page-level band unit:** the `title` prop is dropped. C1 is overview prose + optional `items` only. The H1 is `PageHeaderBand` (decision 152). _The live "What is the … Program?" lines were leftover from when the band carried the H1 (decision 135's note); keeping an unused slot after the band returned would invite a second heading._ → Set aside: keeping the inner title under the H1 — two headings saying the same thing at different sizes; a Fill when the inner title is absent — would show a missing heading on a block that already has one.

**139. S4 sits after the opening, not before it** — when `config.hero` is present (Wijhat, Abbara), S4 renders after C1 + C2, still in Overview (decision 153). ~~C7 sat between them on the opening; it is parked off the opening (decision 152).~~ **Revised at the programme-sections unit:** C7 remounts in Application, not between C1 and S4. **Revised at the band-CTA unit:** C7 sits in the sticky band (decision 159), not in Application. **Revised at the Abbara S4/C3 unit:** S4 is the scope summary (who / what-for / where) wherever it appears; C5 is what you get. Abbara's support cell left S4 — C5 already had the four components. Wijhat already made this split (decision 86 overturned). ~~**Revised at the Production Awards S4 unit:** Production Awards now has S4 too.~~ **Rolled back:** Production Awards has no S4 — eligibility already lives in C1 and C8 (decision 158). ~~Stand for Art still has none.~~ **Revised at the Stand for Art unit:** Stand for Art gets none either, for the same reason (decision 161), so S4 is Wijhat and Abbara only and the who / what-for / where split applies where S4 exists rather than to all four programmes. _The summary cells qualify the programme; they are not the page title. Putting them first made Wijhat and Abbara start on a different shape from Production Awards and Stand for Art._ → Set aside: S4 first, then C1 — what the port had, but it led with cells before the visitor knows the programme's name or what it is.

**140. C3 is a wrapping card grid, not full-width KV rows** — `C3AtAGlance` renders as cards (`border border-black p-2.5`), not numbered and not a stacked table. Label in `text-xs text-neutral-500`, value as body; ~~Production Awards' remaining three facts (eligibility, disciplines, grant period) and Abbara's seven share the wrapping grid~~ **Revised:** Production Awards drops those three leftover cards (decision 158); ~~Abbara's seven stay, and long values wrap inside the card.~~ ~~Rounds: one card per round — title, deadline, results — in a 3-up grid, which is how Wijhat's three rounds render.~~ **Revised:** sequenced dates left C3 for S1 (decision 145); C3 is facts only. **Revised at the Abbara S4/C3 unit:** C3 retires. `C3AtAGlance`, the `C3flat` config arm, and the ProgramPage cases are deleted. The C3 code is not reused. The card chrome lives on as S4's claim/sentence items (decision 160 revised). _C3 is the scan-in-five-seconds block (decisions 85, 98); full-width KV rows made six or seven facts a list to scroll, and grouped KV rows made Wijhat's rounds look like a second C8. The scan job is now S4's._ → Set aside: numbered cards — the facts aren't a sequence; KV for flat and cards only for rounds — two treatments for one block, which is the split decision 61 already closed; full-width rows for rounds because three items fit — the open question this unit resolved; keeping C3 for Abbara — the last consumer, but every leftover card restated S4 or belonged in C8.

**141. C4 steps are numbered cards** — `C4ApplySteps` drops the `<ol>` for the same card chrome C3 used (`border border-black p-2.5`). Each card carries an index, the step label, and the detail; a missing detail still renders a Fill (decision 84). ~~Sequence reads left-to-right, then wraps.~~ **Revised at the C4 full-width unit:** cards stack full-width; index and label sit on one `text-xl font-bold` line (decision 170). Documents and note stay below the cards. ~~and the repeat apply button~~ **Revised at the C7 heading-row unit:** C4 no longer repeats Apply now — C7 is the only apply control (decision 159). Nested use (`nested`) still returns a fragment, so S5's delegated markup does not double-wrap. _The steps are a sequence, which is why they are numbered where C3's facts were not (decision 140)._ → Set aside: keeping the `<ol>` and restyling its items as cards — would preserve list semantics, but the numbers would fight the card chrome; a numbered `Cells` row — already extracted (decision 80), but Cells share borders and don't wrap as a gap grid; a wrapping `minmax(210px)` row — what this decision originally required, but three or four short actions became a scan of narrow columns.

**142. C11 FAQs use a plain accordion variant** — `Accordion` gains a `plain` prop used only by `C11Faqs`. Items have no borders; the question is `text-base font-bold`; rows space with `py-3` / `mb-3`. Default Accordion stays boxed — S3 beneficiary groups are unchanged. ~~AB7 bios are unchanged.~~ **Revised at the About polish unit:** AB7 bios left Accordion for a name-row disclosure (decision 151). _FAQs are a list of questions, not boxed records; the default chrome made four or six Q&As look like another S3._ → Set aside: changing the default Accordion — would restyle S3, which still needs the box; a C11-local expander — a second toggle component doing the same job, against decision 71.

**143. S5's two tracks are tabs, with the shared core above** — `S5ApplicationPaths` is a client block that owns its tab index (decision 64); one tab per path (Individual / Group); the nested C4 cards render in the active panel. The shared core stays above the tab row. The "stacked labelled sections, not tabs (decision 6)" hint is dropped. **Decision 6 revised in place:** S5 is two short tracks a visitor chooses between, not a long option list — stacking both made the second path look like a continuation of the first. Standing constraint 3's long-list rule (decision 19) is unchanged; this is the exception it already implied. **Note:** the active panel is now `Tabs`' optional `children` (decision 146). _Wijhat is the only consumer; the two paths exist so the visitor can pick one, which is what tabs are for._ → Set aside: keeping stacked sections — what the port had, and what decision 6 required, but two short tracks don't meet the long-list test decision 19 later set; lifting the tab index into `ProgramPage` — matches Network/News (decisions 66/78), but only S5 reads the value.

**144. C6 highlight is a grey fill, not a 3px border** — `C6HighlightedNote` drops `Block.callout`. The highlight is an inner `bg-neutral-200` padded band with no border, so the fill survives `codes-off` the same way N15 keeps its inner 3px (the Block wrapper is a `block-frame` again). `Block.callout` is unused and removed. C4's optional `highlight` reuses the same inner treatment. ~~N8, AB2 and N15 stay 3px.~~ ~~**Revised at the About polish unit:** AB2 uses the same grey fill (decision 148); N8 and N15 stay 3px.~~ **Revised at the Vision block unit:** AB2 no longer uses the fill (decision 148); ~~N8 and N15 stay 3px.~~ **Revised at the N5/N8 retirement:** N15 stays 3px; N8 is gone (decision 200). **Decision 57 revised in place for C6 only.** _The 3px on C6 was the only thing that distinguished it from a prose block in review; in client view it became a second border weight on a page of 1px cards. Grey fill is the highlight; weight stays for N15, where the band is a quotation._ → Set aside: keeping 3px on C6 — consistent with the original 57, but three programmes would then have two emphasis signals (cards at 1px, callout at 3px) for notes that aren't quotes; a Block-level fill that skips `.block-frame` — keeps padding in client view, but would leave C6 without the frame-zeroing the rest of the page gets.

**145. Sequenced programme dates live in S1, not C3** — Wijhat's three rounds become six chronological S1 stages (deadline then results, labels carrying the round) with a short lead that the intake is year-round, the calendar repeats, and dates carry no year. Production Awards' Open call / Deadline / Results pull out of C3 into an S1. ~~above the remaining three fact cards~~ **Revised:** those leftover cards left the page (decision 158). C3 drops its rounds mode; `C3AtAGlance` is flat-only. Wijhat has no C3 after this; Production Awards has none after decision 158. **Revised at the Abbara S4/C3 unit:** C3 retires; there is no remaining consumer and the component is deleted. ~~S6 stays immediately after Wijhat's S1.~~ **Revised at the programme-sections unit:** S6 still follows S1 in config order; on the page the Application h2 sits between them (decision 153); C7 is in the sticky band (decision 159). S1 is Overview; S6 is Application. **Decisions 61, 126 and 140 revised in place.** _S1 is the sequence block (label + date on a rail); C3 is facts. Round calendars and open/deadline/results are sequences that were parked in C3._ → Set aside: keeping rounds in C3 as cards — what decision 140 just did, but three round cards still aren't a sequence; a C3 date strip plus S1 — two date treatments on one page.

**146. Tabs are a 1px light strip, with an optional connected panel** — `Tabs` drops overlap (`-mr-px`) and the 2px active weight. The row sits on a 1px `border-neutral-200` bottom rule. Active: `font-bold`, 1px light border on top and sides, white bottom covering the rule so it joins what follows. Inactive: `text-neutral-500`, no box. Optional `children` render in a 1px `border-neutral-200` box with no top border, continuing from the tab row. S5 passes the nested C4 as children so Individual/Group is tabs + one panel. N1 and MN5 pass nothing — they keep a tab row over filters/listing. **Decision 57 revised in place for active tabs only** (~~header rule and primary buttons stay 2px~~ **Revised at the website-shell unit:** header/footer 2px rules are gone; 2px stays on primary buttons only — decision 154). _The previous treatment was a button group, not tabs. `neutral-200` as a 1px rule is the permitted fill colour used as chrome, so the strip doesn't read as the same black 1px as cards and primary buttons._ → Set aside: keeping 2px active tabs — consistent with original 57, but the overlap plus weight made a button row; boxing N1/MN5 the same way as S5 — one treatment, but News and Network tabs sit over a listing, not inside a panel.

## About — presentation

**147. Who We Are and Our Team openings own crumb + H1** — ~~`about-who` and `about-team` set `suppressPageHead`. AB1 takes `crumb` / `pageTitle` from the route registry (crumb `text-xs text-neutral-500`, H1 `text-4xl font-bold`, then the three intro paragraphs — same composition as C1, no inner heading). Our Team drops AB6 and renders crumb + H1 in the shell, then the AB7 groups.~~ **Revised at the page-level band unit:** both pages mount `PageHeaderBand`. AB1 is the three intro paragraphs only. Our Team is the band then the AB7 groups. AB6 stays omitted on Our Team and on Supporters / Past Initiatives / Careers behind `slots`. `suppressPageHead` is gone (decision 152). _The band is the H1; AB1 no longer owns it. AB6's fill stood for net-new micro-copy that does not exist, so omission until copy exists (standing constraint 3)._ → Set aside: ~~keeping the band — would leave two H1s once AB1 titles the page;~~ keeping AB6 as a fill on Our Team — marks missing copy, but the grey strip under the title reads as leftover chrome rather than a gap. **The first set-aside is now the decision.**

**148. AB2 vision is a grey highlight with the heading inside** — ~~drop the 3px border. One inner `bg-neutral-200` padded wrapper holds the Vision heading (`block-heading`) and the sentence, same fill treatment as C6 so it survives `codes-off`. The h2 is inside that wrapper; `Block` does not also take `heading`. N8 and N15 stay 3px.~~ **Revised at the Vision block unit:** AB2 passes `heading="Vision"` to Block — same h2 as Mission and Values — then the sentence as a paragraph. No grey fill, no grid. C6 keeps the fill (decision 144); ~~N8 and N15 stay 3px.~~ **Revised at the N5/N8 retirement:** N15 stays 3px; N8 is gone (decision 200). **Decisions 57, 134, 135 and 144 revised in place.** _Vision sat in a highlight band at a smaller heading while the next two blocks were titled sections; one statement does not need a fill or a grid to sit in that kit._ → Set aside: keeping the grey fill — what this decision first required, but it made Vision a callout on a page of titled blocks; passing `heading` and keeping the fill — two treatments; a one-item numbered grid — would fake a set.

**149. AB3 mission is a gapped card grid, not Cells** — AB3 uses its own wrapping `border` cards with `gap-2.5`, matching AB4. The `01`–`04` eyebrow stays. Shared `Cells` is unchanged — ~~S4 and HM4 stay a tight shared-border row.~~ **Revised:** S4 later left Cells (decision 160); ~~HM4 stays a tight shared-border row.~~ **Revised at the HM3-into-HM4 unit:** HM4 left Cells too (decision 80 revised); the primitive is deleted. **Decision 80 revised in place.** _Mission points and values sit on the same page; a tight cell row under a gapped values grid made four short claims look like a different kit. Cells' overlapping borders can't take a gap without becoming a different primitive._ → Set aside: adding gap to Cells — ~~would restyle S4 and HM4, which need the shared-border row;~~ keeping Cells on AB3 — one primitive, but the mission/values pair wouldn't match.

**150. AB5 spaces the rail below the lead; the gap row is labelled, not filled** — `mt-4` on the Timeline in AB5 only (S1 already has `mb-2` on its lead). AB5 stops passing `fill: 72`; the last row stays `2018–2026` + “Not covered by the published history” with no `text`, so Timeline omits the body. **Decisions 126 and 128 revised in place.** _A fill under a row that already names the gap read as leftover chrome; the labelled row still makes nine unwritten years visible (decision 105)._ → Set aside: keeping the fill — named the gap, but looked like a missing description on every other row; ending at 2017 — already set aside in 128.

**151. AB7 people cards have no outer frame; bios toggle from the name** — drop the card’s outer `border border-black`. The photo box keeps its own border; name / role / country sit under it with no frame. Drop the “Read bio” AccordionItem. Records with a bio: a disclosure arrow next to the name (`▸` / `▾`); the name row toggles the bio in place. Records without a bio stay static — no arrow, no click (decision 48). AB7 is a client block so it can own open/closed state (same pattern as Accordion / S5). Default Accordion stays boxed (S3, C11). **Decisions 71 and 142 revised in place.** _The boxed “Read bio” row under a framed card read as a second chrome layer; the name is the person, so that’s where the disclosure lives._ → Set aside: keeping AccordionItem — one primitive, but a labelled expander under a name is a second title; keeping the outer card border — matched C3/AB4 cards, but a people grid of framed boxes made photo + name look like a form field.

## Page-level header band

**152. The page-header band is page-level; it is the only crumb + H1** — `PageHeaderBand` leaves layout and is mounted by every page shell except Home. `suppressPageHead` is gone. C1 and AB1 drop crumb / pageTitle (C1 also drops the unused inner title); ~~C7 unmounts from the programme opening and stays parked until a later restructure places it with apply / guidelines.~~ **Revised at the programme-sections unit:** C7 remounts first in Application. **Revised at the band-CTA unit:** C7 sits in the sticky band next to the H1 (decision 159), so the title row is the band's, not C1's. `config.dest` is consumed again. Stand for Art: rolling, so the button is never closed (decision 95). The `closed` panel toggle on the other three programmes has a consumer again (decision 153). ~~The band remains the only crumb + H1.~~ **Revised at the editorial-column unit:** the band remains crumb + H1 except on `/news/editorial-post`, where `hideTitle` leaves the crumb in the band and the H1 sits in the sticky left column (decision 199). **Revised at the publication-column unit:** `/publications/research/publication` uses the same `hideTitle` split, with the H1 in the reading column (decisions 201, 202). **Revised at the editorial reading-column unit:** the editorial H1 moves to the reading column too (decision 205). **Revised at the network-record unit:** `/network/record` uses the same split; MN6 renders the H1 (decision 210). **Decisions 33, 137, 138, 139, 147 revised in place.** _One H1 per page. On every other route it is owned by the band._ Opening blocks that also titled the page duplicated it once the band moved into the shell._ → Set aside: keeping C1 / AB1 as the H1 so C7 can sit on the title row — that's what produced the duplicate; deleting C7 — the block is still specced, just not on the opening.

## Programme page sections

**153. Programme pages compose three always-on sections plus optional S3** — `ProgramPage` walks `config.blocks` in current order and buckets by type so relative order inside a section is preserved, except S1, which is always last in Overview when present (decision 162). Overview (always): C1, C2 or the no-figures hint, S4 when `hero` is set, then C5 from the list (Abbara's C6 after C5), then S1 last. ~~then any of S1 / C5 from the list.~~ ~~then any of C3 / S1 / C5 from the list.~~ **Revised at the Abbara S4/C3 unit:** C3 is gone. **Revised at the S1-last unit:** S1 no longer shares config order with C5 — it is pulled out of the overview bucket and mounted last (decision 162). Application: ~~**C7 first**~~ ~~**Revised at the C7 heading-row unit:** C7 sits on the Application h2 row, right-aligned and sticky (decision 159) (`closed={config.rolling ? false : state.closed}` — decision 95 restored), then C4, C8, C9, C10, S2, S6, S5.~~ **Revised at the band-CTA unit:** C7 sits in the sticky band (`closed={config.rolling ? false : state.closed}` — decision 95 restored), not in Application (decision 159). Application is C4, C8, C9, C10, S2, S6, S5. C6 stays next to the block it already qualifies — `section?: 'overview' | 'application'`, default `'application'` (Wijhat after S5); Abbara passes `'overview'` (after C5, still ahead of S1). Past is omitted when S3 is absent or gated off (`offeredOnly && !offered`); its visible h2 uses S3's existing label (`Past beneficiaries` / `Abbara organisations by round`). ~~FAQs then Contact. Overview's `<h2>Overview</h2>` is `sr-only` — in the document outline, hidden in both views, never `.block-heading`. The other section h2s use `.block-heading` at `text-2xl font-bold`.~~ ~~**Revised at the section-rules unit:** the first section's h2 is a visible "About"; FAQs is the last section; Contact is a sentence under FAQs (decision 173), not its own section. All section h2s use `.block-heading`.~~ **Revised at the heading/spacing unit:** About's h2 is `sr-only` again; Application, Past and FAQs keep a visible `.block-heading`. FAQs is the last section; Contact is a sentence under FAQs (decision 173), not its own section. Each section is a full-width `<section>` with `border-b` and inner `PageWidth` (decision 172); a sticky in-page nav sits under the band (decision 174). `Block` gains `headingAs` (default `'h2'`); programme blocks that still pass `heading` (C4, C5, C8, C9, C10, S1, S2, S5, S6) pass `headingAs="h3"` and stay `text-lg`. C11, C12 and S3 drop `heading` so they do not duplicate the section title. C1, C2, C6, C7, S4 stay untitled. ~~No extra section boxes — Figma's grey frames are grouping notes.~~ **Revised at the section-rules unit:** the grouping is a full-bleed bottom rule, not a box. **Decisions 135, 137, 139, 145, 152 revised in place.** _Figma (Mawred, node 287:587) groups the kit this way; the band stays page-level (decision 152)._ → Set aside: wrapping each section in a bordered frame — would add chrome the Figma frames don't mean; putting Stand for Art / Abbara C5 in Application — forms of support are what the programme is, not how to apply.

## Website shell

**154. Global chrome is a sticky right rail** — layout is a two-column flex: scrolling main (`flex-1`, no max-width) on the left and a sticky right rail (`w-64`, `h-screen`, 1px left border) on the right. `SiteHeader` owns the rail top: Logo → UtilityBar (Search + EN / ع, no centering) → vertical nav. ~~Dropdowns stay CSS-only (`group-hover` / `group-focus-within`) and fly left over main (`absolute right-full top-0`).~~ **Revised at the in-flow nav unit:** dropdowns stay CSS-only (`<details>` / `<summary>`, shared `name` so one is open at a time) and expand under the parent in the rail on click — not hover, and not as a left flyout. No panel or row borders; `+` prefixes a collapsed trigger and becomes `-` when open; space below an open menu so the next item sits clear. Subsequent nav items shift down while a menu is open. ~~Publications' Library / Series groups stay.~~ **Revised at the flat-publications-nav unit:** Publications is a flat dropdown like About and Grants — Research & Publications, Mudawanat Podcast, Foundations of Cultural Policies, Mawred Talks. The Library / Series group labels are gone. **Revised at the header-notes unit:** spacing follows Figma 309:392 — 60px viewport to logo, 40px logo to utility, 60px utility to nav, 28px between top-level items, 18px parent to first child, 12px between children. UtilityBar is a bordered Search plus two lang boxes (EN filled, ع bordered), not one EN|AR chip. Hover, the current route, and an open parent are a black fill with white text that hugs the label. The About trigger is labelled About. ~~and the optional Mawred's 20th item stay.~~ **Revised at the HM5-owns-the-20th unit:** the optional Mawred's 20th rail item is gone (decision 44 revised). ~~`SiteFooter` owns the rail bottom: Newsletter is a link that opens a lo-fi popup (Field + Subscribe + Close); Social is a labelled placeholder; copyright stays. Footer link columns and the inline email row are gone.~~ **Revised at the page-end footer unit:** the rail is header only. `SiteFooter` sits under `main` in the content column (decision 179). `PageHeaderBand` stays page-level inside main (decision 152). ~~The state panel stays `fixed` as a review overlay.~~ **Revised at the state-panel unit:** the panel is a top-right overlay, closed by default, opened from a cog (decision 155). **Decision 57 revised in place:** header/footer 2px rules no longer exist; the rail edge is 1px; 2px stays on primary buttons only. **Revised at the PageWidth unit:** `main` carries a 1px left border and vertical padding only; `max-w-300` and horizontal padding live on a shared `PageWidth` wrapper each page shell mounts (decision 171). **Revised at the mobile-chrome unit:** below `lg` the two-column flex and sticky rail drop. Layout is stacked header → main → footer: no flex, no sticky, no rail-edge left border (the header takes a 1px bottom rule). From `lg` the sticky rail stands. ~~The burger overlay is decision 183.~~ **Revised at the in-flow menu unit:** the burger toggles UtilityBar and nav in flow (decision 183). CardGrid and listings do not reflow (decisions 167, 182). _Figma website shell (Mawred, node 299:355) is schematic — labelled slots, not a visual proposal. Stacked utility bar / header / newsletter / footer duplicated chrome above and below every page; the rail is one viewport-tall column while main scrolls._ → Set aside: keeping stacked chrome — what the port had, but it is not the shell Figma specified; a left rail — main is the left column in the schematic; ~~a mobile breakpoint — same as the rest of the wireframes, none.~~ **Revised at the mobile-chrome unit:** chrome now has an `lg` break (decision 183); listings stay as they are.

**155. State panel opens from a top-right cog** — the panel is closed by default. A 1px cog button sits `fixed top-3 right-3`; click opens the same panel below it (still `fixed`, still 2px). Click again closes it. Open/closed is inverted fill on the cog. Hiding the panel does not flip review chrome. ~~Toggles and the `codes` default are unchanged.~~ **Revised at the codes-default unit:** `codes` now defaults off (decision 156). _The always-on bottom-right overlay sat on every page as review chrome the client view doesn't need; a closed-by-default trigger keeps the page readable and the panel one click away._ → Set aside: a Dev on/off that also flips `codes` — would conflate "show the panel" with "client view"; leaving the panel always visible — what we had; putting the cog in the page-header band — Home has no band.

**156. Review chrome is off by default** — `codes` starts `false`; `<body>` carries `codes-off` in layout so the first paint is the client view, not a flash of badges. The panel toggle still turns it on. _The cog already hid the panel; leaving chrome on meant every page still opened annotated. Client view is what the wireframes are for._ → Set aside: keeping default-on and only hiding the panel — what we had; flipping `codes` when the cog opens — would couple "show the panel" with "show chrome".

**157. Figs values are `text-4xl`** — the figure number in `Figs` (C2, HM2, MN2) is `text-4xl`. Labels stay `text-xs`. **Revised after `text-2xl` still sat too close to body type.** _The value is the cell; page H1 is already `text-5xl` (decision 152's band), so `text-4xl` stays under the title and above section headings._ → Set aside: `text-2xl` — permitted-scale top, but not enough contrast; changing only Production Awards' ~400 — would split the shared `Figs` component.

**158. Production Awards drops C3** — the leftover three-card strip (eligibility, disciplines covered, grant period) leaves the page. ~~`C3AtAGlance` stays in the kit; Abbara is the remaining consumer.~~ **Revised at the Abbara S4/C3 unit:** C3 retires entirely. `C3AtAGlance`, the `C3flat` config arm, and the ProgramPage cases are deleted. The C3 code is not reused. Abbara's leftover facts go to S4 (disciplines, where they work) and C8 (budget+years, registration, previous beneficiary); who-can-apply is dropped as an S4 restatement. Abbara's S4 is now the scope summary this decision reserved. ~~Production Awards' titled "Who is eligible" S4 is still reserved for a later unit.~~ ~~**Revised at the Production Awards S4 unit:** that reserved S4 landed as three untitled rows.~~ **Rolled back:** Production Awards has no S4. Who-submits and funds-in-the-region stay in C8; under 35 and Arab origin stay in C1; the birth-year range stays omitted (it shifts every round). The reserved shape restated facts that already had homes. **Decisions 85, 97, 98, 139, 140, 145 and 160 revised in place.** _After dates moved to S1 (decision 145), every leftover card repeated another block: eligibility in C1, disciplines in C5, grant period in C10. Wijhat already has no C3; Stand for Art never did. Abbara's seven cards hit the same failure: who-can-apply restated S4, disciplines and where belonged in S4, the rest were C8 rules._ → Set aside: keeping C3 as the scan table — what decision 85 required, but the scan was three facts the visitor had just read or was about to; a titled "Who is eligible" S4 instead — reserved, tried, and rolled back because the facts already live in C1 and C8; keeping C3 for Abbara only — the last consumer, but it reproduced the same restatement this decision killed C3 for.

**159. C7 sits in the sticky page-header band** — ~~C7 floats right of the Application h2 (`float-right`, ~~`sticky top-4`~~ ~~**Revised at the section-nav unit:** `sticky top-20` so it clears the in-page nav, decision 174~~ **Revised at the heading/spacing unit:** `sticky top-28` so it clears the taller stuck nav) so the button shares that row and remains in view after the heading scrolls away, through Past / FAQs.~~ **Revised at the band-CTA unit:** C7 is a right column in the same sticky wrapper as the crumb, H1 and section nav (`flex` / `self-center`), so Apply now sits next to the title and in-page links from the first paint and stays there through About / Application / Past / FAQs. It is not in Application and has no sticky offset of its own. C4 drops `withRepeat`; Apply now exists once. Same on all four programmes. C7's Block is `flush` so codes-off margin does not drop it off the band. _Two Apply nows was a leftover from remounting C7 above C4's in-block CTA. Sticky is this button staying put, not a second coloured bar — decision 2's per-program-colour CTA stays set aside. The heading-row placement waited until Application was in view, so About had no apply control._ **Decisions 137, 141, 153, 174 and 176 revised in place.** → Set aside: stacking C7 under the h2 — what 153 had; keeping C4's repeat — the live pages do this, but one control already names the destination; a dedicated right column through FAQs — would indent later sections around a 104px gutter; keeping C7 on the Application heading — what this decision originally required, but the heading is a section label, not the apply action's home, and the band is already sticky.

**160. S4 is a 2-up gapped grid; cell labels are h3s** — S4 leaves shared `Cells`. ~~Four (Wijhat) or two (Abbara) cells render `grid-cols-2` with `gap-8`, no card borders.~~ **Revised at the Wijhat S4/C5 unit:** S4 is stacked full-width rows. Each row is the `h3` label (`text-lg font-bold`, not `.block-heading`), the optional lead, then items as wrapping cards reusing C3's chrome (`border border-black p-2.5` — decision 140). ~~`grid-cols-[repeat(auto-fill,minmax(210px,1fr))]`~~ **Revised:** items are `flex flex-wrap`, each card `w-fit max-w-full shrink-0`, so a card is as wide as its copy; two sit on one line when they fit, otherwise they wrap. Body type on every card — no `text-xs` fork. ~~A row whose items are single terms (no `.!?`, at most three words) renders as a `Chip` row, not one-word cards.~~ **Revised:** the chip exception is gone. Short terms (Abbara's disciplines, a one-word Wijhat item) are the same cards; hug-content makes "Cinema" a small card rather than a stretched cell, so the chip size split is unused. The string `HeroCell` variant and its Fill fallback are dropped: every consumer now passes populated cells (decision 125's reasoning, not 84's). The Block stays untitled; the row labels are the titles (decision 153). Wijhat has three rows (Who is it for · For what purpose · From where to where). **Revised at the Abbara S4/C3 unit:** Abbara has three rows (Who is Abbara for · Disciplines covered · Where they work); the support cell is gone (decision 97 revised). ~~**Revised at the Production Awards S4 unit:** Production Awards has three rows.~~ **Rolled back:** Production Awards has no S4 (decision 158). **Revised at the Stand for Art unit:** Stand for Art has no S4 either (decision 161), so S4 is Wijhat and Abbara only. ~~HM4 stays on Cells.~~ **Revised at the HM3-into-HM4 unit:** HM4 left Cells (decision 80 revised); the primitive is deleted. **Decisions 80 and 86 revised in place; 140 noted.** _The 2-up grid made four summary headings look like a scan strip; stacking them with item cards is the at-a-glance treatment C3 established. Equal-width `minmax(210px)` columns stretched short copy and made taxonomy items look like a smaller kit; hugging content keeps one chrome for every item._ → Set aside: ~~keeping Cells — would restyle HM4;~~ putting `.block-heading` on the cell h3s — would hide the only titles the cells have in review chrome; ~~four-up without borders — the lists would be too narrow;~~ keeping the 2-up borderless lists — what this decision originally required, but it left S4 looking like titled prose rather than the scan block; keeping chips for term rows — what the Wijhat S4/C5 unit required, but once cards hug content a one-word card is already a chip.

**161. Stand for Art gets no S4; the page's restated facts are removed instead** — the last programme without a scope summary keeps its shape: C1 and C5 in Overview, C7–C4–C8–C9 in Application, then FAQs and Contact. S4 is Wijhat and Abbara only. What this unit did instead was take out four statements the page made twice. C1's second paragraph drops its closing sentence — the two ways an applicant can be at risk restated the first paragraph's own "because of the work they make … or because of an unsafe environment"; the five prioritised risk types stay. C4 drops "Complete it in Arabic" as a step, leaving the encrypted form and the documents, with the rule kept as C8's condition. C9's label drops ", and it sets the form of support", which the lead's second sentence states in full, so it now matches the other three programmes' short jury labels. C11's organisations question drops the individuals-only clause C8 carries verbatim and keeps only the redirect to Culture Resource's other programmes. **Decisions 94, 139 and 160 revised in place.** _With no S4 there is nothing to move anything into, so the plan's exchange — S4 gains a fact, C1 or C8 gives it up — has no second half, and what was left is one page saying the same thing twice. That is the failure mode decision 158 retired C3 for. Stand for Art's scope facts already have homes: C1 is the prose framing, C8 is the rules._ → Set aside: the planned S4 (Who it's for · What counts as a risk · Where) — it would lift the risk list out of C1 and two items out of C8 to restate them one screen higher, the trade that was rolled back on Production Awards; keeping the Arabic rule as C4's step and dropping C8's item — matches where the live page states it, but "complete it in Arabic" is a property of the form rather than a step, and C8 is where this kit puts application terms (decision 85); trimming the how-much FAQ against C5's "Set case by case" chip — an answer that opens by answering is not the same as an eligibility rule printed twice.

**162. S1 sits last in Overview** — when a programme has a timeline, it renders after C5 (and after Abbara's C6, which still qualifies C5). Production Awards: C1–C2–C5–S1. Wijhat: C1–C2–S4–C5–S1. Abbara: C1–C2–S4–C5–C6–S1. Stand for Art has no S1 — rolling intake, no dates — so Overview stays C1–C5. `ProgramPage` pulls S1 out of the overview bucket and mounts it last, so the rule does not depend on config array order. **Decision 153 revised in place.** _Overview is what the programme is, then what it offers, then when. Dates first made three of the four pages open on a calendar._ → Set aside: keeping S1 after S4 / before C5 — what 153 had, but the visitor then reads dates before they know the grant; putting S1 in Application — the calendar is still this round's facts, not how to apply.

**163. Body type is 16px** — `body` is `1rem` (`text-base`). `text-sm` is remapped to `1rem` so chrome, cards and buttons that were matching the old 14px body follow. `text-xs` stays 12px for labels. **Decision 58 revised in place.** _14px was the port mapping the legacy body onto `text-sm`, not a wireframe constraint; 16px is the default reading size._ → Set aside: leaving `text-sm` at 14px and only bumping unclassed body copy — would split the site into 16px prose and 14px chrome.

**164. The positioning line sits inside the hero frame** — HM1's identity sentence moves out of the row under the frame and into the frame itself, bottom-left, at `text-2xl font-bold` on a white plate. ~~It is the only thing in that corner: the frame caption is not drawn (decision 36 revised). The wayfinding buttons stay on the row below and gain "Who we are" as a third link.~~ ~~**Revised at the HM1 plate unit:** the plate is the sentence plus the three wayfinding buttons under it (`p-3`, `mt-4`, wrapping `gap-2`). The row under the frame is gone. The caption is still not drawn (decision 36 revised).~~ **Revised at the HM1 inline-wayfinding unit:** the plate is the rewritten sentence only. The three destinations sit in the prose as 2px-underlined links; `HOME_WAYFINDING` and the button row are gone (decision 186). The caption is still not drawn (decision 36 revised). Home has no H1 (decision 33), so this line is the largest type on the page. **Decisions 35, 36, 42 and 81 revised in place.** ~~**Revised at the HM1 collage unit:** the line stays bottom-left, on the same white plate, at `z-50` over a corner the five frames leave clear. It does not move out of the imagery — leaving the composition would undo this decision one unit after taking it. **Decision 165.**~~ **Rolled back with decision 165:** the line sits inside the single frame again. _Home opened on a 300px grey box with a slider's controls and a `text-sm` sentence underneath — the block was carrying the site's only identity statement at the same size as a card subtitle, and the imagery and the words were reading as two separate rows. Inside the frame they are one statement, and the size gives Home the hierarchy the missing H1 would have._ → Set aside: leaving the line under the frame and only bumping its size — keeps the two rows, so the frame still reads as a decorative band; a white plate holding caption and line together — fewer boxes, but conflates a per-frame credit with the site's identity sentence; putting the line over the fill with no plate — inside the permitted set, but a `neutral-200` fill is where the image goes, so the text has to sit on something.

**165. HM1 is an overlapping collage, not a sequence** — ~~five hand-placed frames at varied sizes sit in a `relative` composition area and are allowed to overlap; each keeps a 1px black border so an overlap reads as two images rather than one L-shape. Placement (`top` / `left` / `width` / `height` / `z`) lives on `HOME_HERO_FRAMES` beside the captions, so it is content, not component. Nothing moves — a collage that does not move has no sequence, which finishes what decision 35's revision started — and `staticfb` retires with it. The positioning line stays bottom-left over a reserved corner (decision 164 revised). The note top-left names the composition.~~ **Rolled back:** HM1 is again one full-bleed frame (`aspect-[16/9]`, `min-h-[360px]`) with the positioning line inside it and `staticfb` switching the frame note. Placement fields leave `HOME_HERO_FRAMES`. The collage read as five boxes rather than a cinematic hero, which is the same wrong-intention problem Unit 1 fixed for the slider controls. **Decisions 35, 36, 38, 81, 82 and 164 revised in place.** The set-aside is now the decision. _Five overlapping grey rectangles named "collage" still look like a layout experiment, not a hero — lo-fi can show a sequence as one tall frame; it cannot show overlap as cinema._ → Set aside: ~~keeping the single full-bleed frame and only cycling it — a sequence the collage was meant to replace; computing offsets at render — hydrates differently on server and client; cycling frames in place so `staticfb` can stay — motion the composition does not need.~~

**166. Cover is portrait `aspect-3/4`** — the shared `Cover` box is `aspect-3/4 w-full`, not a fixed 120px / 250px height. ~~Card covers (PA4, PC2),~~ the publication-detail cover (PC1), news and initiative images, MN6's photo/logo, and AB7's people photo / default-avatar all share that ratio. `tall` keeps only the no-margin detail-column treatment. **Revised at the listing-row primitives unit:** `Cover` takes an optional `compact` — same `aspect-3/4`, no `w-full` / `mb-2.25` — for a thumb beside a listing row. Full-size Cover is unchanged. **Revised at the publications listing-rows unit:** PA4 and PC2 consume that compact Cover (decision 191). Compact width is `w-24`, not `w-16` — a stacked author / title / language column needs a larger portrait. **Decision 68 revised in place.** ~~AB7 kept a local `h-[105px]` box; revised at the people-portrait unit so the leftover landscape strip uses Cover.~~ _A publication cover is a portrait object; a 192×120 landscape bar was reading as a photo strip. A person photo is the same object — 195×105 was the same strip. Aspect is in the permitted set, and one ratio keeps the one-component rule. Compact is a width, not a second ratio._ → Set aside: keeping the 120px card / 250px detail split — two heights for the same object; `aspect-[2/3]` — more book-like, but taller than the 190px column already used on PC1/MN6; portrait on publication cards only — would fork the primitive decision 68 refused; a second height on AB7 — the leftover the people-portrait unit retired; a second cover component for thumbs — would split the one-component rule decision 68 refused.

**167. CardGrid is four columns** — the shared `CardGrid` is `grid-cols-4`, not `repeat(auto-fill, minmax(190px, 1fr))`. ~~Publications, news, initiatives, rosters, related strips, and AB7 people groups share it.~~ ~~AB7 kept a local `minmax(165px)` auto-fill; revised at the people-grid unit so four cards fill the row.~~ ~~HM4 shared the four-column row, with the fourth slot empty while Abbara is closed.~~ ~~**Revised at the HM4 card-layout unit:** `CardGrid` takes an optional `cols` (`3` | `4`, default `4`). Listings stay four columns; HM4 passes `cols={3}` so three open calls fill the row (decision 184).~~ **Revised at the listing-card unit:** `CardGrid` takes an optional `cols` (`3` | `4`, default `4`). The default stays `4` for AB7 people groups and N14 rosters, which use `CardGrid` without `Card`. Listing and related grids pass `cols={3}` (decision 189). _Portrait covers at 190px / 165px min packed five or six across; four columns give each cover room, and four cards stretch to the container. Chrome now has an `lg` break (decision 183); CardGrid stays a fixed count — a layout, not a reflow. Three open-call cards on four columns was a hole, not a reserved slot._ → Set aside: raising the minmax until five no longer fit — still a reflow, and a wide enough column would bring five back; four columns on publications only — would fork the primitive; keeping AB7 on auto-fill — leftover from the people-portrait unit; keeping HM4 on four columns with an empty slot — the version this revision replaces; keeping listings at four once titles moved to `text-2xl` — cramped cells, which is what 189 replaces.

**168. Facet panels are anchored combobox dropdowns** — `FilterBar` opens each facet as a popup anchored to its own trigger, not a full-width panel under the row. A facet over 12 values gets a real search input that filters the list; every matching value is in a scrollable list with real checkboxes. No truncation, no "Show all". Clear sits in a footer only when that facet has selections. Closed on first paint (`defaultOpenIdx` gone); one facet open at a time; outside click and Escape close it. Selection state lives in the primitive with open/close (decision 64). **Decision 12 revised in place** — its facet clause is superseded; the people-group clause (decision 50) stands. _Three affordances detached from the trigger (search, six wrapped labels, show-all) were one facet; a dropdown is the same job as the rail's nav, and long labels no longer blow the row._ → Set aside: keeping the full-width truncated panel — what decision 12 required, but the panel spanned the bar and long values wrapped to three lines; a headless-UI / shadcn Combobox — would pull styling the lo-fi set forbids.

**169. Directory listing sample is twelve rows per entity tab** — names and countries come from the live community listing; involvement and discipline are authored for the wireframe because the listing HTML does not publish them. Hiba Taim and Al Kasaba stay first on each tab so the filtered-state chips still resolve to a real row. One populated detail page (Al Kasaba); every row still links there. **Decision 110 revised in place.** _Two rows per tab demonstrated the row shape and left the listing looking empty._ → Set aside: scraping involvement and discipline from every live record — the listing does not publish those fields, and filling forty records is not this unit; keeping two as the structural minimum — true for the original populate pass, but the listing no longer reads as a directory.

**170. C4 step cards are full-width, with index and label on one line** — each C4 card is `w-full` in a single-column grid. The step number and headline share one `text-xl font-bold` row (`flex items-baseline`); the detail stays under them in `text-xs text-neutral-500`. Same on nested S5 paths. **Decision 141 revised in place.** _A wrapping `minmax(210px)` row made four short actions a scan of narrow columns, and stacking the number above the label split one heading into two._ → Set aside: keeping the wrap and only inlining the title — leftover columns would still leave cards short of the container; matching weight but leaving the number `text-xl` and the label body size — two styles on one line.

**171. Main carries a left border; page width lives in `PageWidth`** — ~~`main` is `flex-1` with `border-l border-black` and vertical padding only.~~ **Revised at the page-end footer unit:** the 1px left border lives on the content-column wrapper so it runs through `SiteFooter`; `main` keeps vertical padding only (decision 179). Horizontal padding and `max-w-300 mx-auto` move to a shared `PageWidth` wrapper each page shell mounts. Programme pages mount more than one so a section rule can span the column's full width while copy stays in the column. **Decision 154 revised in place.** _A max-width on main cropped any full-bleed rule at the column edges._ → Set aside: keeping max-width on main and drawing the section rule inside it — the Figma annotation wants the rule to the rail; a CSS grid with full-bleed tracks — more machinery than a wrapper.

**172. Programme sections are full-bleed rules with inner `PageWidth`** — each section is a full-width `<section>` with `border-b border-black` and ~~`py-16` (About `pb-16`; FAQs `pt-16` and no border, so main's own bottom padding closes the page)~~ **Revised at the heading/spacing unit:** `py-8` (About `pb-8`; FAQs `pt-8` and no border). Content sits in `PageWidth`. ~~The first section's heading is a visible "About", matching Application / Past / FAQs.~~ **Revised at the heading/spacing unit:** About's h2 reverts to `sr-only`; Application, Past and FAQs keep a visible `.block-heading`. Sections take `scroll-mt-28` so an anchor clears the stuck nav. **Decisions 135, 153 and 159 revised in place.** _Figma 310:458 asked for spacing between sections and a border that reaches the rail._ → Set aside: wrapping each section in a bordered frame — 153 already set that aside, and a box is not a rule; padding only, no rule — the annotation asks for both; a visible About heading — what the section-rules unit had, but the nav already names the section.

**173. Contact is a sentence under FAQs** — C12 renders "If you have more questions, email us at {address}" (`mailto:`) and mounts under C11 inside the FAQs section. No Contact h2, no nav item. The C12 code stays; `config.contact` is still the only source of the address. **Decision 153 revised in place.** _A labelled KV row was a fifth section for one email._ → Set aside: keeping Contact as its own section — five hops for a closing line; retiring C12 — the code is still a real block.

**174. Programme pages have a sticky in-page nav** — `SectionNav` is ~~a `→` plus~~ one anchor per section (About, Application, Past when shown, FAQs), sticky at `top-0` as a direct child of `main` so it is not released when the band scrolls away. Crumb and H1 scroll off. ~~No active-on-scroll tracking; all links sit at the same weight.~~ **Revised at the active-section unit:** an IntersectionObserver on the section elements marks the topmost section that has passed under the stuck bar (`rootMargin: '-112px 0px -60% 0px'`). Each link has a `→` in an always-occupied slot, `invisible` unless that section is active, so the row does not shift; the active link takes `aria-current="true"`. Default active is the first item. The effect keys off the joined item ids, not the array identity. `html` is `scroll-smooth`. ~~Sections take `scroll-mt-24`. C7's sticky offset is `top-20` so it clears the nav (decision 159 revised).~~ ~~**Revised at the heading/spacing unit:** sections take `scroll-mt-28`; C7 is `top-28` (decision 159).~~ **Revised at the band-CTA unit:** sections take `scroll-mt-(--band-h)`; C7 has no sticky offset of its own (decision 159). The stuck bar's `pt-15` alignment is decision 176. _Figma 310:458._ → Set aside: sticking the whole band — would pin a `text-5xl` H1; ~~IntersectionObserver active state — the Figma row shows equal weight.~~ a single leading arrow and equal-weight links — what this decision originally required, and what the Figma row shows, but the arrow then pointed at the row rather than the section in view.

**175. Block-frame gap is 3rem in client view** — `body.codes-off .block-frame { margin-block: 3rem }`. Site-wide, not programme-only. **Decision 135 revised in place.** **Exceptions:** when S5 closes on a highlight and C6 follows, the pair uses 1rem (decision 178); Home HM4 / HM5 / HM6 use 5rem between them (decision 188). _2rem was tight once sections had their own 4rem padding._ → Set aside: a programme-only class — would fork the primitive; 4rem — more than the annotation's "increase".

**176. Sticky section nav aligns with the rail's 60px inset** — the nav wrapper is `sticky top-0 z-20 bg-white pt-15`. White padding masks the strip of page that would otherwise scroll past above the bar; the nav row lands on the rail's logo line (`layout` `pt-15`). Cost: 60px more space between the H1 and the nav in the resting state. The taller stuck bar (~107px) is why sections take `scroll-mt-(--band-h)` (decisions 172, 174). C7 no longer has a `top-*` offset — it lives in the band (decision 159). _A flush `top-0` bar sat above the logo._ → Set aside: `top-15` without the padding — the bar would align when stuck but content would show in the 60px gap above it.

**177. Application in-block highlights sit last in their parent** — S6's three schedule notes wrap in the same `bg-neutral-200` padded band C6 and C4 use (decision 144), still inside S6, at body size rather than `text-xs`, after the when-to-apply rows. S5's save-and-return / purpose sentence leaves `core` for an optional `highlight` string in that same treatment, ~~still above the tabs and so still before C6~~ **Revised:** last in S5, after the tabs, immediately before C6. C4's optional `highlight` is last in C4, after the steps, documents and `note` (decision 91). S6's lead takes `mb-4` so it sits clear of the KV rows. The S5–C6 pair's gap is 1rem, not the 3rem block-frame gap (decision 178). **Decisions 88, 89 and 91 revised in place.** _They are provisions that matter more than their length, which is what the grey fill is for; a second page-level C6 would split Wijhat's visa letter (decision 87) and S6's notes belong with the dates they qualify (decision 89). Mid-block they interrupt the sequence they qualify; last in the parent they close it, and S5's highlight then stacks with C6._ → Set aside: a second C6 for each — would orphan the visa letter as one of three callouts; keeping the notes as a `text-xs` list and the save line as Prose — what this unit replaces, but they read as footnotes against C6 sitting a screen below; keeping S5's highlight above the tabs — what this decision originally required, but it sat between the portal sentence and the path the visitor is there to pick.

**178. Stacked application highlights share the in-block 1rem gap** — when S5 closes on a highlight and C6 follows, S5 takes `tightBelow`: client view drops the pair from 3rem to 1rem (`mt-4`), matching the gap above S5's highlight (tabs to note). C6 stays its own block. Abbara's C6 (after C5, in Overview) is unchanged. **Decision 175 noted.** _Two grey bands read as a stack, not as two blocks; the 3rem frame gap belongs between different kinds of content._ → Set aside: folding C6 into S5 — would keep one frame, but the visa letter is C6 (decision 87); leaving 3rem — what 175 requires, but 3rem between two identical fills is a hole, not a section.

**179. Site footer sits under main, not in the rail** — layout's content column (`flex-1`, 1px left border) is `main` then `SiteFooter`. The rail is header only: logo, search + EN / ع, vertical nav. Footer is a full-bleed top rule with inner `PageWidth`: ~~Newsletter (same popup), Social placeholder, copyright, as a `justify-between` row. Footer link columns and the inline email row stay gone (decision 154).~~ **Revised at the footer-form unit:** Newsletter is the Field + Subscribe form in the footer, not a popup; social is a flex of named accounts, not a placeholder box (decision 197). Footer link columns stay gone (decision 154). Short pages still land the footer at the bottom of the column (`min-h-screen` + `main` `flex-1`). **Decision 154 revised in place.** _The three slots are site chrome, not a programme close; pinning them in the rail put them beside the page rather than after it, and a 240px stack does not survive as a page-width footer without becoming a row._ → Set aside: keeping the footer in the rail — what 154 required, but it never scrolled with the page it closed; restoring the pre-shell link columns — those were the stacked chrome 154 retired.

**180. Browse controls are one compact row** — search field, Search button and every facet trigger sit on a single wrapping row at `px-2.5 py-1`, keeping `text-sm` (16px, decision 163) and taking the height out of the padding rather than the label. `FilterBar` takes an optional `lead` slot for the controls that precede the facets, so MN3's search no longer has a row of its own — its `flex-[1_1_240px]` field was growing to fill the line and pushing the five facets underneath. The trigger's label-to-`+` gap drops `gap-3.5` → `gap-2`. `compact` is opt-in on `Btn` and `Field` (the footer's Subscribe and email field, Apply now and Load more keep the full size); on the facet trigger it is unconditional, since the same control appears on MN3, PA2 and N1 and should not be two sizes. Sort and Clear all in MN4, PA3 and N2 take it too. `CountRow` itself is unchanged — its height falls out of the smaller buttons. _Two rows of full-size controls and a 3rem gap put 250px between the stat strip and the first record, for a toolbar that is one line of chrome._ → Set aside: `text-xs` labels as well as the padding trim — what this decision first had, but 12px controls under a 16px body read as fine print rather than as the site's own chrome, and the row already fits at body size; shrinking the row padding on `CountRow` — the height was coming from the buttons inside it, not the row; a compact facet trigger on the Network only — would fork the primitive across three browse pages; dropping the Search button and leaving the field alone — the field is the only affordance a lo-fi box has, but the button is what says the search runs on submit.

**181. Entity tabs sit in MN4, above the count they scope** — the `Individuals & Groups` / `Organizations & Initiatives` tabs move out of MN5 into MN4, which becomes the listing header: tabs, then count · chips · sort. `activeIdx` / `onTabSelect` move with them; MN5 keeps `orgs` to pick its row set. Block gaps stay at the site-wide 3rem (decision 175) — no `tightBelow` anywhere in this stack: the compaction comes from the single control row and the tabs moving, and closing the gaps on top of that left the filter row, the tabs and the first record reading as one undifferentiated slab. `Tabs` keeps its default `mb-3`, which leaves the active tab's white notch clear of the count row's top border. Entity type is still owned by tabs rather than a facet — **decision 20 noted, not revised** — and the tabs-not-stacked rationale (decision 19) is unchanged. **Decision 175 noted.** _MN4 read "Showing 12 records in Individuals & Groups" a full 3rem above the tab strip that names it, so the count scoped a control the visitor had not seen yet._ → Set aside: `tightBelow` on MN3 and MN4 — what this decision first had, but 1rem crowded the tabs against the filters and put the first record hard under the count row; leaving the tabs in MN5 and only tightening the gaps — the count would still precede the tabs; folding MN4 into MN5 — one frame, but the count row is its own block and its code has to stay; collapsing the tab strip's bottom border into the count row's top border with `-mt-px` — one hairline instead of two, but the count row then paints over the active tab's white notch.

**182. PB2 video series are a two-column grid** — PB2 takes `layout: "row" | "grid"`. Audio (Mudawanat) stays one embed per row at content width; ~~video uses the shared `CardGrid` (`grid-cols-4`, decision 167)~~ **Revised:** video (Foundations of Cultural Policies, Mawred Talks) is a local grid, `grid-cols-1 md:grid-cols-2`, of eight sampled embeds — not `Card`s, and not `CardGrid`. `SeriesPage` derives layout from `kind`. Audio footprint stays `h-[58px]`; video is `aspect-video` (decision 65 revised). No bespoke wrapper (decision 14). CardGrid's default stays four columns for people/roster (decision 167); listing cards are three (decision 189). The one-column mobile step is this listing only — chrome now has an `lg` break (decision 183); CardGrid does not. _SoundCloud's thin player reads as a stack; four YouTube columns were too many for the embed. Spec Structure B already said audio vs video looking different is a treatment on one structure, not two blocks._ → Set aside: a second block — would fork the series listing; four columns — what this decision first had, but the tiles were too small; forking CardGrid's column count — CardGrid is the card rhythm, not this embed listing.

**183. Mobile chrome is a stacked header and a burger overlay** — below `lg`, layout is header → main → footer, not a flex row, and the header is not sticky. The bar is logo + a `size-10` burger (three 1px rules). Search and EN / ع stay out of the bar. ~~The burger opens a full-screen overlay (`fixed inset-0 z-40`, same layer as the newsletter popup) that reuses UtilityBar and the existing `<details>` nav — Close, then search/lang, then the same `NAV` tree. Close on Close, Escape, and pathname change; body scroll locks while open. The state-panel cog stays `fixed top-3 right-3` (decision 155); the header and overlay take `pr-14` so the burger and Close sit clear of it.~~ **Revised at the in-flow menu unit:** the burger toggles one UtilityBar + `NavList` block in flow under the logo/burger row (`hidden` unless open, always `lg:block`). Invert fill when open, same as the cog; no separate Close. Close on Escape and pathname change; no body scroll lock. The header wrapper's bottom rule stays under the expanded menu. The state-panel cog stays `fixed top-3 right-3` (decision 155); the header wrapper still takes `pr-14` so the burger sits clear of it. From `lg` the sticky rail is unchanged. **Decision 154 revised in place.** _A 320px sticky rail on a phone is the page; the overlay was a second container for the same rail. Opening in flow keeps one header._ → Set aside: Search and lang in the bar — the logo is already wide and those controls are the rail's second row; a full-screen overlay — what this decision first had, a second container with its own Close; a drop panel that keeps the header visible — a third chrome layer.

**184. HM4 chips sit under the descriptor; three cards fill the row** — the status `Badge` moves from above the title to under the descriptor, so a wrapping chip does not push programme names out of line. `Badge` is `inline-block max-w-full` so a two-line chip is one box, not two open inline borders. Titles are `text-2xl` like HM6's `DirRow`. `CardGrid` takes `cols={3}` on this strip. **Decisions 40 and 167 revised in place.** _A chip above the title made Wijhat's two-line status the row's height, and four columns left a hole next to three cards._ → Set aside: keeping chips on top and only boxing the wrap — titles would still drop; shortening the Wijhat status so it fits one line — would lose the three-rounds fact; leaving the fourth column empty until another call opens — decision 40's last revision, which left a hole.

**185. HM1 is full-bleed to the content column** — Home mounts HM1 outside `PageWidth` and pulls it into `main`'s `py-4 lg:py-10` with `-mt-4 lg:-mt-10` (same move as the programme band). `Block` gains `flushTop` so client view zeros only the top frame margin; the 3rem bottom stays so the gap to HM2 survives when HM2 is `PageWidth`'s first child. The fill is `w-full` with inner `p-3` for the note and the plate only. It reaches the rail-edge wrapper, not under the rail. **Decision 164 revised in place** (~~wayfinding moves into the plate~~ wayfinding now sits in the sentence — decision 186). HM5 and HM6 action buttons take a `gap-6` column so they cannot sit on the copy. **Revised at the Home polish unit:** those buttons also take `self-start` so they hug the label rather than stretching (decision 187). _The hint had deferred bleed as "at build"; lo-fi can show a frame that hits the column. A row of buttons under a content-width box was the inset the cinematic hero is not._ → Set aside: breaking out of `PageWidth` with negative horizontal margin while staying inside it — still capped at `max-w-300`; dropping `main`'s top padding globally — would pull every page's first block up; putting wayfinding back under the frame — what 164 first required, but then the plate and the links read as two bands.

**186. HM1 wayfinding sits in the positioning sentence** — ~~the identity line is rewritten so Who we are, Grants & Opportunities and Mawred Network are phrases in the prose, each a `border-b-2 border-black` link at the same `text-2xl font-bold` as the rest of the line.~~ **Revised at the HM1 positioning-copy unit:** the line is two sentences plus "More about us →". Linked phrases are "support artistic creativity" → Production Awards, "encourage cultural exchange" → Network, "More about us →" → Who We Are, each a `border-b-2 border-black` link at the same `text-2xl font-bold` as the rest of the line. `HOME_POSITIONING` is an array of text/link parts; `HOME_WAYFINDING` and the `Btn` row under the sentence are gone. **Decisions 36, 42, 129 and 164 revised in place.** _A row of buttons under the sentence split identity from destination, which is the same two-band problem 164 already moved once. Mid-sentence `Btn` chrome would break the line; a 2px underline marks the routes without leaving the type._ → Set aside: keeping the buttons under the sentence — what 164's plate revision required, but they read as a second band; inline bordered chips — the permitted button treatment, but they would wrap as pills inside the largest type on the page; ~~weaving the routes without rewriting the published sentence — the current line does not name those three destinations~~ linking destination labels (Who we are · Grants & Opportunities · Mawred Network) as the phrases — the version this revision replaces.

**187. Home action buttons hug their label; HM4's heading sits clear of the cards** — HM5 Read more and HM6 All news take `self-start` in their `flex-col` parents so they do not stretch to the column. HM4's `CardGrid` takes `mt-8`; after collapse with the heading's `mb-4` the gap is 2rem. `CardGrid` accepts an optional `className`. **Decision 185 revised in place.** _A stretched button in a flex column reads as a bar, not an action; 1rem under Grants & opportunities put the title on the cards._ → Set aside: `w-fit` on `Btn` globally — would affect N4's connected pager; raising Block heading `mb` site-wide — other strips were not the problem.

**188. Home HM4 / HM5 / HM6 sit 5rem apart** — HM5 and HM6 take `looseAbove`: client view raises those two frame tops from 3rem to 5rem, so the gaps HM4→HM5 and HM5→HM6 collapse to 5rem. HM2→HM4 stays 3rem. Review view is unchanged. **Decision 175 noted.** _`text-4xl` section titles made the site-wide 3rem read as one slab once the three strips sat in a row; 5rem is the same opt-in as `tightBelow`, the other way._ → Set aside: raising the site-wide 3rem — decision 175 already set 4rem aside; wrapping the three in a gapped stack — would fight the frame margins rather than use them.

**189. Listing Card titles are h2 at Block-h3 size; listing grids are three columns** — shared `Card` titles are `<h2 className="text-2xl font-bold">`, matching Block's h3 visual, not Block's h2 (`text-3xl`) and not `SectionHeading` (`text-4xl`). No `block-heading` class — review chrome should not hide card titles. `titleClassName` is gone; HM4 drops its override. ~~Listing and related `CardGrid`s (PA4, N3, AB9, PC2, N13, AB11) pass `cols={3}`; HM4 already did.~~ **Revised at the publications listing-rows unit:** PA4 and PC2 leave `CardGrid` for `LibraryRow`s (decision 191). ~~Remaining listing and related `CardGrid`s (N3, AB9, N13, AB11, HM4) stay three columns.~~ **Revised at the news listing-rows unit:** N3 and N13 leave `CardGrid` (decision 192). ~~Remaining listing and related `CardGrid`s (AB9, AB11, HM4) stay three columns.~~ **Revised at the past-initiative listing-rows unit:** AB9 and AB11 leave `CardGrid` (decision 194). Remaining listing `CardGrid` is HM4. AB7 and N14 stay four. Related-strip Block headings stay `h2` at `text-3xl`, so card titles sit at the same rank as "Related posts" / "Related publications" / "Other past initiatives" — the cost of verbatim reuse (standing constraint 4), not a `titleAs` variant. **Decision 167 revised in place.** _Four-column listing cards were cramped once titles moved to `text-2xl`; a card that looks different on two pages is a bug._ → Set aside: `titleAs` so related cards stay `h3` — would fork the primitive standing constraint 4 forbids; matching Block h2 (`text-3xl`) — would compete with section titles; keeping four columns and only bumping type — titles would wrap harder in a narrower cell; keeping PA4 / PC2 on three-column cards — the version this revision replaces.

**190. News category is a facet, not a tab row** — N1 drops All / Announcements / News & Events tabs. Category and Programme sit as two FilterBar facets; “All” is the unfiltered default, not a value. `NewsLandingPage` no longer owns a category index; `filterNewsPosts` no longer cuts by tab. N2 chips any applied filter and no longer scopes the count to an open tab. **Decisions 27, 28, 78, 115, 116 and 124 revised in place.** _Category is a cut on one feed, the same job Programme already does, and two short values do not warrant tabs (decision 19)._ → Set aside: keeping the tabs because decision 27 called category the primary dimension — primary does not mean a different control; stacking the two categories as labelled sections — decision 19, but the feed is one reverse-chronological list, not two option groups.

**191. Publications library listings are thumb DirRows** — ~~PA4 drops the three-column `CardGrid`. Each record is a `DirRow`: compact Cover (`label="cover"`) in `leading`, title, languages as muted text (the same meta the card subtitle carried).~~ **Revised:** PA4 drops the three-column `CardGrid`. `LibraryRow` is its own row — compact Cover (`label="cover"`, `w-24`) beside a stacked column: author and languages as muted `text-xs`, title as `h2` at `text-2xl font-bold` between them. It leaves `DirRow`, whose single-line name + trailing meta cannot hold that stack. Nine sampled rows and the overflow line stay (decisions 104, 108). PC2 imports `LibraryRow` so related titles match the library (standing constraint 4). **Revised at the PC2 no-thumb unit:** PC2 omits the compact Cover (decision 204). Author is a stand-in (`Author name`) — the live listing and detail do not publish it (decision 105). ~~N3 / N13 stay on `CardGrid` until the News listing-rows unit.~~ **Revised at the news listing-rows unit:** N3 / N13 leave `CardGrid` (decision 192). **Revised at the two-up listings unit:** PA4 wraps those rows two-up from `lg` (decision 196); PC2 stays stacked. **Decisions 104, 108, 166 and 189 revised in place.** _A publication in a directory is a named record with a cover; three-column cards were the leftover of listing `Card`. Trailing the language on a `DirRow` put the meta where a directory badge goes, not where a catalogue line reads._ → Set aside: keeping PA4 / PC2 on three-column cards — would leave the library as the only directory still on a grid; forcing the stack through `DirRow` — would overload a single-line primitive MN5 / AB12 / HM6 still need; languages as `Badge`s — would invent chip chrome the card never had; omitting the author line until names exist — hides the slot the spec already requires.

**192. News listings are thumb DirRows** — ~~N3 takes the first visible post as an HM5-style lead: `Banner` left, title as `h2` at `text-2xl font-bold` (not `SectionHeading`), date and category as `Badge`s, Read more. Remaining posts are `DirRow`s with compact Cover (`label="image"`) in `leading`. `NewsCard` is that row; N13 reuses it, not the lead. Excerpt stays a `slots` caption on the lead only. One match → lead only; empty state, filter bar, count row and pager are unchanged.~~ **Revised:** every N3 item is a thumb `DirRow` — compact Cover (`label="image"`) in `leading`, title, date and category as `Badge`s. `NewsCard` is that row; N13 reuses it. No featured lead. Excerpt is an available unused field with no home on a row, so `slots` left the news landing. Empty state, filter bar, count row and pager are unchanged. HM6 stays the image-less `DirRow` (decision 43). ~~**Decisions 43, 189 and 191 revised in place.**~~ **Revised:** `NewsCard` leaves `DirRow` for the same stacked row as PA4 — compact Cover (`label="image"`, `w-24`) beside date and category as muted `text-xs`, title as `h2` at `text-2xl font-bold` between them. Date above, category below. N13 reuses that row. **Revised at the editorial reading-column unit:** N13 omits the compact Cover (decision 205). HM6 stays the image-less `DirRow` (decision 43). Excerpt still has no home on a row. **Decisions 43, 189 and 191 revised in place.** _A dated archive is a list of named records; trailing date and category as badges put the meta where a directory chip goes, not where a catalogue line reads._ → Set aside: keeping N3 / N13 on three-column cards — would leave News as the only dated listing still on a grid; a featured lead then rows — the version the last revision replaced; forcing the stack through `DirRow` — would overload a single-line primitive HM6 still needs; date and category as `Badge`s — the version this revision replaces.

**193. Block catalog is a second review index** — `/wireframes/blocks` mounts every live block file once, grouped by kit, with a jump row of code links. Catalog chrome (code, label, usage links) is always-visible page content, not `.code` / `.block-label`, so it survives `codes-off`. One representative sample per code from existing page configs; unused N10/N11 are included with “not used on any page yet”; retired codes with no file (C3, HM3, HM7, HM8, N5, N8, N12) stay out. The route appears as a card on `/wireframes` (`indexLabel`) and is not in the rail nav. No `statesKey` — the panel keeps the global review-chrome toggle only. **Decision 56 noted.** _`/wireframes` lists screens; this lists blocks. Review chrome would hide the labels the catalog exists to show._ → Set aside: auto-scanning `components/blocks/` — would couple the page to the filesystem; putting it in the rail — it is a review tool, not a site page; one entry per programme variant or panel state — the catalog is a gallery, not a state matrix.

**194. Past initiative listings are thumb rows** — AB9 drops the three-column `CardGrid`. `InitiativeCard` is the same stacked row as PA4 / N3: compact Cover (`label="image"`, `w-24`) beside title as `h2` at `text-2xl font-bold` and the one-line teaser as muted `text-xs` below. It leaves `DirRow`. Twelve sampled rows; no filter bar and no pagination (decision 52). AB11 imports `InitiativeCard` so related titles match the directory (standing constraint 4). **Revised at the two-up listings unit:** AB9 wraps those rows two-up from `lg` (decision 196); AB11 stays stacked. **Decision 189 revised in place.** _A retired initiative in a directory is a named record with an image; three-column cards were the leftover of listing `Card`._ → Set aside: keeping AB9 / AB11 on three-column cards — would leave Past Initiatives as the only named-record listing still on a grid; forcing the stack through `DirRow` — would overload a single-line primitive MN5 / AB12 / HM6 still need; inventing a date or category line to match N3 exactly — the spec is image · title · teaser, and optional fields render by omission.

**195. Past Initiatives sits under Grants & Opportunities** — the rail item moves out of About and into the Grants & Opportunities dropdown, after Abbara. Crumbs follow (`Home › Grants & Opportunities › Past Initiatives`). The URL stays `/about/past-initiatives` — the About kit (AB9–AB11) still owns the page; only the nav parent changes. On that route the Grants group is the active section. _Retired programmes read as the closed end of Grants, not as an About identity page._ → Set aside: keeping it under About because the spec listed it as an About sub-page — spec §3.4 is the page-type, not the nav parent; moving the URL under `/programs/` — would mix a simple listing/detail into the programme kit without changing the blocks.

**196. PA4 and AB9 listings are two-up from `lg`** — the library and past-initiative directories wrap their existing rows in `grid-cols-1 lg:grid-cols-2 lg:gap-x-8`. Item treatment is unchanged (decisions 191, 194). PC2, AB11, N3 and N13 stay one stacked column. Column gap only — the row's `border-b` still divides the pairs. **Decisions 191 and 194 revised in place.** _A compact cover plus stacked meta is half a content-column wide once the rail is in; a full-width stack under-uses the listing._ → Set aside: two-upping PC2 / AB11 — related strips are three items; a 2+1 last row is leftover density, not a directory; two-upping N3 / N13 — a dated archive browses as a list; using `md` — chrome's bigger-screen break is `lg` (decision 183); gap on both axes — would fight the row's `border-b`.

**197. Footer newsletter is the form; socials are named accounts** — `SiteFooter` drops the Newsletter popup. ~~The email Field and Subscribe button sit in the footer row (full size, decision 180).~~ **Revised:** "Subscribe to our newsletter" sits on its own line above the email Field and Subscribe button (full size, decision 180). ~~Social is a flex of outbound links — Instagram `@culture_resource`, Facebook `CultureResource` — not a labelled placeholder box.~~ **Revised:** Social is a flex of outbound links labelled Instagram and Facebook. Handles stay in the href, not on the page. Not a labelled placeholder box. Copyright stays. The footer remains a wrapping `justify-between` row under `main`. `SiteFooter` is a server component again. **Decision 179 revised in place.** _A page-width footer can hold the signup; hiding it behind a popup was the rail-width workaround 179 already left behind. ~~Handles name the accounts;~~ Network names are the row; the href names the account. Icon squares would be imagery. A one-word "Newsletter" beside the field named the slot, not the ask._ → Set aside: keeping the popup because 154 retired the inline email row — that retirement was for the 240px rail, not the page-end footer; icon boxes like the retired N12 — share targets are unnamed slots, these are named networks; printing the handles beside the labels — the version this revision replaces; adding YouTube / X until those networks are named — the two named ones are the row; keeping "Newsletter" inline with the field — the version the previous revision replaced.

**198. N12 Share retires** — ~~posts close on body (and any roster/quotes) then Related posts.~~ **Revised:** the announcement closes on body (and any roster/quotes) then Related posts; the editorial post closes on page-level prev/next (decision 209). `N12Share.tsx` and `NEWS_SHARE_TARGETS` are deleted; both detail shells and the block catalog drop the mount. The N12 code is not reused (same precedent as C3, HM3). **Decision 193 revised in place.** _The live eight-target bar was already a copy/QA call; unnamed icon squares are imagery, and the footer already names the accounts. Native share / copy-link is browser chrome, not a page block._ → Set aside: four unnamed targets plus copy-link — leftover of the live bar; leaving the file unused like N10/N11 — those wait for a post shape that is not sampled; share has no remaining job.

**199. Editorial post is a sticky two-column** — `/news/editorial-post` splits the main: crumb stays in `PageHeaderBand` (`hideTitle`); ~~the H1, N7 (category · date) and N6 sit in a left column that is `lg:sticky`; N9 is the right column; N13 stays full-width below as the main-area close.~~ ~~**Revised:** N7 and N6 sit in a left column that is `lg:sticky`; the H1, N9 and N13 are the right column (decision 205).~~ **Revised:** N6 sits in the left column; N7 wraps the H1 in the reading column — category above, date below (decision 206). ~~N13 sits under the body in that column (decision 205).~~ **Revised:** N13 leaves this shell; page-level prev/next closes it (decision 209). Type stays on the existing scale (`text-5xl` H1, `text-xs` meta). N6 is square in that column; full-width on the announcement it stays `aspect-video`. Sticky only from `lg` so a stacked layout does not pin the image over the body. **Decision 152 revised in place.** _The Figma (Mawred, node 424:488) puts identity in a persistent left stack and reading in the right; a full-width band H1 then image then body is the leftover of the announcement stack._ → Set aside: applying the split to `/news/post` too — that page still has rosters and quotes that need the full column; ~~keeping the H1 in the band and only moving image + meta — the title belongs with the image in the Figma.~~ **Revised:** putting the H1 with the image — the version decision 205 replaces.

**200. N5 and N8 retire** — `N5PrevNext` and `N8KeyDetail` are deleted; both post shells and the block catalog drop the mounts. `keyDetail` leaves the post config; `edge` leaves `WireframeState` (decision 122 revised). Codes not reused (same precedent as N12). **Revised:** the editorial post gains a page-level ← prev / next → pair that is not N5 (decision 209); the N5 code stays unused. **Decisions 57, 119, 122, 123, 193 revised in place.** _Prev/next as a titled block was a template the sample couldn't populate (123); the key-detail line duplicated the event the body already names. Figma's editorial has neither._ → Set aside: leaving them unused like N10/N11 — those wait for a post shape that is not sampled; these have no remaining job.

**201. Publication detail is a sticky two-column** — `/publications/research/publication` splits the main the same way as the editorial post (decision 199): crumb stays in `PageHeaderBand` (`hideTitle`); ~~the H1, cover and PC1 metadata sit in a left column that is `lg:sticky`; the abstract is the right column (`flushTop`);~~ **Revised:** cover and PC1 metadata sit in a left column that is `lg:sticky`; the H1, abstract and PC2 are the right column (decisions 202, 203). **Revised:** that split holds from `lg`; below it the six slots reflow (decision 208). Type stays on the existing scale (`text-5xl` H1). The cover stays portrait `aspect-3/4` (decision 166), not square — square was N6's column treatment. Download buttons stay with the metadata on desktop. Sticky only from `lg`. **Decision 152 revised in place.** _Identity left, reading right; a full-width band H1 then a 190px cover beside the fields was the leftover of the first port (decision 15). ~~MN6 keeps that older split.~~ **Revised:** MN6 takes the same split (decision 210)._ → Set aside: making the cover square like N6 — a report is a portrait object; moving downloads into the reading column — they are record actions, not prose; ~~keeping the H1 in the band — the title belongs with the cover, same as 199.~~ **Revised:** putting the H1 with the cover — the version decision 202 replaces.

**202. Publication H1 sits above the abstract** — ~~the title moves to the right column, above the body. Left is cover + metadata + downloads only.~~ **Revised:** from `lg` the title sits in the reading column, above the body; left is cover + metadata + downloads only. Below `lg` the title follows the cover (decision 208). **Decision 201 revised in place.** _A portrait cover plus a `text-5xl` title in the same column is taller than the reading column; the title belongs with the text it names._ → Set aside: squaring the cover so the left stack could keep the title — would drop the publication portrait (decision 166); putting the H1 back in the band — would separate it from both the cover and the abstract.

**203. PC2 sits in the reading column** — Related publications move under the abstract, still stacked `LibraryRow`s (decision 196). Not a full-width close. **Revised:** from `lg` PC2 stays in the reading column; at every breakpoint it is last (decision 208). **Decision 201 revised in place.** _The reading column is now title + abstract + related; a full-width strip under a sticky cover repeated the leftover announcement close. Three rows fit the half-column._ → Set aside: keeping PC2 full-width to match N13 — N13 on the announcement still closes a stacked news page; ~~on the editorial post it now sits in the reading column too (decision 205).~~ **Revised:** N13 left the editorial post (decision 209).

**204. PC2 omits the compact Cover** — related rows keep author · title · languages; the thumb is gone. PA4 still shows it. `LibraryRow` takes `cover` (default on). **Decisions 191 and 203 revised in place.** _The strip sits in the reading column next to a full portrait; repeating three compact covers beside that cover is leftover directory chrome._ → Set aside: keeping the thumbs so PC2 matches PA4 verbatim — standing constraint 4, but this is the same page-type's related strip, not a foreign card, and the cover is already on the page.

**205. Editorial post matches the publication reading column** — ~~H1, N9 and N13 sit in the right column;~~ **Revised:** H1 and N9 sit in the right column; N13 leaves this shell (decision 209). ~~N7 and N6 stay sticky left.~~ **Revised:** N6 stays sticky left; N7 moves to the reading column (decision 206). N13 omits the compact Cover (`NewsCard` `cover`, default on for N3). Announcement N13 stays full-width below the stacked post, still without thumbs — one block. **Decisions 199, 192 and 204 revised in place.** _The publication split (202–204) is identity left, reading + related right; a left-column title next to a square image had the same height problem as the portrait._ → Set aside: applying the split to `/news/post` — still has rosters and quotes that need the full column; keeping N13 thumbs on the announcement — would make one block look different on two pages.

**206. Editorial N7 wraps the H1** — category above the title, date (and optional byline) below it. Left column is N6 only. Announcement N7 stays the inline category · date strip — no children, no stack. The page still owns the H1; N7 takes it as `children`. **Decisions 199 and 205 revised in place.** _Category is a kicker for the title it names, not a label on the image; date belongs with the text it timestamps._ → Set aside: stacking category and date above the title — would bury the date; keeping N7 on the left with the image — leftover of the identity stack once the title had already moved.

**207. PC1 drops Country and Theme** — those two KV rows leave the record. Author and Type stay as fill bars; Language and Year still carry values. PA2 still filters on Country and Theme. **Decision 105 revised in place.** _They are library facets, not record identity; empty bars on the detail page restated the listing's taxonomy without adding anything the record publishes._ → Set aside: dropping Author and Type too — those still name gaps the listing and record both withhold; inventing Country/Theme from the facets — the live page still does not publish them.

**208. Publication detail reflows by breakpoint** — cover, meta, downloads, title, abstract and PC2 are separate grid items. Mobile stacks cover → title → meta → abstract → downloads → related. From `md`, a compact portrait (`w-24`) sits beside the title; the rest stay stacked in that order. From `lg`, the current two-column treatment stays: sticky left is cover + meta + downloads; right is title + abstract + PC2. **Decisions 201, 202 and 203 revised in place.** _The desktop identity stack is too tall to read on a phone; title after cover and downloads after the abstract is the content order. ~~`md` is this page only — chrome's bigger-screen break stays `lg` (decision 183).~~ **Revised:** `md` is the record-detail tablet pair (publication and MN6, decision 210); chrome's bigger-screen break stays `lg` (decision 183)._ → Set aside: using `order` on the existing two columns — cannot put title between cover and meta without splitting the identity stack; applying the tablet pair at `lg` — would drop the sticky identity column.

**209. Editorial post closes on page-level prev/next** — `/news/editorial-post` drops N13. A `flex justify-between` ← prev / next → pair sits last in the shell, above the site footer. Not N5: labels only, no neighbour titles, no block code. The editorial `PageWidth` is `min-h-[calc(100dvh-<main padding>)]` so `mt-auto` parks the pair at the bottom of the viewport when the post is short. ~~`main` is a column flex so the shell can `flex-1`.~~ **Revised:** `main` stays a block; a column-flex `main` made every `PageWidth` shrink-to-fit because `mx-auto` disables stretch. Announcement keeps N13. Both labels link to the announcement route — the sample still cannot name real neighbours (decision 123). **Decisions 198, 199, 200, 203 and 205 revised in place.** _Related posts restated the archive next to a sequential pair; the pair is scaffold, the same way the crumb is. Filling the column is what keeps the pair off the middle of a short page._ → Set aside: restoring N5 with placeholder titles — the sample still cannot name neighbours (123); putting the pair in the reading column — would sit mid-page beside a taller image; sticky `bottom-0` — would overlay the body on a long post; making `main` a column flex — shrinks every page that uses `PageWidth`.

**210. MN6 record reflows by breakpoint** — cover, meta (Country, Discipline), title, description and engagements are separate grid items. Mobile stacks title → cover → meta → description → records. From `md`, a compact portrait (`w-24`) sits beside the title; the rest stay stacked. From `lg`, sticky left is cover + meta; right is title + description + engagements. The band is crumb only (`hideTitle`); MN6 renders the H1 from `record.name`. **Decisions 152, 201 and 208 revised in place.** _The leftover photo-left / fields-right split (decision 15, kept at 201) was the same leftover the publication detail already left. Title-first on mobile because the name is the record; a publication is identified by its cover (decision 208)._ → Set aside: cover-first like 208 — a network record is named, not covered; keeping the 190px flex split — leftover of the first port; putting the H1 back in the band — would separate it from the description it names.

**211. MN6 engagement title matches listing rank; involvement stands alone** — the project title is `h2` `text-2xl font-bold`, the same size as PC2 / PA4 listing titles (decision 189). The involvement badge sits on its own row; programme · track · year wrap on the line below. **Decision 111 noted.** _A `text-sm` strong under a wrapping meta row read as a caption, not as the engagement; the badge is the involvement type, not a prefix on the programme line._ → Set aside: keeping badge + meta on one wrapping row — the long track name crowded the badge; leaving the title at `text-sm` — too small next to the reading-column H1 and the related-publication titles it now sits beside.

**212. MN6 drops the back button** — no ← Back to the directory. The crumb already names Mawred Network. **Decision 24 noted.** _A second exit restated the band; publication and editorial detail pages have none._ → Set aside: linking it — still a second control for a path the crumb owns.

**213. C4 `note` uses the same grey fill as `highlight`** — the bold paragraph becomes a `bg-neutral-200` padded band, the same inner treatment as C4 `highlight`, S5, S6 and C6 (decision 144). Fields stay split: `note` is the caution, `highlight` is the labelled reassurance (decision 91). Production Awards and Abbara pick up the same treatment. **Decision 91 revised in place.** _A bold line under a document list read as leftover warning type, not as a provision; the grey fill is what this kit uses for provisions that matter more than their length (decision 177)._ → Set aside: folding the Stand for Art caution into `highlight` — would lose the label-vs-caution split 91 set; inventing a label for the note — no source label exists; leaving it bold — what 91 originally rendered, but it no longer matches every other application caution.




