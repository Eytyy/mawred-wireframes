Where the content on each populated page came from, block by block. One section per page-type, appended as each is populated.

Records four things per block: the source it came from, what was shortened or rewritten, what was split out of a single live passage into more than one block, and what was left as a gap. Read it when a figure or a date on a wireframe needs checking, or when the next unit needs to know how the last one handled the same shape.

Copy is being rewritten, not migrated — everything below is wireframe-length adaptation for structure and volume, never final wording.

---

## Production Awards

Sources: `content/programs/audit-production-awards.md` for substance; the live page (`mawred.org/artistic-creativity/production-awards/?lang=en`) for prose length and the current round's dates — open call 17 Aug 2026, deadline 19 Oct 2026 16:00 Beirut, results end of March 2027.

| Block | From | What happened to it |
| --- | --- | --- |
| **C1** Overview | Audit tab 1 items 1–2 | The page H1 is the programme name from the route registry; the live "What is the Production Awards Program?" inner heading is dropped (decision 138). The intro paragraph and the scale line are compressed into two short paragraphs: launch year, the under-35 and Arab-origin conditions, the five disciplines in the live intro's order; then 25 grants a year, five per discipline, ~400 since 2004. The live birth-year range (January 1992–December 2011) is dropped — it changes every round. After C3 left this page (decision 158) that range has no scan card; it is only implied by C1's "under 35". |
| **C2** Impact figures | Audit tab 1 item 2 | Two of the three rails carry real figures — ~400 grants since 2004, 25 grants per year. Labels were re-cut to match what the audit actually sources: "years running" became "grants per year". **Gap:** no country count exists in the audit or on the live page, so that rail keeps its `00` placeholder. Live puts these numbers in the overview prose, not a figures strip. |
| **S1** Programme timeline | Audit tab 1 item 3 | Open call / Deadline / Results leave C3 for S1 (decision 145): 17 August 2026 · 19 October 2026, 16:00 Beirut · End of March 2027. Date-only stages — no fill under the rail (decision 126 revised). The live lead "accepted and evaluated once a year as per the dates below" is implied by the rail, not restated. |
| **C3** At-a-glance | — | **Dropped on this page (decision 158).** The three leftover cards — eligibility, disciplines, grant period — repeated C1, C5 and C10. The component stays; Abbara is the remaining consumer. |
| **C4** How to apply | Audit tab 1 item 6 | The four portal steps as numbered cards in a wrapping row (decision 141), each with a one-line detail taken from the live step text. Arabic-except-English (Guidelines terms) lands on step 3; incomplete-not-reviewed lands on step 4. The bold "read the guidelines first" warning becomes the block's `note`, below the cards rather than above them. The repeat apply button stays below the note. Sits in Application after C7 (decision 153). |
| **C5** Forms of support | Audit tab 1 item 7 | Five discipline records replacing four generic ones, each with a coverage line carrying the live scope (visual arts and music in particular list many more forms than a one-liner holds) and the real ceiling in the amount chip. Amounts are as audited and read "Up to €8,500" etc., because the live page states them as maxima, not fixed grants (Performing Arts, Visual Arts and Music & Sound Art €8,500; Literature €6,500; Cinema €13,000). Every record now carries an amount, so C5's "record without an amount" hint no longer shows on this page — it still shows on Abbara and Stand for Art. Sits in Overview after S1 (decision 153). |
| **C8** Conditions / exclusions | Audit tab 1 item 8 **and** tab 2 items 1–2 | Three live passages fold into one block: the About-tab exclusions, the Guidelines application terms, and the Guidelines eligibility detail (re-application, who submits, under-18). Eight items, shortened from running prose. The re-application rule names both live conditions. Who-submits is one line instead of five role bullets. Under-18 is "additional documents" instead of the three named files. Ethnicity/citizenship is not restated here — C1's "of Arab origin, wherever they live" stands for it. |
| **C9** Selection | Audit tab 2 item 3 | Jury structure in the block label and lead sentence — independent, three members per discipline, changed yearly. The four selection criteria become the list, in the live page's own terms. The live "once results are announced, agreements are sent" line that opens this section is not repeated — C10 starts at the installments. |
| **C10** Contract terms | Audit tab 2 item 4 | Eight items from one long prose passage: the 70/30 installments with the reports-and-documentation trigger on the second, cancellation if the agreement isn't signed within 45 days, 18-month completion (this was also C3's grant-period card), the one-time extension (six months, one year for cinema, requested 30 days ahead), the IP-rights document, the two co-signing guarantors, cancellation and clawback with the withheld final installment, and the logo and credit obligation. Cancellation, clawback and the withheld installment were three live clauses, merged into one item. Dropped from the live passage: the agreement "details scope, conditions and duration" (generic); extension requests go to awards@ (C12 already has the address); "without citing cause" on refusal; the exact credit phrase; the publicity/news citation line. |
| **S2** Downloads | Audit tab 1 items 4–5 | Five per-discipline application form previews in the live page's order plus the required-documents list, all PDF. Live puts these on the About tab, above How to apply; the wireframe puts them at the end of Application, after the contract terms. |
| **S3** Past beneficiaries | — | Round labels become real years (2025, 2024, 2023). **Gap:** beneficiary names and countries stay placeholder rows — they aren't in the audit and come from Network records per spec §B6. Block stays behind the `offered` state, as its own section when shown (decision 153). |
| **C11** FAQs | Audit tab 3 | Nothing to populate. The live FAQ tab in English carries only a pointer to the Arabic page, so the fill bars stand for content that doesn't exist. Hint sharpened to say so; supplying English FAQs is a client call. Questions render as a plain accordion — no item borders, `text-base font-bold` (decision 142). |
| **C12** Contact | Audit in-template footer | awards@mawred.org. Live also prints this under the empty FAQ tab; the wireframe has it once, as its own section. |

**C7** remounts at the top of Application (decision 153); `dest` on the config names `apply.mawred.org`, the URL the audit records. Overview is C1–C2–S1–C5; Application is C7–C4–C8–C9–C10–S2; S3 its own section when `offered`; then FAQs / Contact.

### Live page → wireframe (source for a later client note)

The live page is three tabs (About · Application Guidelines · FAQs) plus a footer contact line. The wireframe is four always-on sections (Overview · Application · FAQs · Contact) plus optional Past. Copy is rewritten, not migrated — this table is the move/compress record, not final wording. Live source: [Production Awards](https://mawred.org/artistic-creativity/production-awards/?lang=en).

#### About the Program

| Live passage | Wireframe | Move / compress |
| --- | --- | --- |
| Crumb Home / Artistic Creativity / Production Awards | Page-header band | Nav parent is Grants & Opportunities, not Artistic Creativity — IA change, not a copy cut. |
| H1 "Production Awards" | Page-header band | Same heading, page-level (decision 152). |
| Inner heading "What is the Production Awards Program?" | — | Dropped (decision 138). The H1 is the heading. |
| Launch 2004; under 35; born January 1992–December 2011; Arab region or diaspora; five disciplines | C1 paragraph 1 | Birth-year range dropped (changes every round). "Of Arab origin, wherever they live" stands for origin + residence; "regardless of ethnicity and citizenship" is not restated. |
| Up to 25 / year, five per discipline; ~400 since 2004 | C1 paragraph 2 + C2 | Same facts. C2 is a figures strip the live page does not have. C2's countries rail is a published-data gap (`00`). |
| "Accepted and evaluated once a year as per the dates below" | S1 (implied) | Not restated as a sentence. |
| Open call 17 August 2026 · Deadline 19 October 2026, 16:00 Beirut · Results end of March 2027 | S1 | Moved off the About-tab prose headings onto a rail (decision 145). |
| Five application-form preview PDFs | S2 | Moved from About into Application, after contract terms. |
| Required-documents PDF | S2 (last file) | Same move. |
| How to apply — four portal steps | C4 | Moved from About into Application. Arabic-except-English and incomplete-not-reviewed pulled in from Guidelines and attached to steps 3 and 4. |
| Bold warning: read the guidelines first | C4 `note` | Below the steps, not above. |
| Five disciplines: coverage + ceiling (€8,500 / €6,500 / €13,000) | C5 ("Available grants") | Stays in Overview. Coverage slightly shortened; ceilings kept as "Up to". |
| Important notes: no reproductions; websites, workshops, org support, academic/research, TV series | C8 items 1–2 | Moved from About into Application Conditions. |
| APPLY NOW (About tab) | C7 (+ C4 repeat) | One button at the top of Application, repeated under the steps. Destination named as `apply.mawred.org`. |

#### Application Guidelines

| Live passage | Wireframe | Move / compress |
| --- | --- | --- |
| **Who is eligible** (titled section) | C1 one line + C8 bullets | No titled eligibility block. The live section is the largest compression on the page. A later S4 ("Who is eligible") is the reserved shape if the client wants it back as a section. |
| Born January 1992–December 2011 | — | Omitted. Only implied by C1 "under 35". |
| Originate from an Arab country regardless of ethnicity and citizenship, resident or abroad | C1 | Compressed to "of Arab origin, wherever they live". |
| Past recipients: previous grant closed; two rounds passed (eligible for the third) | C8 item 5 | Both conditions kept, one bullet. |
| Who submits: cinema director · performing-arts director or choreographer · literature writer · music main musician / performer / composer / sound artist / producer who writes their own music · visual-arts main artist or curator | C8 item 7 | One line: "director, choreographer, writer, main musician, or main artist or curator, depending on the discipline." Music's longer role list is the cut. |
| Application entirely in Arabic except English fields | C4 step 3 | Moved onto the apply step, not restated in C8. |
| Uploads PDF, JPEG or PNG | C8 item 6 | Kept. |
| Incomplete applications disregarded | C4 step 4 | Moved onto the apply step. |
| One project per applicant per round | C8 item 3 | Merged with the no-equipment rule. |
| Complete within 18 months of signing; extension possible | C10 items 3–4 | Was also C3's "Grant period" card — that card is gone (decision 158). |
| Grant may not buy machinery or equipment | C8 item 3 | Merged with one-project-per-round. |
| Funds spent in, or for the benefit of, the Arab region | C8 item 4 | Kept. |
| Under 18: birth certificate · guardian approval letter · guardian ID/passport | C8 item 8 | Compressed to "supply additional documents." The three files are the cut. |
| **How are grantees selected** | C9 | Kept as its own block. |
| Independent jury of three per discipline, new each year | C9 label + lead | Kept. |
| Criteria: quality and relevance · originality · practicability of the projected budget · feasibility of completion by the deadline | C9 list | Kept in the live page's terms. |
| **Contractual procedures and obligations** | C10 | Kept as its own block; eight bullets from a long prose passage. |
| Agreement details scope, conditions, duration | — | Dropped as generic. |
| IP-rights document | C10 item 5 | Kept. |
| 70% on signing · 30% after financial and narrative reports and documenting material | C10 item 1 | Kept, including the reports trigger. |
| Unsigned after 45 days → grant cancelled | C10 item 2 | Kept. |
| Incomplete on time: extension or cancellation, Mawred decides | C10 item 7 | Merged with clawback and the withheld installment. |
| Cancellation covers only spend before the cancel date; remainder returned | C10 item 7 | Merged (see above). |
| Second installment withheld if reports or commitments unmet | C10 item 7 | Merged (see above). |
| One extension only, requested ≥30 days before the end; 6 months, or 1 year for cinema; email awards@; may refuse without citing cause | C10 item 4 | Duration and 30-day notice kept. Email (already C12) and "without citing cause" dropped. |
| Two guarantors co-sign; at least one an expert who knows the project | C10 item 6 | Kept. The live "pledge to guarantee implementation" is implied by co-sign. |
| Logo + exact credit phrase in all outputs; grant cited in all news/publicity | C10 item 8 | Compressed to "logo and the agreed support credit." Exact phrase and the publicity line are the cut. |
| APPLY NOW (Guidelines tab) | C7 | Same button as the About-tab CTA — not repeated a third time. |

#### FAQs + footer

| Live passage | Wireframe | Move / compress |
| --- | --- | --- |
| "AVAILABLE ONLY IN ARABIC" | C11 fill bars | Gap kept visible. Supplying English FAQs is a client call. |
| "For further questions… awards@mawred.org" (under FAQ) | C12 | Once, as the Contact section. |
| Same address in the in-template footer | C12 | Not duplicated. |

Checked against the live page after the first pass, and corrected: the re-application rule had been recorded as a gap when the live Guidelines tab states it in full; the amount chips read as fixed grants rather than ceilings; and four items said more than the source did — the reproduction exclusion had gained restaging and re-editing, two selection criteria had gained qualifiers, the second installment was triggered by completion rather than by reports, and the 45-day window had lost its consequence. The C5 coverage lines had also been cut so far back that they understated what the grants fund. Every one is now the live page's own substance. Re-checked 2026-09-06 when C3 left the page (decision 158): the three leftover cards were the only remaining duplicates of C1 / C5 / C10.

---

## Wijhat

Sources: `content/programs/audit-wijhat.md` for substance; the live page (`mawred.org/artistic-creativity/wijhat/?lang=en`) for prose length and dates. Every audited figure was confirmed against the live page before writing.

**The round dates carry no year.** The live page states "16 January at 16:00 Beirut time" and "announced on 28 February" with no year anywhere — Wijhat runs a fixed calendar that repeats, unlike Production Awards' single dated round. S1 renders the dates as the source states them, year-less, with the round in each stage label. No year was inferred. The lead says so.

| Block | From | What happened to it |
| --- | --- | --- |
| **S4** Page-top summary | Audit pre-tab block | Renders after C1 + C2, not at page top (decision 139). The live page's four headings become the four cells, in its order — Who is it for · For what purpose · From where to where · The grant — each with its own bullet list. The two cells whose live text has a lead-in keep it: "To take part in:" and "Up to €7,000, to cover:". The ceiling is stated as a ceiling. The arrows graphic is dropped — per-program logos are out (audit §1). |
| **C1** Overview | Audit tab 1 items 1–2, 4 | The page H1 is the programme name from the route registry; the live "What is Wijhat?" inner heading is dropped (decision 138). Three short paragraphs compressed from four live ones: the problem framing (travel funding, visas, networking) and the programme as the response, up to €7,000, travel within and outside the region; then 25–30 grants a year across three rounds with the partial-living-expenses caveat in the live page's own terms — travel support, not a living allowance; then the no-fixed-duration rule and that only the stated activity's period is covered. Both caveats fit C1, so neither needed a home elsewhere. |
| **C2** Impact figures | Audit tab 1 item 2, §1 | Four rails, labels re-cut to what the audit actually sources: 25–30 grants a year and 3 rounds a year carry figures. **Gaps:** no cumulative grant total and no country count exists in the audit or on the live page, so those two rails keep `00`. |
| **S1** Programme timeline | Audit tab 1 item 6 | The three rounds as six chronological stages — Round 1 deadline · Round 1 results · Round 2 deadline · Round 2 results · Round 3 deadline · Round 3 results — on the same rail Abbara uses for Open / Deadline / Results (decision 145). Dates are year-less — see above. Short lead: year-round intake, repeating calendar, dates carry no year. No C3 on this page after the move. Date-only stages, no fills (decision 126 revised). |
| **S6** Which round | Audit tab 1 items 7–8 | Two live passages fold into one block. The lead states the two-date rule the live page spells out: travel must fall after the round's results, the application must arrive before its deadline. The three rows turn that into the answer the block's question asks for, naming the earliest round a given travel date can use; the hint says later rounds stay open for later travel, so the rows can stay short without implying exclusivity. The three deadline rules — no early announcement, screening starts immediately after the deadline, a late application rolls to the next round if still eligible — land here as notes rather than in C8 (decision 89). S1 ends Overview; S6 starts Application after C7 (decision 145 revised, 153). |
| **S5** Application paths | Audit tab 2 items 2–5 | The other judgement call of this unit. The two live tracks share ~80% of their content, so the shared core carries everything common — the portal register/activate/log-in/select-Wijhat sequence, the save-and-return form, and stating the travel's purpose and its fit with the programme's aims — and each path carries only what actually differs, plus its own document list (decision 88). Individual: the individual form, and describing the schedule, activities and expected outcomes. Group: registering on behalf of the group, the group form, and all the group's travel costs in one itemised budget. The two paths render as tabs, core above, nested C4 cards in a connected panel (decisions 143, 146). The file format and size rules moved to C8 so the document lists stay lists of documents. The duplicated "express your purpose" line in the source is stated once. **Flagged:** the group document list omits the "detailed CV" line the individual list has — rendered as sourced, not repaired, because adding it would invent a requirement; whether the omission is deliberate is a client confirmation. |
| **C6** Highlighted note | Audit tab 1 item 3 | The visa recommendation letter, which had no owning block. Placed immediately after S5 so it answers the "grantees obtain their own visas" line the visitor has just read (decision 87). Still Application — C6 defaults to that section (decision 153). The letter goes to the relevant embassy, is arranged after the contract is signed, and the visa itself stays the grantee's responsibility. Grey fill, no 3px border (decision 144). |
| **C8** Conditions / exclusions | Audit tab 2 item 1, plus items 2 and 4 | Eleven items, shortened from running prose, following the same merge as Production Awards (decision 85): the eligibility rules from the Guidelines tab — Arab-country origin regardless of ethnicity or citizenship and resident or abroad, departure or arrival in the Arab region, individuals and groups, host may assist but not apply on behalf of an invitee, under-18 with written guardian consent — plus the 16-month / four-round re-application bar counted from the announcement date, that other Mawred grant recipients may still apply, and the three application terms both tracks share (one application per round for a single project, Arabic except the fields marked English, previous-works format and size limits) and that incomplete applications are discarded before the jury. |
| **C9** Selection | Audit tab 1 item 9 | Jury structure in the label and lead — independent, three members, changed each round, drawn from cultural actors and artists with recognised Arab and international expertise. Screening for eligibility and completeness precedes it in the same lead. The criteria list carries the two the live page names; the lead keeps its "mainly", because the live page states these as the main considerations rather than the full set. |
| **C10** Contract terms | Audit tab 2 item 6 | Nine items from one long prose passage. The installment split is a **range** — 60–80% on signing, 20–40% on the financial and narrative reports — not Production Awards' fixed 70/30. Then the 60-day signing window with cancellation, the actual-cost cap, 12 months from announcement, the grant tied to the stated activity with annulment and discretionary written-request repurposing, the withheld second installment, clawback, and the Wijhat and Culture Resource credit obligation. |
| **C7 / C12** Apply / Contact | Audit §1, in-tab and footer contact lines | C7 remounts at the top of Application (decision 153); `apply.mawred.org`. C12 is its own Contact section; wijhat@mawred.org. |
| **C11** FAQs | Audit tab 3 | Nothing to populate — the same English gap as Production Awards. The live FAQ tab carries only a pointer to the Arabic page. Questions render as a plain accordion (decision 142). |
| **S3** Past beneficiaries | — | Kept its year groups rather than splitting to rounds; three rounds a year would only be worth showing once records exist to fill them. **Gap:** beneficiary names and countries stay placeholder rows — they come from Network records per spec §B6. Block stays behind the `offered` state, as its own section when shown (decision 153). |

Wijhat has no C3, no C5 and no S2. The rounds live in S1 (decision 145); the covered-costs list lives in S4's "The grant" cell rather than in a single-record C5 (decision 86); and no downloadable forms are published — the budget form comes with the online application.

**Two client-side content-integrity bugs on the live page, flagged and not reproduced:** the clawback clause carries unresolved editor comments in the published copy ("((OR probably clearer… ))"), so C10 states the clause plainly instead; and the footer contact mailto points at `abbara@mawred.org` while the visible text reads wijhat@. Both suggest other pages may carry similar artifacts.

---

## Stand for Art

Sources: `content/programs/audit-stand-for-art.md` for substance; the live page (`mawred.org/stand-for-art/?lang=en`) for prose length. The audit and the live page agree line for line here — every figure, list and note in the audit is present on the page, including the six English FAQs — so nothing below is a gap the live page contradicts.

**The first programme whose English FAQ tab has real content.** Production Awards and Wijhat both carry only a pointer to the Arabic page; Stand for Art carries six Q&As in English, so C11 is populated for the first time and the "English FAQs don't exist" assertion stops being baked into the block.

**Confidentiality is the page's organising fact, not fine print.** The form is encrypted, routes to JotForm rather than `apply.mawred.org`, and can't be saved and reopened. It sits as C4's top highlight, not as a page-level C6 (decision 91).

| Block | From | What happened to it |
| --- | --- | --- |
| **C1** Overview | Audit tab 1 items 1–4 | The page H1 is the programme name from the route registry; the live "What is Stand for Art?" inner heading is dropped (decision 138). Three paragraphs from four live passages: what the programme supports and the 2016 launch after the study of regional risks; the five prioritised risk types and the two ways an applicant can be at risk, which the live page keeps as separate lists but which read as one paragraph at wireframe length; then the goals — free expression, living and working in safety and dignity — with the support stated as **temporary**, and the rolling intake. The "applications all year, no rounds, no deadlines" line lands here because there is no C3 to carry it. |
| **C2** Impact figures | — | No block. Stand for Art publishes no figures, and naming beneficiary counts would sit badly with the confidentiality the programme runs on. The existing hint says the absence is by design. C7 remounts at the top of Application (decision 153). |
| **C3** At-a-glance | — | No block. No dates, no rounds, no call status exist (audit §3). |
| **C6** Highlighted note | — | Removed from this page. The confidentiality note moved into C4's `highlight` (decision 91 revised). Wijhat and Abbara keep C6. |
| **C4** How to apply | Audit tab 2 items 1–3 | Three steps as numbered cards (decision 141): the encrypted form, Arabic except the fields marked English, attach the documents. The data-confidentiality note is C4's `highlight` at the **top** of the block — same grey fill as C6 (decision 144) — so an applicant sees it before the steps, not as a page-level callout (decision 91). Then the five required documents as C4's own `documents` list — the same render S5 was hand-rolling for Wijhat, now owned by C4 (decision 90) — still below the cards. The non-resumable-form warning stays the block's `note`, below the list, because it is a caution about how to prepare rather than a step: the form is completed in one sitting, documents to hand. Application is C7 then C4–C8–C9 (decision 153). |
| **C5** Forms of support | Audit tab 1 item 5 | Two records, each with its own `items` list rather than a compressed sentence — "up to six months" attaches to the living-expenses line only, and a single line would let it read as covering all support (decision 93). Emergency fund carries four covered cost types and the chip **"Set case by case"**; no figure and no range is invented, because the live page states there are none. Referral carries three connection types and no chip, so C5's "record without an amount" hint still shows here. Moves **before** the Application section — forms of support as "what this is" (decision 153). |
| **C8** Conditions / exclusions | Audit tab 1 item 6, tab 2 item 1, tab 3 item 3 | Five items: the three eligibility criteria (Arab-country origin regardless of ethnicity or citizenship and resident or abroad; artist or cultural actor; in peril or facing a tangible and verifiable threat), plus **individuals only**, promoted out of the FAQ because it is an eligibility rule and not a question (decision 94), plus the Arabic-language rule. |
| **C9** Selection | Audit tab 2 item 4, plus tab 1 | Committee structure in the label and lead — six members, artists and cultural managers with field expertise, aware of developments in the Arab region and in exile, gender-diverse and geographically distributed — and that the committee both decides and sets the level of support against how grave the case is. The five live steps go in the block's new `process` list, an ordered sequence rather than criteria, and they run to the signed agreement because there is no C10 on this page (decision 92). Stand for Art has no criteria list to render. |
| **C11** FAQs | Audit tab 3 | The six live Q&As, each answer compressed to one or two sentences: journalists (only in cultural or artistic journalism, with published work); artists not at risk (no); organisations (no — individuals only); family (support goes to the individual, family circumstances and dependents taken into account); how much (no preset figures — severity and urgency, programme budget, cost of living); visas (cannot intervene, can supply a support-certifying letter). Questions render as a plain accordion — no item borders, `text-base font-bold` (decision 142). |
| **C7 / C12** Apply / Contact | Audit §1, in-template footer | C7 remounts at the top of Application (decision 153). C7's destination hint names the encrypted JotForm and says explicitly that it is **not** `apply.mawred.org`, because the divergence is the point. C12 is its own Contact section and carries `artists@mawred.org` — the address the mailto resolves to, not the singular `artist@` the visible text shows. |
| **S2 / S3 / S4 / S5 / S6 / C10** | — | None of these blocks are on the page. No forms are published to download; past beneficiaries would contradict the programme's confidentiality; there is no above-tabs hero summary (audit §3b, as with Production Awards); there is one application path, not several; there are no rounds to reason about; and there are no installments, windows, guarantors, clawback or credit obligations in the contractual footprint — just committee decision, applicant agreement, signed agreement. |

**Rolling intake needed a state key of its own.** The `program` states key offers "Applications closed" and `Show "offered" blocks`, and C7's closed render names a next round — all three are meaningless on a page with no rounds and no S3. Stand for Art now uses a `programRolling` key with no toggles and a note explaining both absences, and `ProgramConfig.rolling` makes `ProgramPage` pass `closed={false}` regardless of a `closed` value carried in from another programme page (decision 95).

**Flagged for the client:** the contact mismatch — visible text `artist@mawred.org`, mailto `artists@mawred.org`. This is the second contact-link bug in two pages (Wijhat's footer mailto points at abbara@), so it is now a site-wide contact-link QA pass rather than a one-off. The global footer's Instagram embed is also empty with a stray `-->` comment visible, a recurring artifact worth checking sitewide.

---

## Abbara

Sources: `content/programs/audit-abbara.md` for substance; the live page (`mawred.org/cultural-organizations-management/abbara/?lang=en`) for prose length, the current round's dates and the beneficiary directory. The audit and the live page agree throughout — the 87/60/27 figures, the €22,000 and its exclusions, the eight dated stages, the five eligibility criteria, the five-member jury and its three criteria, the six application requirements, the empty English FAQ tab and the absent contractual section are all on the page as audited.

**The map is not wireframed, and the directory sits in its own section after Application** (decision 96, 153). The component register drops the live geo-map from the proposal — design clash plus limited embed-styling control — and moves impact into the stat strip, with a rebuild logged as a client-revisit option to plan only if the client elects to keep a map. So C2 carries the impact (87 · 60 · 27 · 8 rounds) and S3 is published unconditionally rather than behind the `offered` state. Application is C7 then C4–C8–C10; C5 + C6 join Overview after S1.

**One divergence from source order, acted on.** The live Timeline lists "End of contracting period: February 2028" sixth, ahead of the second workshop (2026) and the technical-assistance programme (2026–2027). S1 exists to preserve order and duration, so the eight stages render chronologically (decision 99).

| Block | From | What happened to it |
| --- | --- | --- |
| **S4** Page-top summary | Audit pre-tab header block | Renders after C1 + C2, not at page top (decision 139). The live page's two above-tab headings become the two cells, with their own bullets — "Who is Abbara for" (2 bullets) and "What kind of support does the programme offer" (4, including the €22,000 and the three service components). The port's four bare labels were placeholders borrowed from Wijhat's shape and are gone (decision 97). The malformed leading question mark on both live headings ("?Who is Abbara for") is a CMS artifact — dropped, and flagged. |
| **C1** Overview | Audit tab 1 items 1–3 | The page H1 is the programme name from the route registry; the live "What is the Abbara program?" inner heading is dropped (decision 138). Three paragraphs: the 2011 launch with the candid 87 supported / 27 ceased / 60 active statement kept as the audit insists (don't sanitise it); the two sector-context paragraphs compressed to one; then Abbara as the response, building administrative viability and asking each round's organisations to work together. The three programme goals go in the block's new `items` list rather than a block of their own (decision 102). |
| **C2** Impact figures | Audit tab 1 item 1 | The first programme whose rails all carry figures — 87 organisations supported, 60 still active, 27 ceased operating, 8 rounds since 2011. The port's "countries" rail is replaced: no country count exists for Abbara either, and the register puts the attrition figures here as the page's impact now the map is dropped. |
| **C3** At-a-glance | Audit tab 2 item 1 | Seven eligibility cards replacing four bare labels — who can apply, disciplines, where they work, budget band (US$50,000–500,000 expenditure or income), years operating (at least three), registration (unregistered initiatives may apply within transparent legal frameworks), previous beneficiaries (a prior Abbara beneficiary may not apply). Long values wrap inside the card (decision 140). Eligibility earns the strip here where the other three programmes' narrative eligibility went to C8 (decision 98); no dates in this block — Abbara's are a sequence and go to S1. |
| **S1** Programme timeline | Audit tab 1 item 6 | Eight stages in chronological order, not source order (decision 99): open call 2 Jun 2025 · deadline 17 Jul 2025 16:00 Beirut · results end Sep 2025 · first workshop week of 9–15 Nov 2025 · agreements signed Feb 2026 · second workshop during 2026 · technical assistance 2026–2027 · end of contracting Feb 2028. The block's `lead` carries the intro line — a round runs 24–30 months depending on each organisation's needs — and names the dates as the current round, Abbara 08. Date-only stages omit the body rather than rendering a fill: the live timeline is label and date only, so there is no stage prose, and a fill would look like missing copy (decision 126 revised). |
| **C4** How to apply | Audit tab 2 item 2, plus item 3 | The three portal steps as numbered cards (decision 141), each with a one-line detail. Two of the live application requirements move here as the block's `note`, below the cards, because they are cautions about working through the form rather than conditions on the application: save each section before moving on, and don't leave the attachments to the last hours before the deadline. **No documents list** — the live page names asterisked attachments without enumerating them, so nothing was invented. Application is C7 then C4–C8–C10 (decision 153). |
| **C5** Forms of support | Audit tab 1 item 4 | The four-component bundle as four records, the asymmetric pattern the register describes: only Financial support carries a chip (€22,000), so C5's "record without an amount" hint still shows on this page. Each record carries its own `items` rather than a compressed sentence — covered organisational costs and the 24-month limit; two workshops with two representatives per organisation plus online peer-learning; assistance tailored after a needs assessment; exchange visits and post-programme contact that sometimes becomes a partnership. Joins Overview after S1, with C6 (decision 153). |
| **C6** Highlighted note | Audit tab 1 item 4, financial support | The bare port label becomes the real exclusion: the €22,000 is core funding for staffing, premises and running costs, it does not cover programme activities, and it lasts no more than 24 months. Kept immediately after C5, which it qualifies; both sit in Overview (`section: 'overview'`, decision 153). Grey fill, no 3px border (decision 144). |
| **C8** Conditions / exclusions | Audit tab 2 item 3 | Six application requirements rewritten for an organisational applicant, with a lead saying the programme funds organisations and initiatives, not individuals, and that an incomplete or ineligible application is eliminated before the jury: every blank filled and every asterisked attachment included; Arabic apart from the blanks requiring English; Arabic-required documents in Arabic; no WeTransfer; figures in Euro; submitted before the deadline. Eligibility is not here — it went to C3 (decision 98). |
| **C9** Selection | Audit tab 1 items 5, 7 | The label names the five-member per-round jury (larger than Production Awards' and Wijhat's three). The lead folds in the "How it works" screening sequence — the team eliminates applications that miss the eligibility criteria, jury members assess the rest individually, then meet to discuss and select. The three live criteria become the list: future vision, goals and potential impact; organisational structure, capacities and skills; the motives for applying. |
| **C10** Contract terms | Audit tab 2 note | Held in the near-empty mode the register defines for Abbara (decision 101), carrying only the two contract facts the English page states: contracts signed after the first workshop on the basis of the first-draft strategic plan, and the funding component running no more than 24 months inside a contracting period ending February 2028 for the current round. Nothing borrowed from the other three programmes. **Gap:** see below. |
| **S3** Beneficiary directory | Live pre-tab directory | Eight round groups, Abbara 08 first down to Abbara 01, with all 56 organisation names the live directory publishes (11 · 10 · 8 · 8 · 2 · 8 · 4 · 5). The block's new `label` reads "Abbara organisations by round" rather than "Past beneficiaries", because 60 of the 87 are still active and the live heading says so (decision 100). That label is also the section h2 (decision 153). Country renders by omission — the live directory publishes none. The largest group is 11 records, under the ~12 truncation threshold, so every group renders in full. Published unconditionally: no `offered` or `offeredOnly` flags. Its own section, between Application and FAQs. |
| **C11** FAQs | Audit tab 3 | Nothing to populate — the same English gap as Production Awards and Wijhat. The live FAQ tab carries only "Available only in Arabic" and a pointer to the Arabic page, so C11 is back to its placeholder render and Stand for Art stays the exception. Questions render as a plain accordion (decision 142). |
| **C7 / C12** Apply / Contact | Audit §1, in-tab footer | C7 remounts at the top of Application (decision 153); `apply.mawred.org`. C12 is its own Contact section; abbara@mawred.org — which is the address the mailto resolves to here, confirming the Wijhat footer bug is Wijhat-side. |

Abbara has no S2 (no forms are published to download), no S5 (one application path) and no S6 (a single call per round, with the sequence shown by S1) — the last two closed as WJ-only in the register.

**A states key of its own.** The `program` key offers `Show "offered" blocks`, which would be inert here: Abbara's S3 is published content, not an offered block, so the toggle would change nothing. The route now uses a `programDirectory` key carrying the `closed` toggle only — the closed state is real, Abbara 08's call closed in July 2025 — with a note saying why the second toggle is absent (decision 103, following 95).

**Gaps, both English-side and neither filled.** The FAQ tab has no English content at all. And the English Guidelines tab has no contractual section: no installments, no signing or completion windows, no guarantors, no clawback, no citation or credit obligation. The audit flags this as a possible English content gap rather than a genuine absence — the programme plainly has contracts, since they are signed after the first workshop — so C10 states the two facts the page does carry and the rest is recorded for verification against the Arabic page, not borrowed from a sibling programme.

**Flagged for the client:** the two summary headings render with the question mark at the front, the same CMS artifact as Wijhat; several beneficiary entries are named but unlinked (`href="#"` — Hassala films, Association l'Art Vivant, Stories film Lab, Damascus Theatre Lab), a migration QA item for the directory; and the global footer's empty Instagram embed with its stray `-->` appears here too, now seen on enough pages to be a global-footer fix.

---

## Publications

Sources: `specs/publications.md` for structure and the three series intros; the live library (`mawred.org/research-publications/?lang=en`) for the 33 record titles, language lines and every facet value list; the live detail page (`mawred.org/research-publication/mawreds-initiative-lebanon-humanitarian-fund-through-cultural-organizations/?lang=en`) for the one populated record.

**One record, not 33** (decision 104). The library publishes 33 titles; only the Lebanon Humanitarian Fund report was in scope for a detail page. PA4 samples the first nine cards and an overflow line names the rest.

| Block | From | What happened to it |
| --- | --- | --- |
| **PA1** Intro line | — | Still an unused slot, behind the `slots` toggle. The live library has no intro. |
| **PA2** Filter bar | Live listing facets | All six facets carry the live value lists: 51 authors (Achraf Maher … Wedad Salloum — the live count, against the spec's ~50), 13 countries, 2 themes, 3 languages, 3 types, 14 years (2006–2016, then 2021, 2023, 2024). Long facets still render six plus "Show all N", so Author shows six real names. `Lybia` [sic] is kept as published. |
| **PA3** Count row | Live listing | Default total 30 → 33, matching the live grid. Filtered and empty states still chip Language: Arabic and Year: 2016 as wireframe stand-ins. |
| **PA4** Results grid | Live listing | 33 `{ title, languages }` records, Lebanon report first, in live order. Nine sampled cards; the overflow line reads "… 33 items in the grid" so the sample cannot be misread as the set (decision 108). |
| **PB1** Series intro | Spec §1.2–1.4, plus the live intros for length | Three series, compressed to the spec's own paragraph counts: Mudawanat three (living archive + British Council; two 2020 rounds; follow on SoundCloud), Foundations three (Cultural Policies programme since 2009; interview-video series; nine episodes by Marwa Helmy with per-episode reference links and a summary), Mawred Talks one (2021 talks series — the stray "." lines on the live page are dropped). The headline Fill becomes an unused `heading` slot; none of the three has a headline above the intro (decision 107). Outbound chips are the ones the port already carried. |
| **PB2** Media listing | Spec §1.2–1.4 | Unchanged this unit — four embed footprints plus the overflow line naming 7 / 9 / 13 items. Per-item captions stay behind the `slots` toggle. **Client call:** Foundations' intro promises per-episode reference links and a summary, which is exactly that caption slot; whether those surface on the page is not settled here. |
| **PC1** Publication record | Live Lebanon report | Title in the page-header band. Two-paragraph abstract compressed from the live single paragraph. Language `Arabic, English` and Year `2026` on the metadata table; Author, Country, Theme and Type stay fill bars because the live detail page publishes none of the taxonomy the library filters on (decision 105). Two primary buttons, English then Arabic, replacing the single "Open the document" (decision 106). |
| **PC2** Related publications | Live library titles | Three neighbouring records so "related" reads as related: Insights into Cultural Policies in Lebanon, Study on legislation governing the culture sector in Lebanon, Cultural Ecosystem in the Arab Region. How "related" is determined remains a client/build call; these three only stand the slot up. |

**Gaps, both real and both on the live detail page.** The Year facet stops at 2024 while the Lebanon record is dated April 2026 — the record is either untagged or the facet list is stale. And the detail page shows none of the taxonomy the library filters on, so four of PC1's six rows have nothing to carry.

**Flagged for the client:** the author/date line on the live detail page renders as "Beirut in 2026-06-16T…April 16th, 2026" — the same CMS artifact as the footer's "Beirut in"; `Lybia` [sic] in the Country facet; and the global-footer Instagram artifact seen on every page so far.

---

## Mawred Network

Sources: `specs/mawred-network.md` for structure; the live listing (`mawred.org/mawred-community/?lang=en`) for the intro prose and all five facet value lists; four participant records for the directory sample and the one populated record — Al Kasaba Theatre and Cinematheque, Editions Motifs, Hiba Taim, Nour Shantout.

**Four records, not several hundred** (extends decision 104). Two per entity tab, so both tabs carry real names, and Al Kasaba on the detail route.

**The record shape in the spec is wrong, and this unit corrects it.** Spec §B5 lists one involvement · programme · round · year · project per record, from the single-engagement Tarek Elazhary record. Three of the four sampled records contradict it: Al Kasaba and Editions Motifs each carry **two** engagements — Participant and Grantee on the same All Around Culture project — and the round slot holds a track name (`Cultural Alliances - Regional Workshop`) rather than `Round 02`. MN6 now renders a repeating engagement group (decision 111).

| Block | From | What happened to it |
| --- | --- | --- |
| **MN1** Intro / purpose | Live intro + page-tail notice | Two paragraphs: what the directory is (everyone Culture Resource has worked with since 2004, filterable on five fields, each entry linking to its record), then the responsible-data commitment — consent, privacy, security, ownership — promoted from the page tail per spec §B1. `communications@mawred.org` keeps its correction line. The port's headline Fill becomes an unused `heading` slot (decision 113, following 107). The live "under development" notice is retired per spec decision 2, and the `resources@mawred.org` data-access invitation goes with it — named in the hint as a client call, because it may be permanent content wearing a temporary notice. |
| **MN2** Impact strip | Live facet lists | Three of the four rails carry real figures — 55 countries, 38 programmes & initiatives, 21 years tagged across 2004–2025. **Gap:** the network's own record count. The live listing publishes no count, no pagination and no per-entity totals, so that rail keeps `00` (decision 112). |
| **MN3** Filter bank | Live facet lists | All five facets carry their real values: Involvement 8, Programme 38, Discipline 22, Country 55, Year **21** — the port had 22. Long facets still render six plus "Show all N", so Programme shows Abbara → Circairo. `Entrepeneurship` [sic] is kept as published. |
| **MN4** Count row | — | The invented `318` / `94` tab counts and the page size of 40 are gone. The row reads "Showing N records in \<tab\>" with no "of N" total (decision 112), and the filter chips become values the sample actually satisfies — Involvement: Student · Country: Morocco on the individuals tab, Programme: All Around Culture · Country: Palestine on the organisations tab — so the filtered state narrows to a real row. |
| **MN5** Listing | Four participant records | Two rows per tab: Hiba Taim (Morocco · Student · Visual Arts) and Nour Shantout (Syria, Austria · Grantee · Visual Arts); Al Kasaba Theatre and Cinematheque (Palestine · Participant + Grantee · Performing Arts, Cinema) and Editions Motifs (Algeria · Participant + Grantee · Literature). A record is one row per entity, not per engagement, so the two organisations carry two badges each. `Load 40 more` becomes `Load more records`, since the page size was invented too. All four rows link to the one populated record. |
| **MN6** Record | Live Al Kasaba record | Logo cover, then Country and Discipline as the only record-level fields, a two-paragraph body compressed from four live ones, and two engagement groups: Participant · All Around Culture · Cultural Alliances — Regional Workshop · 2021 with a project title and **no** description, then Grantee · Alliance Support · 2021 with the same title and a two-paragraph description. No descriptor row — organisations don't carry one, where Hiba Taim and Nour Shantout do ("Painter and Cake Designer", "Visual Artist") and carry no body prose instead. Both omissions render by omission (standing constraint 3). |

**The `sparse` toggle is retired** (decision 114). The populated record shows the omission natively — no descriptor, and an engagement with a title and no description — so the `record` states key carries no toggles and `NetworkRecordPage` is a server shell. `sparse` is out of `WireframeState` entirely; nothing else read it.

**Gaps.** No record count for the network or either entity type. The Year facet has no 2006, so 2004–2025 is 21 tagged years, not 22 — either a year with no records or a tagging gap. And the live listing returns **no directory rows at all** in its HTML: the several-hundred-record list is JS-rendered, which is a build concern for an index meant to be found and cited.

**Flagged for the client:** whether the `resources@` data-access ask survives the retired notice; `Entrepeneurship` [sic] in the Discipline facet, and `Libya` spelled correctly here against `Lybia` in the Publications Country facet, so the two facet lists disagree; the participant pages carry the same `Beirut in <timestamp>` artifact as every other page, plus dead decorative `Asset-2/3/4.svg` links (spec §A4) and Previous/Next record navigation that the wireframe does not reproduce.

---

## News

Sources: `specs/news.md` for structure; the live landing (`mawred.org/mawred-news/?lang=en`) for the feed shape and the 26-page pager; two live posts in full — [Results of Production Awards 2026](https://mawred.org/mawred-news/results-of-production-awards-2026/?lang=en) and [Opening of Made With Your Magic Exhibition in Beirut](https://mawred.org/mawred-news/opening-of-made-with-your-magic-exhibition-in-beirut/?lang=en) — plus the title and date of [Grantees of the Second Round of Wijhat 2026](https://mawred.org/mawred-news/grantees-of-the-second-round-of-wijhat-2026/?lang=en) for the third feed card.

**Three posts, two detail pages, two post kinds** (decision 115, extending 104 and 110). The announcement post is the results of a round: prose, then grantee and jury rosters with jury quotes. The event post is prose plus one hard fact. They are the two body shapes spec decision 3 splits on, and the sample is sized so the category tabs do real work — All 3, Announcements 2, News & Events 1.

**Two new blocks, and neither needs a plugin.** The rosters and quotes were the one thing on this page-type that had no block, and the reason to record it here is the build question they raise. `references/program-component-register.md` already commits the kit to "ACF Flexible Content or Gutenberg block patterns"; on that footing a roster is an ACF Repeater inside a block (or InnerBlocks) and an attributed quote is `core/pullquote`, which ships with WordPress. So the cost of N14 and N15 is editorial data entry, not build — mitigated by the shared-source relation spec decision 2 and `specs/mawred-network.md` §B6 already flag, since the grantee records are the same records the Network directory holds. That is why the inserts were added rather than left as prose: the structure is real, the data is already owned elsewhere, and the prose body stays available for anything that doesn't fit.

| Block | From | What happened to it |
| --- | --- | --- |
| **N1** Category filter | Live nav + spec §B1 | Tabs keep the spec's starter facets (All · Announcements · News & Events) and **lose their invented counts** — no post total is published anywhere and the taxonomy doesn't exist yet, so per-category counts cannot (decision 116, following 112). The Programme facet gains its real values: Production Awards, Wijhat, Stand for Art, Abbara. That retires the last kit rendering `"value"`, so decision 109's closing note is revised in place. The facet list and the tagging model remain a **client call** — these are the spec's starters, not a confirmed taxonomy. |
| **N2** Count row | Live listing | "Showing N posts" scoped to the open tab, **no denominator** — same gap as the Network (decision 112). The zero-results branch is now computed from the post list rather than read off the panel (decision 124): opening News & Events with the Wijhat chip active is reachable in two clicks and had been rendering an empty grid with a pager under it. |
| **N3** Post feed | Three live posts | Three real cards — title, date, category tag — newest first, followed by the overflow line "… 26 pages of posts in the archive" so three cards can't be misread as the feed (decision 108). `NewsCard` takes an optional post and, given none, renders exactly the placeholder card it did before (decision 84), which is what N13 and HM6 still get. The excerpt stays a `slots` caption: no live card publishes one. |
| **N4** Pagination | Live pager | Unchanged — 26 pages is real, server-rendered and published, the one number on this page-type that is. |
| **N5** Prev / next | — | **Left placeholder, deliberately** (decision 123). A post's neighbours come from the whole archive; the only sampled neighbour of the announcement is the Wijhat post, whose link borrows the announcement's own route, so populating it would make "next post" point at the page you are on. The hint says so. |
| **N6** Featured image | Both live posts | Gains an optional caption, carrying the announcement's image credit — which the live page prints as a trailing italic line at the very end of the body, a screen and a half from the image (decision 121). The event post publishes no credit, so its caption renders by omission. |
| **N7** Post meta strip | Both live posts | Real date and category per post, and the byline name is now the author the live posts actually carry: `Mawred`. The byline stays a **client call**, and the hint now says what the call is worth — every post is authored "Mawred", so a byline as it stands tells a reader nothing. |
| **N8** Key-detail line | Live event post | On the **event** post, not the announcement: Thursday 30 July, 6:00–9:00 pm · Beirut Art Center. Worth recording, because the block was specced as an announcement insert and it is the event post that has the harder single fact. The results announcement has no deadline and no results date left to state — it *is* the results. |
| **N9** Body | Both live posts | Announcement: three paragraphs — the round and its five disciplines, then 548 applications and 29 selected with the five selection criteria, then the country spread (11 countries, source order kept). Event post: five paragraphs compressed from five live ones — the opening, the project's chapters across Tunis / Beirut / Damascus / Jeddah / Baghdad, the maqām the title borrows, what the project reads into them, and the 22 artists with the seven-plus-seven commission split. |
| **N14** Roster *(new)* | Live announcement | One block for grantees **and** jury (decision 117). A grantee card carries name · country · role, the project title with its production stage, and a one-line description; a jury card carries the first three and the project rows render by omission (standing constraint 3). Cinema publishes five grantees and three jurors, Literature six and three. |
| **N15** Quotes *(new)* | Live announcement | One or more attributed blockquotes at 3px (decision 57), placed immediately after the jury roster they come from rather than at the post tail, because each quote is that discipline's juror on that discipline's submissions (decision 118). Cinema's two are compressed to two paragraphs each from the live five. |
| **N12** Share | Live post | Unchanged — four targets plus copy-link. Trimming the live eight is still a copy/QA call for the client. |
| **N13** Related posts | The sample | Takes real posts; each page shows the sample's other two, so "related" reads as related and no card links to the page you are on. How relatedness is actually determined stays spec decision 4 — by tag, falling back to date-proximate. Given no posts the block still renders three placeholder cards (decision 84). |

**Sampling inside the announcement** (decision 120). Cinema is published in full — five grantees, three jurors, two quotes. Literature carries its roster and jury with no quotes, which is what shows that quotes are per-discipline rather than per-post. The remaining three disciplines resolve to one line: "… Music, performing arts and visual arts follow in the same shape, each with six grantees and a three-member jury — 29 grantees across five disciplines". All five would be 29 grantee cards and fifteen juror cards on one screen; the length is a fact the overflow line can state without spending the page on it.

**Two routes and two states keys** (decision 119). `/news/post` is the announcement, on the existing `newsdetail` key; `/news/editorial-post` is the event post, on a new `newseditorial` key carrying `byline` only. The `structured` toggle is **retired** and leaves `WireframeState` altogether (decision 122, following 114): the rosters and quotes are this post's published content, so a toggle over them would exist only to take real content away. `edge` survives on the announcement because N5 is the one block on the page still standing for the template rather than for the post.

**Gaps.** No published post total and no real category taxonomy, so N2 has no denominator and the tabs carry no counts. **N10 (schedule) and N11 (CTA) now render nowhere** — they want a multi-date programme with venues and times and a registration form, which is the Living Fabric forum shape (spec §A2), and no such post was in scope. The blocks stay in the kit; the absence is a sampling gap, not a structural change. Grantee rosters overlap the Network records and the programme past-beneficiaries — the shared source is flagged, not designed. The eight-target share bar stays untrimmed pending the client's copy/QA call.

**Flagged for the client:** the announcement's country spread is published in no particular order (Egypt 3, Syria 4, Lebanon 3, Palestine 4, Yemen 1, Oman 1, Iraq 2, Morocco 2, Tunisia 6, Algeria 1, Sudan 2) — kept as sourced rather than sorted, but it reads as a list that wants an order. Both live posts repeat the title as an H2 under the featured image, which the page-header band already carries. Spec §A4's inventory flags stand: one post parented under `/uncategorized/`, "Gallery" link artifacts in the live Related strip, and the internal `Mawred News` / `SliderEN` categories doing no user-facing work. And the global-footer Instagram artifact appears on both posts, as on every page audited so far.

---

## About — Who We Are

Sources: `specs/about.md` §3.1 for the block order and §4 for the graduations; the live [Who We Are](https://mawred.org/who-we-are/?lang=en) for the three intro paragraphs, the one-line vision, the four mission points and the six labelled values; the live [History](https://mawred.org/about-us/history/?lang=en) for the dated narrative the timeline is built from.

**Two live pages into one, which is the merge the spec asks for.** Who We Are supplies AB1–AB4, History supplies AB5. The join is real rather than nominal: History's opening paragraphs and its one undated passage don't fit timeline rows, so they become AB5's lead, and the page reads identity-then-origin without a second H1 in the middle.

**AB1–AB5 take content directly, with no fill-bar fallback** (decision 125). Every other populated block this task has touched carries an optional content prop because it is shared across page-types and populated one unit at a time (decision 84). These five have exactly one consumer each — `WhoWeArePage` — so the fallback would have been unreachable code.

| Block | From | What happened to it |
| --- | --- | --- |
| **AB1** Intro | Live Who We Are, 3 paragraphs | The page H1 and crumb are `PageHeaderBand` (decision 152); AB1 is the three intro paragraphs only. Kept as prose — the spec's anchor statement, and the one block on the page that doesn't graduate. Three paragraphs held at three: what Culture Resource is and what it believes; the range of services and programmes, ending on the "public good and a basic human right" line the live page makes its own; then staff and governance — a general assembly of 35 cultural leaders from the Arab region and Belgium, and a seven-member artistic board. That third paragraph is the passing sentence spec §3.2 says is Our Team's only existing context, so it is left here **and** noted as the sentence Our Team inherits whenever it is populated — deferred 2026-09-01, no longer the next unit. |
| **AB2** Vision | Live Who We Are, vision line | One sentence, verbatim: "An Arab region where cultural diversity is celebrated, and creativity, inclusivity, and equity are fostered." Heading and sentence sit in a `bg-neutral-200` padded band, same fill as C6 (decision 148) — not the 3px quotation weight N8 and N15 keep. |
| **AB3** Mission | Live Who We Are, 4 mission points | The four points in live order, each compressed to one sentence, as a wrapping gapped card grid matching AB4 (decision 149). The `0N` eyebrow stays: the points are not ranked, but the numbering is what makes four cards read as one set rather than four claims. Cells (S4, HM4) stay a tight shared-border row. |
| **AB4** Values | Live Who We Are, 6 values | Six `{ label, text }` pairs — Community engagement and inclusiveness · Collaboration · Trust · Diversity · Transparency · Support and preservation — each description cut to one sentence from the live two or three. The live page renders the first as `inclusiveness:we believe`, a missing space after the colon; it is a CMS artifact and the label is rendered clean. |
| **AB5** History timeline | Live History, in full | The headline graduation (spec §4). The lead carries two paragraphs: the arc the rail covers (programmes launched, institutions co-founded and spun off, a base that moved Cairo → Beirut), and the joint initiatives that sit on no single year — Tunisia Balad El Fann with the Tunisian Ministry of Culture, Tandem Shaml with the European Cultural Foundation and MitOst, the Redzone Festival with KKV in Norway (decision 127). Then eleven dated rows, each a year plus a milestone plus a paragraph: 2003 founding and registration in Belgium · 2004 Awwal Rabie, Production Awards, Youth Platforms · 2005 Mawa3eed, El Genaina Theater, Cultural Management Training · 2006 AFAC co-founded and co-directed to 2009 · 2009 Cultural Policy Research · 2010 the DAAS music-education pilot · 2012 Abbara, and Imkan added to training · 2013 Action for Hope incubated, independent since 2015 · 2013–14 the master's feasibility study with the first class in October 2018 · 2016–17 the Egypt programmes spun off and the base moved to Beirut · 2017 Kon Ma' Al-Fann and mobility grants beyond the region. A twelfth row spans **2018–2026** and is labelled “Not covered by the published history”, with no fill bar (decision 128 revised). The live page's closing rhetorical paragraph ("stories still to be told") is dropped as voice rather than content — copy-rewrite owns it. |

`Timeline` renders `text` if present; else Fill only when `fill` is passed (decision 126 revised). AB5's 2018–2026 gap row is labelled only — no `fill` (decision 150). Abbara's S1 — and Wijhat's and Production Awards' — are label-and-date only, so those stages omit the body rather than looking unpopulated.

**The gap, and it is nine years wide.** The published history stops at 2017. Everything since — the master's programme past its first class, the move through the Lebanon crises, All Around Culture, the 20th anniversary — is unwritten, so the rail ends on a labelled row (`2018–2026` — Not covered by the published history) rather than on 2017 (decision 128). The fill bar under that row is dropped: the label already names the gap, and the grey rect read as leftover chrome (decision 150). Synthesising it from the news archive was available and not taken: a history is what an organisation says about itself, and the wireframe's job is to show that nine years of that are missing.

**Flagged for the client:** the value label renders as `inclusiveness:we believe` on the live page, a missing space that also suggests the values were pasted rather than authored in the field; the history is a single unbroken wall of dated prose with no headings, which is why it is the graduation the spec names first; and the same global-footer Instagram artifact appears here as on every page audited so far. The Our Team page name is still an open call (spec §5.1) and is surfaced in that page's hint, not resolved here.

### Our Team

Not populated. Crumb + H1 are `PageHeaderBand` in the shell (decision 152). **AB6 is omitted** — it was a Fill standing for net-new micro-copy that does not exist; omission until copy exists. AB6 remains on Supporters / Past Initiatives / Careers behind `slots`. **AB7** cards have no outer frame: photo box keeps its border; name / role / country sit under it. Records with a bio carry a disclosure arrow next to the name (`▸` / `▾`); records without one stay static (decision 151). Team-section copy is still net-new — Mawred supplies it.

---

## Home

Sources: `specs/home.md` for structure (the live Home was audited there in §1 and was not re-read); the populated inner page-types for every figure, date, name and paragraph — `lib/pages/about.ts` (`WHO_WE_ARE_INTRO`), `lib/pages/programs.ts` (four C1 overviews, Wijhat's rounds, Production Awards' deadline), `lib/pages/news.ts` (three posts and the exhibition body's first two sentences), `lib/pages/publications.ts` (`LIBRARY_RECORDS`, `MUDAWANAT`), `lib/pages/network.ts` (`NETWORK_FIGURES` and the sample records). Home surfaces those kits and writes back to none of them.

**Three findings that shaped the unit.** Spec §3B's "~400 grants since 2004" is Production Awards alone, and 87 organisations is Abbara's — no Mawred-wide grant total is published, so that rail keeps `00` (decision 131). The live Home's "Wijhat is open all year" is wrong against the populated Wijhat page, which has three rounds with deliberately year-less deadlines; HM3 says applications year-round, decided in three rounds, and invents no year. Stand for Art is open all year, so `nocalls` is unreachable — kept as a template branch because spec §3C requires the empty to be laid out (decision 130).

| Block | From | What happened to it |
| --- | --- | --- |
| **HM1** Cinematic hero | Who We Are intro[0]; Production Awards 2026 roster; Network sample | Positioning line is the published identity sentence tightened to hero length — founding year, artistic creativity, exchange within and beyond the region, artists' right to freedom of expression; the heritage / civil-society / access clauses drop, and graduating the line stays a **copy call**. Five frames, so the marker bar counts something real: Alayham Ali, Sarah Zeryab and Karima Ahdad from the 2026 results post, then Al Kasaba and Nour Shantout from the Network sample; frame 1's caption renders. The trailing headline Fill retires (decisions 107/113). Wayfinding labels unchanged — "Grants & Opportunities" vs "Programs" is still a **client call**. |
| **HM2** Impact strip | Network facet counts; founding year | Rails re-cut to `00` grants awarded since 2003 · `55` countries · `38` programmes & initiatives · `23` years since 2003. 55 and 38 are the Network facet counts; 23 is years since founding, not Network's 21 tagged years. **Gap:** no Mawred-wide grant total, so the first rail keeps `00` rather than borrowing Production Awards' ~400 or Abbara's 87 (decision 131). Mosaic untouched — tiles stay uncaptioned (decision 37). **Which figures lead is a client call.** |
| **HM3** Open opportunities | Four programme C1s; live call status | Three cards, each a status badge · name · C1-compressed descriptor → programme page. Production Awards: deadline 19 Oct 2026, 16:00 Beirut. Wijhat: applications year-round, decided in three rounds. Stand for Art: open all year, no rounds, no deadlines. Abbara is absent because Abbara 08 closed in July 2025. Descriptors are not written here — they come from `HOME_PROGRAMMES` (decision 132). |
| **HM4** Programmes overview | Four programme C1s | The four names with the same descriptors HM3 uses, as a compact text row (decision 40). `HOME_PROGRAMMES` is the single source; a programme with no `status` (Abbara) still appears here. An open programme is in both strips on purpose: HM3 says act now, HM4 says here is the whole offer. |
| **HM5** Featured / spotlight | Exhibition post (`MADE_WITH_YOUR_MAGIC`) | Default feature is Made With Your Magic, Beirut — two sentences compressed from that post's body (the opening with Tarek Abou El Fetouh; the 22 artists and the Tunis / Beirut / Damascus / Jeddah / Baghdad chapters), linking to `/news/editorial-post`. The `twentieth` state stays structural (decision 133): `/mawreds-20th` is still a stub and no campaign content is sourced, so that branch is fill bars, recorded as a **gap**. Whether the 20th is permanent nav or a temporary campaign is still a **client call**. |
| **HM6** Latest news | News sample (decision 115) | The three real posts via `NewsCard`, each linking to its own route — Made With Your Magic (27 July, editorial), Wijhat second-round grantees (20 July, announcement route), Production Awards 2026 results (6 May). The port's four-card strip is gone (`HOME_NEWS_COUNT` 4 → 3). |
| **HM7** Latest from Publications | First three `LIBRARY_RECORDS`; `MUDAWANAT` | One combined strip (decision 41): Lebanon Humanitarian Fund report (Arabic, English), Cultural Ecosystem in the Arab Region (Arabic, English, French), New Voices in Cultural Policies (Arabic, English), plus a Mudawanat card tagged Episode. **Gap:** no episode titles are sourced, so the media card carries the series (`Podcast series · 7 episodes`), not an episode. Type tags because the strip mixes two kinds (decision 43). |
| **HM8** Identity lead-in | Who We Are intro[1] | One paragraph — the services-and-public-good cut, so it does not repeat HM1's sentence — and a route into Who We Are. The heading Fill retires (decisions 107/113); the H1 is the destination's page-header band (decision 152). |

`Btn` gained an optional `href` this unit (decision 129). Home's wayfinding, "Read more", "All news", "All publications" and "Who we are" pass it; every other call site still renders a `<button>`.

**Gaps.** No Mawred-wide grant total. No 20th-anniversary campaign copy, and `/mawreds-20th` is still a stub. No Mudawanat episode titles.

**Flagged for the client:** the live Home's "Wijhat is open all year" is not reproduced, because the populated Wijhat page has three rounds; the wayfinding label, which figures lead, graduating the hero line, and the 20th-as-nav-or-campaign question remain open; and the `nocalls` empty state's "See all programmes" action is still a button without an href — `EmptyState` takes a label only (decision 73), and the branch is unreachable.
